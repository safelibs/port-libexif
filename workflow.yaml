linear: true
yaml_source_mode: inline-only
verifier_encoding: explicit-phases
phases:
  - id: impl_01_abi_packaging
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_01_abi_packaging` (ABI, Headers, Packaging, and Reusable Package Artifacts).
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Preexisting Inputs
      - `original/libexif/Makefile.am`
      - `original/configure.ac`
      - `original/libexif/libexif.sym`
      - `original/libexif/exif-tag.c`
      - `original/debian/libexif12.symbols`
      - `safe/Cargo.toml`
      - `safe/Cargo.lock`
      - `safe/COPYING`
      - `safe/README`
      - `safe/NEWS`
      - `safe/SECURITY.md`
      - `safe/build.rs`
      - `safe/cshim/`
      - `safe/src/ffi/`
      - `safe/src/lib.rs`
      - `safe/src/primitives/`
      - `safe/src/runtime/`
      - `safe/src/object/`
      - `safe/src/parser/`
      - `safe/src/tables/`
      - `safe/src/mnote/`
      - `safe/include/libexif/`
      - `safe/libexif.pc.in`
      - `safe/libexif-uninstalled.pc.in`
      - `safe/debian/`
      - `safe/po/`
      - `safe/contrib/examples/`
      - `safe/doc/libexif-api.html/`
      - `safe/tests/abi_layout.rs`
      - `safe/tests/run-package-build.sh`
      - `safe/tests/run-export-compare.sh`
      - `safe/tests/run-c-compile-smoke.sh`
      - `safe/tests/run-original-object-link-compat.sh`
      - `safe/tests/run-performance-compare.sh`
      
      New Outputs
      - Updated Rust ABI/export implementation in `safe/src/ffi/{types.rs,panic_boundary.rs}`, `safe/src/lib.rs`, and the owning exported modules under `safe/src/{primitives,runtime,object,parser,tables,mnote}/` when `abi_layout`, export comparison, or C compile smoke exposes a layout or symbol mismatch
      - Updated `safe/build.rs` if the export map, SONAME flags, or build-time env propagation need correction
      - Updated `safe/debian/*` packaging metadata and install rules
      - Updated `safe/tests/abi_layout.rs`
      - Phase-local reusable package artifact root contract rooted at `safe/.artifacts/<implement-phase-id>/`, including `metadata/source-commit.txt`, `metadata/package-inputs.sha256`, `metadata/validated.ok`, and the non-mutating metadata-print path needed by verifiers
      - Deterministic compile-smoke outputs under `safe/.artifacts/<implement-phase-id>/compile-smoke/{shared,static}/`, including the forced-static `public-api-smoke-static` artifact that verifiers inspect directly
      - Updated package-backed wrapper scripts that reuse `PACKAGE_BUILD_ROOT` instead of rebuilding the same current-commit artifacts, and that turn `LIBEXIF_REQUIRE_REUSE=1` into checker-visible failures on accidental rebuilds
      
      File Changes
      - `safe/build.rs`
      - `safe/Cargo.toml` and `safe/Cargo.lock` only if build configuration or helper dependencies change
      - `safe/src/ffi/{types.rs,panic_boundary.rs}`
      - `safe/src/lib.rs`
      - `safe/src/primitives/{byte_order.rs,format.rs,ifd.rs,utils.rs}`
      - `safe/src/runtime/{log.rs,mem.rs}`
      - `safe/src/object/{content.rs,data.rs,entry.rs}`
      - `safe/src/parser/{data_load.rs,data_save.rs,loader.rs}`
      - `safe/src/tables/{data_options.rs,tag_table.rs}`
      - `safe/src/mnote/{mod.rs,base.rs,apple.rs,canon.rs,fuji.rs,olympus.rs,pentax.rs}`
      - `safe/include/libexif/*.h`
      - `safe/libexif.pc.in`
      - `safe/libexif-uninstalled.pc.in`
      - `safe/debian/{rules,control,libexif12.symbols,libexif12.install,libexif-dev.install,libexif-dev.docs,libexif-doc.docs,libexif-doc.examples,libexif-doc.doc-base,changelog}`
      - `safe/tests/{abi_layout.rs,run-package-build.sh,run-export-compare.sh,run-c-compile-smoke.sh,run-original-object-link-compat.sh,run-performance-compare.sh}`
      
      Implementation Details
      - Consume or update the listed artifacts in place. Do not refetch, recollect, rediscover, or regenerate them from scratch.
      - Preserve the existing ABI mirror strategy in `safe/src/ffi/types.rs`; fix layout or naming drift only if `abi_layout` or export checks prove a mismatch.
      - Handle public ABI and export-surface fixes in this phase. If `cargo test --release --test abi_layout` fails, update `safe/src/ffi/{types.rs,panic_boundary.rs}`. If `run-export-compare.sh` or `run-c-compile-smoke.sh` reveals missing, extra, or mis-exported public symbols, fix the owning Rust module and any `safe/src/lib.rs` export glue here instead of deferring the change.
      - Preserve the original header install set from `original/libexif/Makefile.am`. Do not replace copied headers with generated Rust output.
      - Preserve the original symbol or version contract from `original/libexif/libexif.sym` and `original/debian/libexif12.symbols`. If a symbol mismatch is found, fix the Rust export surface or packaging; do not silently edit the manifest unless the original manifest itself is provably wrong for this workspace.
      - Preserve Debian package ownership and install paths: runtime library under `libexif12`, headers, pkg-config data, and static archive under `libexif-dev`, docs, examples, and doc-base metadata under `libexif-doc` with files still living under `/usr/share/doc/libexif-dev/`.
      - Consume the existing API docs tree in place. Do not regenerate `safe/doc/libexif-api.html/` while it already exists; package and validate the checked-in tree.
      - Keep the real shared object file `libexif.so.12.3.4` plus direct `libexif.so.12` and `libexif.so` symlinks.
      - Make `safe/tests/run-package-build.sh` idempotent for a populated `PACKAGE_BUILD_ROOT`. It must record `metadata/source-commit.txt`, `metadata/package-inputs.sha256`, and `metadata/validated.ok`, it must expose `--print-package-inputs-manifest` for non-mutating checker diffs, and it may reuse an existing root only when those files still match the current workspace and the extracted package validation checks still pass.
      - `safe/tests/run-package-build.sh` must store `metadata/package-inputs.sha256` as the exact sorted `sha256sum`-style manifest, with one `<sha256><two spaces><repo-relative-path>` line per input file, over every package-affecting file: `safe/{Cargo.toml,Cargo.lock,build.rs,COPYING,README,NEWS,SECURITY.md,libexif.pc.in,libexif-uninstalled.pc.in}`, `safe/src/**`, `safe/cshim/**`, `safe/include/**`, `safe/debian/**`, `safe/po/**`, `safe/contrib/examples/**`, `safe/doc/libexif-api.html/**`, `safe/tests/run-package-build.sh`, and any `original/**` file still consumed by the safe build such as `original/libexif/libexif.sym` and `original/libexif/exif-tag.c`.
      - `safe/tests/run-package-build.sh` validation must explicitly include `libexif-dev/usr/lib/$multiarch/libexif.a` and `root/usr/lib/$multiarch/libexif.a`. Do not treat the static archive as implied by the `.deb` build alone.
      - `safe/tests/run-c-compile-smoke.sh` must prove both shared-link and static-link compatibility from the packaged root. At least one smoke target must be forced to link against the packaged `libexif.a`, and the resulting binary must be written to `safe/.artifacts/<implement-phase-id>/compile-smoke/static/public-api-smoke-static`, must execute successfully, and must have no `NEEDED` entry for `libexif.so.12` in `readelf -d` output.
      - If the metadata or extracted-package validation no longer matches, `safe/tests/run-package-build.sh` must rebuild that root in place before any wrapper continues. If `LIBEXIF_REQUIRE_REUSE=1` is set, it must fail instead of rebuilding. This invalidation behavior is part of this phase.
      - When `LIBEXIF_REQUIRE_REUSE=1` blocks a missing or stale phase-local package root, `safe/tests/run-package-build.sh` must emit the exact substring `reuse-required package root is missing or stale`. Package-backed wrappers that delegate to it must preserve that substring so verifiers can assert the contract without inferring why the command failed.
      - `safe/tests/run-export-compare.sh`, `safe/tests/run-c-compile-smoke.sh`, `safe/tests/run-original-object-link-compat.sh`, and `safe/tests/run-performance-compare.sh` must preserve inherited `LIBEXIF_REQUIRE_REUSE=1` so later verifiers can prove wrapper-level reuse instead of a silent nested rebuild through `run-package-build.sh`. This phase covers missing-root and stale-root negative coverage for `run-export-compare.sh`.
      - Treat serial execution as part of the implementation. If a failure only reproduces under concurrent package builds, fix the workflow or harness ordering rather than adding retry loops.
      
      Success Criteria
      - `cargo test --release --test abi_layout`
      - `PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_01_abi_packaging bash tests/run-package-build.sh`
      - `bash tests/run-package-build.sh --print-package-inputs-manifest` must diff cleanly against `/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_01_abi_packaging/metadata/package-inputs.sha256`
      - `test -f /home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_01_abi_packaging/libexif-dev/usr/lib/$(dpkg-architecture -qDEB_HOST_MULTIARCH)/libexif.a`
      - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_01_abi_packaging bash tests/run-export-compare.sh`
      - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_01_abi_packaging bash tests/run-c-compile-smoke.sh`
      - `/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_01_abi_packaging/compile-smoke/static/public-api-smoke-static` must exist, execute successfully, and have no `NEEDED` entry for `libexif.so.12` in `readelf -d`
      - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_01_abi_packaging.missing bash tests/run-package-build.sh` must fail with `reuse-required package root is missing or stale`
      - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_01_abi_packaging.missing bash tests/run-export-compare.sh` must fail with `reuse-required package root is missing or stale`
      - A copied stale root made from `/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_01_abi_packaging` with mismatched `metadata/source-commit.txt` must cause `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT=<stale-copy> bash tests/run-export-compare.sh` to fail with `reuse-required package root is missing or stale`
      
      Git Commit Requirement
      - Commit the phase work to git before yielding. Do not yield with uncommitted changes.
  - id: check_01_abi_tester
    type: check
    bounce_target: impl_01_abi_packaging
    prompt: |-
      Reviewer role: Tester for `ABI, Headers, Packaging, and Reusable Package Artifacts` in check phase `check_01_abi_tester`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_01_abi_tester`. Do not modify files.
      
      Specific Things to Verify
      - verify the original ABI, the installed development surface including `libexif.a`, Debian packages and docs, and the reusable phase-local package-root contract for the root builder and export wrapper.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_01_abi_packaging
      expected_manifest=$(mktemp)
      missing_root_log=$(mktemp)
      missing_export_log=$(mktemp)
      stale_export_log=$(mktemp)
      static_smoke="$PACKAGE_BUILD_ROOT/compile-smoke/static/public-api-smoke-static"
      missing_root="${PACKAGE_BUILD_ROOT}.missing"
      stale_root="${PACKAGE_BUILD_ROOT}.stale"
      trap 'rm -f "$expected_manifest" "$missing_root_log" "$missing_export_log" "$stale_export_log"; rm -rf "$stale_root"' EXIT
      cargo test --release --test abi_layout
      PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh
      multiarch=$(dpkg-architecture -qDEB_HOST_MULTIARCH)
      test -f "$PACKAGE_BUILD_ROOT/libexif-dev/usr/lib/$multiarch/libexif.a"
      test -f "$PACKAGE_BUILD_ROOT/root/usr/lib/$multiarch/libexif.a"
      test "$(cat "$PACKAGE_BUILD_ROOT/metadata/source-commit.txt")" = "$(git -C /home/yans/code/safelibs/ported/libexif rev-parse HEAD)"
      bash tests/run-package-build.sh --print-package-inputs-manifest >"$expected_manifest"
      diff -u "$expected_manifest" "$PACKAGE_BUILD_ROOT/metadata/package-inputs.sha256"
      test -f "$PACKAGE_BUILD_ROOT/metadata/validated.ok"
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh >/dev/null
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-export-compare.sh
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-c-compile-smoke.sh
      test -x "$static_smoke"
      "$static_smoke"
      ! readelf -d "$static_smoke" | grep -F 'libexif.so.12'
      rm -rf "$missing_root"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_root" bash tests/run-package-build.sh >"$missing_root_log" 2>&1; then
        cat "$missing_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_root_log"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_root" bash tests/run-export-compare.sh >"$missing_export_log" 2>&1; then
        cat "$missing_export_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_export_log"
      rm -rf "$stale_root"
      cp -a "$PACKAGE_BUILD_ROOT" "$stale_root"
      printf 'stale\n' >"$stale_root/metadata/source-commit.txt"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$stale_root" bash tests/run-export-compare.sh >"$stale_export_log" 2>&1; then
        cat "$stale_export_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$stale_export_log"
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_01_abi_senior
    type: check
    bounce_target: impl_01_abi_packaging
    prompt: |-
      Reviewer role: Senior reviewer for `ABI, Headers, Packaging, and Reusable Package Artifacts` in check phase `check_01_abi_senior`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_01_abi_senior`. Do not modify files.
      
      Specific Things to Verify
      - review ABI or packaging drift against the original manifests, confirm the static archive remains packaged and linkable, and confirm wrapper reuse of the fixed phase-local package root.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_01_abi_packaging
      expected_manifest=$(mktemp)
      static_smoke="$PACKAGE_BUILD_ROOT/compile-smoke/static/public-api-smoke-static"
      trap 'rm -f "$expected_manifest"' EXIT
      multiarch=$(dpkg-architecture -qDEB_HOST_MULTIARCH)
      test -f "$PACKAGE_BUILD_ROOT/libexif-dev/usr/lib/$multiarch/libexif.a"
      bash tests/run-package-build.sh --print-package-inputs-manifest >"$expected_manifest"
      diff -u "$expected_manifest" "$PACKAGE_BUILD_ROOT/metadata/package-inputs.sha256"
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-export-compare.sh
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-c-compile-smoke.sh
      test -x "$static_smoke"
      "$static_smoke"
      ! readelf -d "$static_smoke" | grep -F 'libexif.so.12'
      git show --stat --format=fuller HEAD
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_02_core_behavior
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_02_core_behavior` (Core Parser, Tables, Object Model, and Existing Regression Surface).
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Preexisting Inputs
      - `safe/build.rs`
      - `safe/src/lib.rs`
      - `safe/src/ffi/types.rs`
      - `safe/src/i18n.rs`
      - `safe/src/primitives/`
      - `safe/src/runtime/`
      - `safe/src/object/`
      - `safe/src/parser/`
      - `safe/src/tables/`
      - `safe/src/mnote/`
      - `safe/tests/primitives_tables.rs`
      - `safe/tests/object_model.rs`
      - `safe/tests/cve_regressions.rs`
      - `safe/tests/ported_c.rs`
      - `safe/tests/run-original-test-suite.sh`
      - `safe/tests/run-c-test.sh`
      - `safe/tests/run-original-shell-test.sh`
      - `safe/tests/run-original-nls-test.sh`
      - `safe/tests/run-test-mnote-matrix.sh`
      - `safe/tests/original-c/`
      - `safe/tests/original-sh/`
      - `safe/tests/support/`
      - `safe/tests/testdata/`
      - `original/libexif/exif-tag.c`
      - `relevant_cves.json`
      - `safe/tests/cve-regressions-manifest.txt`
      
      New Outputs
      - Updated Rust logic in primitives, runtime, object, parser, table, i18n, and current helper-backed MakerNote modules when the copied upstream suite exposes a compatibility bug
      - Updated or additional minimal regressions in `safe/tests/`
      - Updated copied original test harnesses, shell or NLS runners, nested NLS fixtures, or generated tag-metadata wiring only if needed to preserve the intended original behavior contract
      
      File Changes
      - `safe/build.rs`
      - `safe/src/lib.rs`
      - `safe/src/i18n.rs`
      - `safe/src/primitives/{byte_order.rs,format.rs,ifd.rs,utils.rs}`
      - `safe/src/runtime/{mem.rs,log.rs,cstdio.rs}`
      - `safe/src/object/{content.rs,data.rs,entry.rs}`
      - `safe/src/parser/{data_load.rs,data_save.rs,loader.rs,limits.rs,mod.rs}`
      - `safe/src/tables/{mod.rs,data_options.rs,gps_ifd.rs,tag_table.rs}`
      - `safe/src/mnote/{mod.rs,base.rs,apple.rs,canon.rs,fuji.rs,olympus.rs,pentax.rs}`
      - `safe/tests/{primitives_tables.rs,object_model.rs,cve_regressions.rs,ported_c.rs,run-original-test-suite.sh,run-c-test.sh,run-cve-regressions.sh,run-original-shell-test.sh,run-original-nls-test.sh,run-test-mnote-matrix.sh}`
      - `safe/tests/original-c/**`
      - `safe/tests/original-sh/**`
      - `safe/tests/support/**`
      
      Implementation Details
      - Consume or update the listed artifacts in place. Do not refetch, recollect, rediscover, or regenerate them from scratch.
      - Preserve the ABI-visible public-struct semantics. C callers are still allowed to observe and mutate fields such as `ExifData::ifd`, `ExifData::data`, `ExifContent::entries`, `ExifEntry::data`, and `ExifEntry::parent`.
      - Use the existing regression harnesses as the driver for fixes: null handling, default-tag insertion, byte-order conversion, `exif_loader_get_buf`, `exif_data_fix`, `exif_content_fix`, `exif_entry_get_value`, parse or save symmetry, table-string behavior, tag-table ordering or lookup, current helper-backed MakerNote behavior exercised by `run-original-test-suite.sh`, NLS or localedir behavior, and shell-script compatibility.
      - Handle any compatibility failure exposed by `bash tests/run-original-test-suite.sh` in this phase. Do not defer such failures to phase 3 just because the exercised path touches `safe/src/mnote/*`; phase 3 is reserved for removing vendor-helper C and making those MakerNote paths Rust-owned after they are behaviorally correct.
      - If the copied-suite failure comes from generated tag metadata or gettext or localedir wiring, fix the extractor or env propagation in `safe/build.rs` and keep the resulting behavior covered by `primitives_tables`, `test-tagtable`, or the NLS runner rather than inventing a separate phase.
      - Preserve checked arithmetic and parser budgets in `safe/src/parser/limits.rs`; expand them only when a legitimate compatibility case requires it and a regression test proves the new bound.
      - Preserve parser tests that require explicit fixtures. Keep the existing explicit fixture argv and `TEST_IMAGES` contracts in the copied parser runners instead of weakening them into rediscovery or implicit fixture enumeration.
      - When a bug is found via an upstream-style C test or shell script, add or tighten the smallest possible regression under `safe/tests/` so the issue becomes locally reproducible without depending solely on the full suite.
      - Keep the locale-pinned environment in every runner that compares safe and original behavior.
      - Preserve the copied shell and NLS fixtures in place. Update nested `safe/tests/original-c/**` or `safe/tests/original-sh/**` only when that preserves the original test intent rather than papering over a library bug.
      - Prefer safe helper functions over widening raw-pointer manipulation or adding new `unsafe`.
      
      Success Criteria
      - `cargo test --release --test primitives_tables`
      - `cargo test --release --test object_model`
      - `cargo test --release --test cve_regressions`
      - `bash tests/run-original-test-suite.sh`
      
      Git Commit Requirement
      - Commit the phase work to git before yielding. Do not yield with uncommitted changes.
  - id: check_02_core_tester
    type: check
    bounce_target: impl_02_core_behavior
    prompt: |-
      Reviewer role: Tester for `Core Parser, Tables, Object Model, and Existing Regression Surface` in check phase `check_02_core_tester`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_02_core_tester`. Do not modify files.
      
      Specific Things to Verify
      - verify the Rust object model, parser, scalar helpers, table metadata and strings, helper-backed MakerNote behavior exercised by the copied upstream suite, shell regressions, the NLS surface, and current CVE regressions.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      cargo test --release --test primitives_tables
      cargo test --release --test object_model
      cargo test --release --test cve_regressions
      bash tests/run-original-test-suite.sh
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_02_core_senior
    type: check
    bounce_target: impl_02_core_behavior
    prompt: |-
      Reviewer role: Senior reviewer for `Core Parser, Tables, Object Model, and Existing Regression Surface` in check phase `check_02_core_senior`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_02_core_senior`. Do not modify files.
      
      Specific Things to Verify
      - review fixes for parser, table, i18n, helper-backed MakerNote, and object correctness, ensure each failure has a minimal regression, and confirm the locale-pinned original-suite contract stayed intact.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      bash tests/run-original-test-suite.sh
      git show --stat --format=fuller HEAD
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_03_makernote_rust_completion
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_03_makernote_rust_completion` (MakerNote Rust Completion).
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Preexisting Inputs
      - `safe/build.rs`
      - `safe/cshim/exif-log-shim.c`
      - `safe/src/mnote/`
      - `original/libexif/apple/`
      - `original/libexif/canon/`
      - `original/libexif/fuji/`
      - `original/libexif/olympus/`
      - `original/libexif/pentax/`
      - `safe/tests/run-original-test-suite.sh`
      - `safe/tests/run-test-mnote-matrix.sh`
      - `safe/tests/original-c/test-apple-mnote.c`
      - `safe/tests/original-c/test-fuzzer.c`
      - `safe/tests/original-c/test-fuzzer-persistent.c`
      - `safe/tests/original-c/test-mnote.c`
      - `safe/tests/testdata/`
      - `safe/tests/cve_regressions.rs`
      
      New Outputs
      - Rust implementations of the vendor MakerNote helpers that currently come from `build.rs`
      - Simplified `safe/build.rs` with vendor-helper C compilation removed after the Rust replacements are complete
      - Additional MakerNote regressions or fixtures if downstream behavior requires them
      - Updated copied upstream MakerNote-sensitive runners or focused C tests only when preserving their original regression intent requires harness maintenance alongside the Rust port
      - Updated `test-original.sh` usage or help text only if the reason it stages `original/` changes
      
      File Changes
      - `safe/build.rs`
      - `safe/src/mnote/{mod.rs,base.rs,apple.rs,canon.rs,fuji.rs,olympus.rs,pentax.rs}`
      - `safe/src/object/data.rs` and `safe/src/parser/{data_load.rs,data_save.rs}` if MakerNote integration points need adjustment
      - `safe/tests/{cve_regressions.rs,run-original-test-suite.sh,run-test-mnote-matrix.sh,run-c-test.sh}`
      - `safe/tests/original-c/{test-apple-mnote.c,test-fuzzer.c,test-fuzzer-persistent.c,test-mnote.c}`
      - `safe/tests/testdata/*` only if new MakerNote fixtures are truly needed
      - `test-original.sh` only if its usage or staging comments must describe a narrower remaining `original/` dependency
      
      Implementation Details
      - Consume or update the listed artifacts in place. Do not refetch, recollect, rediscover, or regenerate them from scratch.
      - Port the remaining vendor-specific MakerNote identification, load or save, enumeration, and formatting logic from the vendored C helper sources into Rust.
      - Replace the concrete helper seams already visible in the current tree: `safe/src/mnote/apple.rs` and `safe/src/mnote/fuji.rs` are thin helper trampolines only, while `safe/src/mnote/canon.rs`, `safe/src/mnote/olympus.rs`, and `safe/src/mnote/pentax.rs` expose public ABI entry points that still call `safe_helper_*` foreign functions. Phase 3 removes those helper dependencies while keeping the Canon, Olympus, and Pentax exported symbol surface unchanged.
      - Preserve the exported ABI and visible behavior of `exif_mnote_data_canon_new`, `exif_mnote_data_olympus_new`, `exif_mnote_data_pentax_new`, `mnote_canon_entry_get_value`, `mnote_olympus_entry_get_value`, `mnote_pentax_entry_get_value`, and the associated tag metadata functions.
      - Keep `ExifMnoteDataMethods` and `ExifMnoteData` layout-compatible with the original `exif-mnote-data-priv.h` contract.
      - Keep the existing copied upstream MakerNote-sensitive checks in scope because helper removal can regress parser-adjacent MakerNote behavior: `run-test-mnote-matrix.sh`, `test-apple-mnote`, and `test-fuzzer` as orchestrated by `bash tests/run-original-test-suite.sh`. Do not defer that coverage to phase 9.
      - Phase-3 verifiers must execute `bash tests/run-test-mnote-matrix.sh` explicitly in addition to `bash tests/run-original-test-suite.sh`. Do not rely on the wrapper suite alone to prove the dedicated MakerNote matrix still runs after helper-removal edits.
      - Delete `build.rs` MakerNote helper compilation once the Rust replacements are in place. The only planned remaining C shim should be the narrow variadic logging boundary, unless that can also be removed safely.
      - The helper-removal proof in this phase must target the real vendored build inputs and the surviving C-build structure, not just today's helper symbol names. The phase-3 verifiers must reject any remaining `build.rs` reference to `../original/libexif/{apple,canon,fuji,olympus,pentax}/*.c`, any `cargo:rerun-if-changed` for those files, and any helper-builder scaffolding such as `compile_mnote_helper`, `build_mnote_helpers`, `emit_mnote_rerun_hints`, or `safe_helper_*` wrapper names. They must also prove that `build.rs` compiles exactly one C source file via `cc::Build`, namely `cshim/exif-log-shim.c`; no extra `.file(...)` or `.files(...)` calls may remain.
      - Removing vendor-helper compilation does not authorize removing every `../original` build input. `build.rs` may continue consuming `original/libexif/libexif.sym` and `original/libexif/exif-tag.c` until there is an already-committed replacement for the symbol map and tag-table source of truth.
      - If phase 3 narrows the reason `test-original.sh` stages `original/`, update its usage or help text to describe the remaining dependency accurately rather than deleting the staging step prematurely.
      - Preserve existing CVE protections such as Olympus zero-denominator formatting and Canon size limits while moving the implementation into Rust.
      - Add focused regressions for any newly ported MakerNote path that previously relied only on the helper C code.
      
      Success Criteria
      - `bash tests/run-original-test-suite.sh`
      - `bash tests/run-test-mnote-matrix.sh`
      - `cargo test --release --test cve_regressions`
      - `! rg -n '\.\./original/libexif/(apple|canon|fuji|olympus|pentax)/[^"]+\.c' build.rs`
      - `! rg -n 'cargo:rerun-if-changed=\.\./original/libexif/(apple|canon|fuji|olympus|pentax)/' build.rs`
      - `rg -n 'cshim/exif-log-shim\.c' build.rs`
      - `test "$(rg -c '\.file\(' build.rs)" = "1"`
      - `! rg -n '\.files\(' build.rs`
      - `! rg -n 'compile_mnote_helper|build_mnote_helpers|emit_mnote_rerun_hints|safe_helper_exif_mnote_data_|safe_helper_mnote_' build.rs src/mnote`
      
      Git Commit Requirement
      - Commit the phase work to git before yielding. Do not yield with uncommitted changes.
  - id: check_03_mnote_tester
    type: check
    bounce_target: impl_03_makernote_rust_completion
    prompt: |-
      Reviewer role: Tester for `MakerNote Rust Completion` in check phase `check_03_mnote_tester`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_03_mnote_tester`. Do not modify files.
      
      Specific Things to Verify
      - verify Apple, Canon, Fuji, Olympus, and Pentax MakerNote behavior after helper removal, keep copied upstream MakerNote-sensitive regressions passing, and confirm that `safe/build.rs` no longer compiles or tracks vendored MakerNote helper C sources from `original/libexif/{apple,canon,fuji,olympus,pentax}` or any MakerNote C.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      bash tests/run-original-test-suite.sh
      bash tests/run-test-mnote-matrix.sh
      cargo test --release --test cve_regressions
      ! rg -n '\.\./original/libexif/(apple|canon|fuji|olympus|pentax)/[^"]+\.c' build.rs
      ! rg -n 'cargo:rerun-if-changed=\.\./original/libexif/(apple|canon|fuji|olympus|pentax)/' build.rs
      rg -n 'cshim/exif-log-shim\.c' build.rs
      test "$(rg -c '\.file\(' build.rs)" = "1"
      ! rg -n '\.files\(' build.rs
      ! rg -n 'compile_mnote_helper|build_mnote_helpers|emit_mnote_rerun_hints|safe_helper_exif_mnote_data_|safe_helper_mnote_' build.rs src/mnote
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_03_mnote_senior
    type: check
    bounce_target: impl_03_makernote_rust_completion
    prompt: |-
      Reviewer role: Senior reviewer for `MakerNote Rust Completion` in check phase `check_03_mnote_senior`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_03_mnote_senior`. Do not modify files.
      
      Specific Things to Verify
      - review the MakerNote porting diff, confirm exported MakerNote symbols and outputs stay compatible, keep copied upstream MakerNote-sensitive checks passing, and confirm that the only remaining compiled C source in `build.rs` is the log shim rather than MakerNote helper code.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      bash tests/run-original-test-suite.sh
      bash tests/run-test-mnote-matrix.sh
      ! rg -n '\.\./original/libexif/(apple|canon|fuji|olympus|pentax)/[^"]+\.c' build.rs
      ! rg -n 'cargo:rerun-if-changed=\.\./original/libexif/(apple|canon|fuji|olympus|pentax)/' build.rs
      rg -n 'cshim/exif-log-shim\.c' build.rs
      test "$(rg -c '\.file\(' build.rs)" = "1"
      ! rg -n '\.files\(' build.rs
      ! rg -n 'compile_mnote_helper|build_mnote_helpers|emit_mnote_rerun_hints|safe_helper_exif_mnote_data_|safe_helper_mnote_' build.rs src/mnote
      git show --stat --format=fuller HEAD
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_04_source_link_compat
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_04_source_link_compat` (Source and Link Compatibility Against Existing Original Clients).
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Preexisting Inputs
      - `safe/tests/run-package-build.sh`
      - `safe/tests/run-c-compile-smoke.sh`
      - `safe/tests/run-original-object-link-compat.sh`
      - `safe/tests/link-compat/object-manifest.txt`
      - `safe/tests/link-compat/run-manifest.txt`
      - `original/test/*.o` when present
      - `original/contrib/examples/*.o` when present
      - `original/test/nls/print-localedir.o` when present
      - `original/test/Makefile.am`
      - `original/contrib/examples/Makefile.am`
      - `safe/contrib/examples/*.c`
      - `safe/tests/smoke/public-api-smoke.c`
      
      New Outputs
      - Phase-local package root `safe/.artifacts/impl_04_source_link_compat/` with fresh package metadata and validated overlay contents
      - Updated link-compat manifests or harness rules if new coverage is required
      - Updated library fixes exposed by relink or runtime mismatches
      - Updated compile-smoke source list if a missed public surface needs explicit coverage
      
      File Changes
      - `safe/tests/run-c-compile-smoke.sh`
      - `safe/tests/run-original-object-link-compat.sh`
      - `safe/tests/link-compat/{object-manifest.txt,run-manifest.txt}`
      - `safe/tests/smoke/public-api-smoke.c`
      - `safe/contrib/examples/*.c`
      - `safe/src/**` only as required by compatibility failures
      
      Implementation Details
      - Consume or update the listed artifacts in place. Do not refetch, recollect, rediscover, or regenerate them from scratch.
      - Consume the existing object manifests. Do not weaken this phase into source-only recompilation.
      - If original object or wrapper artifacts are missing, regenerate them only via the existing `original/` build targets that the current harness already uses.
      - Reuse the fixed phase-local package root for every package-backed command in this phase. Do not create a new temp package build for each wrapper script.
      - Run `PACKAGE_BUILD_ROOT=... bash tests/run-package-build.sh` once, then rerun the compile and relink wrappers under `LIBEXIF_REQUIRE_REUSE=1`. Both wrappers must fail with the exact substring `reuse-required package root is missing or stale` when pointed at a deliberately missing root under `LIBEXIF_REQUIRE_REUSE=1`. Do not hide a rebuild behind the nested wrapper call.
      - Preserve the manifest-level comparison policy:
        - `exact_streams` unless a plan-documented reason requires pointer normalization
        - `normalize_hex_pointers_in_streams` only for known pointer-printing cases such as `test-fuzzer`
      - Preserve byte-for-byte output-file comparison for `test-extract`, `thumbnail`, `write-exif`, and any new file-producing entry added later.
      - If a relink failure reveals an ABI-visible behavior discrepancy, add the smallest possible new manifest entry or local regression in addition to the library fix.
      
      Success Criteria
      - `PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_04_source_link_compat bash tests/run-package-build.sh`
      - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_04_source_link_compat bash tests/run-c-compile-smoke.sh`
      - `/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_04_source_link_compat/compile-smoke/static/public-api-smoke-static` must exist, execute successfully, and have no `NEEDED` entry for `libexif.so.12` in `readelf -d`
      - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_04_source_link_compat bash tests/run-original-object-link-compat.sh`
      - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_04_source_link_compat.missing bash tests/run-c-compile-smoke.sh` must fail with `reuse-required package root is missing or stale`
      - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_04_source_link_compat.missing bash tests/run-original-object-link-compat.sh` must fail with `reuse-required package root is missing or stale`
      
      Git Commit Requirement
      - Commit the phase work to git before yielding. Do not yield with uncommitted changes.
  - id: check_04_link_tester
    type: check
    bounce_target: impl_04_source_link_compat
    prompt: |-
      Reviewer role: Tester for `Source and Link Compatibility Against Existing Original Clients` in check phase `check_04_link_tester`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_04_link_tester`. Do not modify files.
      
      Specific Things to Verify
      - verify that source-compiled clients and already-built original object files still compile, relink, and behave identically against `libexif-safe` while reusing one phase-local package root, and that the relink wrapper honors reuse-only mode.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_04_source_link_compat
      missing_root_log=$(mktemp)
      missing_link_log=$(mktemp)
      static_smoke="$PACKAGE_BUILD_ROOT/compile-smoke/static/public-api-smoke-static"
      missing_root="${PACKAGE_BUILD_ROOT}.missing"
      trap 'rm -f "$missing_root_log" "$missing_link_log"' EXIT
      PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-c-compile-smoke.sh
      test -x "$static_smoke"
      "$static_smoke"
      ! readelf -d "$static_smoke" | grep -F 'libexif.so.12'
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-original-object-link-compat.sh
      rm -rf "$missing_root"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_root" bash tests/run-c-compile-smoke.sh >"$missing_root_log" 2>&1; then
        cat "$missing_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_root_log"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_root" bash tests/run-original-object-link-compat.sh >"$missing_link_log" 2>&1; then
        cat "$missing_link_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_link_log"
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_04_link_senior
    type: check
    bounce_target: impl_04_source_link_compat
    prompt: |-
      Reviewer role: Senior reviewer for `Source and Link Compatibility Against Existing Original Clients` in check phase `check_04_link_senior`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_04_link_senior`. Do not modify files.
      
      Specific Things to Verify
      - review manifest changes, confirm comparison modes were not weakened, and confirm the link-compat harness reuses the phase-local package root.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_04_source_link_compat
      static_smoke="$PACKAGE_BUILD_ROOT/compile-smoke/static/public-api-smoke-static"
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-c-compile-smoke.sh
      test -x "$static_smoke"
      ! readelf -d "$static_smoke" | grep -F 'libexif.so.12'
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-original-object-link-compat.sh
      git show --stat --format=fuller HEAD
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_05_downstream_compile
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_05_downstream_compile` (Downstream Compile Compatibility Matrix).
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Preexisting Inputs
      - `dependents.json`
      - `test-original.sh`
      - `safe/tests/run-package-build.sh`
      
      New Outputs
      - Phase-local package root `safe/.artifacts/impl_05_downstream_compile/` with fresh package metadata and validated overlay contents
      - Phase-local Docker image `libexif-original-test:impl_05_downstream_compile` with recorded image-input freshness metadata and an inspectable `io.safelibs.libexif.image-inputs-sha256` label
      - Updated `test-original.sh` with explicit `compile`, `runtime`, and `all` modes, full-matrix downstream summary writers, and a non-mutating `--print-image-inputs-sha256` helper for verifiers
      - A concrete compile-mode assertion for every current downstream name in `dependents.json`
      - A checker-visible downstream compile summary at `safe/.artifacts/<implement-phase-id>/downstream/compile-matrix.tsv` with one successful row per dependent name in `dependents.json`
      - Checked-in helper manifests or probe sources under `safe/tests/downstream/**` only if the per-dependent compile assertions become too large to maintain inline in `test-original.sh`
      - New minimal local regressions under `safe/tests/` for any library bug discovered through the downstream compile matrix
      - Library fixes required by compile-time failures
      
      File Changes
      - `test-original.sh`
      - `dependents.json` only if a factual inventory error must be corrected in place
      - `safe/tests/downstream/**` only if helper manifests or probe sources are truly needed
      - `safe/tests/*.rs`, `safe/tests/*.sh`, or `safe/tests/original-c/**` for distilled regressions
      - `safe/tests/testdata/*` only if a new fixture is required to reproduce a real downstream bug
      - `safe/src/**` as required by failures
      
      Implementation Details
      - Consume or update the listed artifacts in place. Do not refetch, recollect, rediscover, or regenerate them from scratch.
      - Extend `test-original.sh` with `--mode compile`, `--mode runtime`, and `--mode all`; keep `all` as the default broadest end-to-end entry point.
      - `--mode all` must run the complete compile matrix first and then the runtime matrix, reusing the same validated `LIBEXIF_DOWNSTREAM_PACKAGE_ROOT` and the same validated `LIBEXIF_ORIGINAL_TEST_IMAGE` within that invocation, and it must refresh both downstream summary files from that single run.
      - Preserve the exact downstream name contract in `test-original.sh`: `REQUIRED_DEPENDENTS` must stay synchronized with `dependents.json`, and `--only` must continue using those exact case-sensitive names.
      - `--mode compile` must cover all 15 downstream names in `dependents.json`. The inventory already records compile evidence via `dependency_paths`, but compile-mode membership is still the full fixed dependent-name list. It must not silently skip a dependent just because the existing script only had a runtime probe for it before, and it must not collapse shared source-package builds into fewer named results.
      - For each unique source package from the existing inventory, build the actual Ubuntu source package inside the Docker container using the already selected source-package identity from `dependents.json`: `apt-get build-dep -y --no-install-recommends <source_package>`, `apt-get source <source_package>`, and `DEB_BUILD_OPTIONS='nocheck noautodbgsym nostrip' dpkg-buildpackage -us -uc -b`.
      - The required source-package build coverage is: `exif`, `fbi`, `eog-plugins`, `tracker-miners`, `shotwell`, `foxtrotgps`, `gphoto2`, `gtkam`, `minidlna`, `gerbera`, `ruby-exif`, `libexif-gtk`, `camlimages`, and `imagemagick`.
      - For duplicate downstream names that share a source package, one source build is acceptable inside a single `--mode compile` invocation only if each downstream name still has an explicit post-build assertion. `eog-plugin-exif-display` and `eog-plugin-map` are the main required shared-build case.
      - Each compile assertion must check for the relevant build product by dependent name, such as the produced `.deb`, shared object, or built executable. When the built product is an ELF consumer of `libexif.so.12`, also assert that `ldd` resolves it to the active safe library installed from `LIBEXIF_DOWNSTREAM_PACKAGE_ROOT`.
      - `test-original.sh --mode compile` must refresh `LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/compile-matrix.tsv` with the exact header `name<TAB>source_package<TAB>assertion<TAB>artifact<TAB>status`, one row per dependent name in the exact `dependents.json` order, and `status=ok` for every successful row. The `assertion` field must describe the dependent-specific post-build check, and the `artifact` field must identify the built file or package that satisfied that check.
      - `test-original.sh --mode compile --only <name>` must not overwrite or truncate an existing 15-row `downstream/compile-matrix.tsv`. If per-dependent logs or artifacts are useful for focused reruns, place them under a separate non-summary path such as `downstream/only/compile/<name>/`.
      - A full-matrix invocation of `test-original.sh --mode runtime` without `--only`, and the runtime half of `test-original.sh --mode all`, must refresh `LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/runtime-matrix.tsv` with the exact header `name<TAB>source_package<TAB>assertion<TAB>artifact<TAB>status`, one row per runtime-capable dependent in the exact `jq -r '.dependents[] | select(.runtime_functionality != null) | .name' dependents.json` order, and `status=ok` for every successful row. The `assertion` field must describe the dependent-specific runtime probe, and the `artifact` field must identify the runtime log, inspected output, or generated file that satisfied that probe. Compile-only dependents must never appear in this file.
      - Treat `ImageMagick` as compile-only for mode membership because `dependents.json` records `"runtime_functionality": null`. Preserve the existing `identify -verbose` execution as `ImageMagick`'s compile-mode post-build assertion after the source package finishes building, but do not schedule `ImageMagick` inside runtime mode.
      - The existing smaller compile probes for `libexif-gtk3`, `CamlImages`, and `ImageMagick` may remain as debugging loops, but they do not replace the full source-package build contract.
      - `test-original.sh` must install prebuilt safe packages from `LIBEXIF_DOWNSTREAM_PACKAGE_ROOT`, must validate that root's `metadata/source-commit.txt`, `metadata/package-inputs.sha256`, and `metadata/validated.ok` against the current workspace before launching the container, must expose `--print-image-inputs-sha256`, and may reuse an existing `LIBEXIF_ORIGINAL_TEST_IMAGE` only when `docker image inspect` shows a recorded digest that still matches the current harness inputs. That image-freshness key must cover `test-original.sh`, `dependents.json`, and any checked-in `safe/tests/downstream/**` helper files used by the phase. It must not rebuild `safe/` inside the container once the phase-local package root exists.
      - `LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/` is reserved for host-visible downstream summaries and per-run logs. It is a downstream-harness output area, not part of `metadata/package-inputs.sha256` or the `validated.ok` package-content checks.
      - After `test-original.sh` validates `LIBEXIF_DOWNSTREAM_PACKAGE_ROOT`, it must treat every path under that root except `downstream/` as read-only. If containerized probes need writable host-visible output, route that output through `downstream/` or another separate writable mount rather than making the validated package tree writable.
      - The downstream harness must keep `LIBEXIF_DOWNSTREAM_PACKAGE_ROOT` reusable after each compile-mode run. Summary or per-dependent log writes may happen only under `downstream/`; the validated package contents and metadata must remain untouched so `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT" bash safe/tests/run-package-build.sh` still succeeds after `./test-original.sh --mode compile`.
      - With `LIBEXIF_REQUIRE_REUSE=1`, `test-original.sh` must emit `reuse-required docker image is missing or stale` for a deliberately missing or stale `LIBEXIF_ORIGINAL_TEST_IMAGE`, and `reuse-required package root is missing or stale` for a deliberately missing or stale `LIBEXIF_DOWNSTREAM_PACKAGE_ROOT`.
      - Before any phase-5 checker invocation that uses `downstream/compile-matrix.tsv` as proof, delete that file from the phase-local root so the exact `./test-original.sh --mode compile` run must recreate it. A leftover 15-row summary from an earlier bounce is not acceptable evidence.
      - First delete any preexisting `downstream/compile-matrix.tsv`. Then `./test-original.sh --mode compile` may build the phase-local image if needed and must recreate the 15-row compile summary. Then `LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode compile --only exif` must succeed without rebuilding the image or repackaging `libexif-safe`, must leave the 15-row compile summary intact, and must leave the phase-local package root valid for `LIBEXIF_REQUIRE_REUSE=1 bash safe/tests/run-package-build.sh`. Deliberate missing-image, stale-image, missing-package-root, and stale-package-root reruns must fail with the exact reuse-only substrings above.
      - Use `./test-original.sh --mode compile --only <name>` as the tight failure reproduction loop for a single dependent.
      - For each consumer-visible failure, first reproduce it through the Docker harness, then create the smallest possible non-Docker regression under `safe/tests/` whenever feasible.
      
      Success Criteria
      - `PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile bash safe/tests/run-package-build.sh`
      - `cd /home/yans/code/safelibs/ported/libexif && ./test-original.sh --print-image-inputs-sha256` must match `docker image inspect libexif-original-test:impl_05_downstream_compile --format '{% raw %}{{ index .Config.Labels "io.safelibs.libexif.image-inputs-sha256" }}{% endraw %}'`
      - `rm -f /home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile/downstream/compile-matrix.tsv && LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_05_downstream_compile LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile ./test-original.sh --mode compile`
      - `/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile/downstream/compile-matrix.tsv` must be recreated by that invocation with header `name<TAB>source_package<TAB>assertion<TAB>artifact<TAB>status`, its name column must diff cleanly against `jq -r '.dependents[].name' /home/yans/code/safelibs/ported/libexif/dependents.json`, and it must contain exactly 15 rows with `status=ok`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_05_downstream_compile LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile ./test-original.sh --mode compile --only exif`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_05_downstream_compile-missing LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile ./test-original.sh --mode compile --only exif` must fail with `reuse-required docker image is missing or stale`
      - An intentionally stale Docker tag such as `docker tag ubuntu:24.04 libexif-original-test:impl_05_downstream_compile-stale` must cause `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_05_downstream_compile-stale LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile ./test-original.sh --mode compile --only exif` to fail with `reuse-required docker image is missing or stale`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_05_downstream_compile LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile.missing ./test-original.sh --mode compile --only exif` must fail with `reuse-required package root is missing or stale`
      - A copied stale root made from `/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile` with mismatched `metadata/source-commit.txt` must cause `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_05_downstream_compile LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=<stale-copy> ./test-original.sh --mode compile --only exif` to fail with `reuse-required package root is missing or stale`
      
      Git Commit Requirement
      - Commit the phase work to git before yielding. Do not yield with uncommitted changes.
  - id: check_05_downstream_compile_tester
    type: check
    bounce_target: impl_05_downstream_compile
    prompt: |-
      Reviewer role: Tester for `Downstream Compile Compatibility Matrix` in check phase `check_05_downstream_compile_tester`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_05_downstream_compile_tester`. Do not modify files.
      
      Specific Things to Verify
      - verify that every dependent in `dependents.json` still builds against the packaged safe headers or libraries inside the Ubuntu 24.04 Docker harness, reusing one phase-local package root and one phase-local image tag, and that the exact `--mode compile` run recreates a 15-row checker-visible success summary instead of reusing a stale partial subset.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile
      PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh
      cd /home/yans/code/safelibs/ported/libexif
      export LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_05_downstream_compile
      export LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile
      expected_image_digest="$(./test-original.sh --print-image-inputs-sha256)"
      compile_summary="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/compile-matrix.tsv"
      expected_compile_names=$(mktemp)
      actual_compile_names=$(mktemp)
      missing_image_log=$(mktemp)
      missing_root_log=$(mktemp)
      stale_image_log=$(mktemp)
      stale_root_log=$(mktemp)
      missing_image_tag="${LIBEXIF_ORIGINAL_TEST_IMAGE}-missing"
      stale_image_tag="${LIBEXIF_ORIGINAL_TEST_IMAGE}-stale"
      missing_package_root="${LIBEXIF_DOWNSTREAM_PACKAGE_ROOT}.missing"
      stale_package_root="${LIBEXIF_DOWNSTREAM_PACKAGE_ROOT}.stale"
      trap 'rm -f "$expected_compile_names" "$actual_compile_names" "$missing_image_log" "$missing_root_log" "$stale_image_log" "$stale_root_log"; rm -rf "$stale_package_root"; docker image rm -f "$stale_image_tag" >/dev/null 2>&1 || true' EXIT
      rm -f "$compile_summary"
      ./test-original.sh --mode compile
      test -f "$compile_summary"
      test "$(head -n1 "$compile_summary")" = $'name\tsource_package\tassertion\tartifact\tstatus'
      jq -r '.dependents[].name' dependents.json >"$expected_compile_names"
      awk -F'\t' 'NR > 1 { print $1 }' "$compile_summary" >"$actual_compile_names"
      diff -u "$expected_compile_names" "$actual_compile_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$compile_summary")" = "15"
      test "$(docker image inspect "$LIBEXIF_ORIGINAL_TEST_IMAGE" --format '{% raw %}{{ index .Config.Labels "io.safelibs.libexif.image-inputs-sha256" }}{% endraw %}')" = "$expected_image_digest"
      LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode compile --only exif
      awk -F'\t' 'NR > 1 { print $1 }' "$compile_summary" >"$actual_compile_names"
      diff -u "$expected_compile_names" "$actual_compile_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$compile_summary")" = "15"
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT" bash safe/tests/run-package-build.sh >/dev/null
      docker image rm -f "$missing_image_tag" >/dev/null 2>&1 || true
      if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE="$missing_image_tag" ./test-original.sh --mode compile --only exif >"$missing_image_log" 2>&1; then
        cat "$missing_image_log" >&2
        exit 1
      fi
      grep -F "reuse-required docker image is missing or stale" "$missing_image_log"
      docker tag ubuntu:24.04 "$stale_image_tag"
      if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE="$stale_image_tag" ./test-original.sh --mode compile --only exif >"$stale_image_log" 2>&1; then
        cat "$stale_image_log" >&2
        exit 1
      fi
      grep -F "reuse-required docker image is missing or stale" "$stale_image_log"
      rm -rf "$missing_package_root"
      if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_DOWNSTREAM_PACKAGE_ROOT="$missing_package_root" ./test-original.sh --mode compile --only exif >"$missing_root_log" 2>&1; then
        cat "$missing_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_root_log"
      rm -rf "$stale_package_root"
      cp -a "$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT" "$stale_package_root"
      printf 'stale\n' >"$stale_package_root/metadata/source-commit.txt"
      if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_DOWNSTREAM_PACKAGE_ROOT="$stale_package_root" ./test-original.sh --mode compile --only exif >"$stale_root_log" 2>&1; then
        cat "$stale_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$stale_root_log"
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_05_downstream_compile_senior
    type: check
    bounce_target: impl_05_downstream_compile
    prompt: |-
      Reviewer role: Senior reviewer for `Downstream Compile Compatibility Matrix` in check phase `check_05_downstream_compile_senior`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_05_downstream_compile_senior`. Do not modify files.
      
      Specific Things to Verify
      - review the downstream compile additions, confirm the harness uses the existing inventory rather than rediscovering apps, confirm the container consumes prebuilt safe packages instead of rebuilding them, and confirm that the exact verifier run recreates a per-dependent compile summary naming all 15 dependents.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile
      PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh
      cd /home/yans/code/safelibs/ported/libexif
      export LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_05_downstream_compile
      export LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_05_downstream_compile
      expected_image_digest="$(./test-original.sh --print-image-inputs-sha256)"
      compile_summary="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/compile-matrix.tsv"
      expected_compile_names=$(mktemp)
      actual_compile_names=$(mktemp)
      missing_image_log=$(mktemp)
      stale_image_log=$(mktemp)
      stale_image_tag="${LIBEXIF_ORIGINAL_TEST_IMAGE}-stale"
      trap 'rm -f "$expected_compile_names" "$actual_compile_names" "$missing_image_log" "$stale_image_log"; docker image rm -f "$stale_image_tag" >/dev/null 2>&1 || true' EXIT
      rm -f "$compile_summary"
      ./test-original.sh --mode compile
      test -f "$compile_summary"
      test "$(head -n1 "$compile_summary")" = $'name\tsource_package\tassertion\tartifact\tstatus'
      jq -r '.dependents[].name' dependents.json >"$expected_compile_names"
      awk -F'\t' 'NR > 1 { print $1 }' "$compile_summary" >"$actual_compile_names"
      diff -u "$expected_compile_names" "$actual_compile_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$compile_summary")" = "15"
      test "$(docker image inspect "$LIBEXIF_ORIGINAL_TEST_IMAGE" --format '{% raw %}{{ index .Config.Labels "io.safelibs.libexif.image-inputs-sha256" }}{% endraw %}')" = "$expected_image_digest"
      LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode compile --only exif
      awk -F'\t' 'NR > 1 { print $1 }' "$compile_summary" >"$actual_compile_names"
      diff -u "$expected_compile_names" "$actual_compile_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$compile_summary")" = "15"
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT" bash safe/tests/run-package-build.sh >/dev/null
      docker image rm -f "${LIBEXIF_ORIGINAL_TEST_IMAGE}-missing" >/dev/null 2>&1 || true
      if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE="${LIBEXIF_ORIGINAL_TEST_IMAGE}-missing" ./test-original.sh --mode compile --only exif >"$missing_image_log" 2>&1; then
        cat "$missing_image_log" >&2
        exit 1
      fi
      grep -F "reuse-required docker image is missing or stale" "$missing_image_log"
      docker tag ubuntu:24.04 "$stale_image_tag"
      if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE="$stale_image_tag" ./test-original.sh --mode compile --only exif >"$stale_image_log" 2>&1; then
        cat "$stale_image_log" >&2
        exit 1
      fi
      grep -F "reuse-required docker image is missing or stale" "$stale_image_log"
      git show --stat --format=fuller HEAD
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_06_downstream_cli_library
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_06_downstream_cli_library` (Downstream CLI and Library Runtime Matrix).
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Preexisting Inputs
      - `dependents.json`
      - `test-original.sh`
      - `safe/tests/`
      - `safe/tests/testdata/`
      - `safe/tests/run-package-build.sh`
      
      New Outputs
      - Phase-local package root `safe/.artifacts/impl_06_downstream_cli_library/` with fresh package metadata and validated overlay contents
      - Phase-local Docker image `libexif-original-test:impl_06_downstream_cli_library` with recorded image-input freshness metadata
      - Updated downstream Docker assertions for these consumers
      - New minimal local regressions under `safe/tests/` for issues found through Docker
      - Library fixes required by consumer-visible runtime failures
      
      File Changes
      - `test-original.sh`
      - `dependents.json` only if a factual inventory error must be corrected in place
      - `safe/tests/*.rs`, `safe/tests/*.sh`, or `safe/tests/original-c/**` for distilled regressions
      - `safe/tests/testdata/*` only if a new fixture is required to reproduce a real downstream bug
      - `safe/src/**` as required by failures
      
      Implementation Details
      - Consume or update the listed artifacts in place. Do not refetch, recollect, rediscover, or regenerate them from scratch.
      - Use the existing 15-entry `dependents.json` inventory. Do not rediscover a new app list.
      - Use `./test-original.sh --mode runtime --only <name>` for tight failure reproduction loops.
      - Runtime mode in this phase must cover only dependents whose `runtime_functionality` is non-null. `ImageMagick` is therefore intentionally excluded here and remains compile-mode-only; `./test-original.sh --mode runtime --only ImageMagick` must fail fast with the exact substring `compile-only dependent: ImageMagick only supports --mode compile` instead of being treated as a runtime probe.
      - Reuse the phase-local package root and phase-local Docker image tag for every `--only` invocation in this phase. Run one initial runtime invocation without `LIBEXIF_REQUIRE_REUSE`, then reuse-only reruns for the remaining apps and the compile-only negative case. Do not rebuild safe packages or the image once the phase-local artifacts exist.
      - Keep fixture generation inside `test-original.sh` as the source of truth for synthetic images; extend it in place only when a real app scenario needs a new EXIF shape.
      - Preserve the contract that the container installs locally built `safe/` Debian packages and that downstream binaries must resolve `libexif.so.12` to the safe package.
      
      Success Criteria
      - `PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_06_downstream_cli_library bash safe/tests/run-package-build.sh`
      - `LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_06_downstream_cli_library LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_06_downstream_cli_library ./test-original.sh --mode runtime --only exif`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_06_downstream_cli_library LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_06_downstream_cli_library ./test-original.sh --mode runtime --only exiftran`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_06_downstream_cli_library LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_06_downstream_cli_library ./test-original.sh --mode runtime --only gphoto2`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_06_downstream_cli_library LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_06_downstream_cli_library ./test-original.sh --mode runtime --only ruby-exif`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_06_downstream_cli_library LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_06_downstream_cli_library ./test-original.sh --mode runtime --only CamlImages`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_06_downstream_cli_library LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_06_downstream_cli_library ./test-original.sh --mode runtime --only ImageMagick` must fail with `compile-only dependent: ImageMagick only supports --mode compile`
      
      Git Commit Requirement
      - Commit the phase work to git before yielding. Do not yield with uncommitted changes.
  - id: check_06_downstream_tester
    type: check
    bounce_target: impl_06_downstream_cli_library
    prompt: |-
      Reviewer role: Tester for `Downstream CLI and Library Runtime Matrix` in check phase `check_06_downstream_tester`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_06_downstream_tester`. Do not modify files.
      
      Specific Things to Verify
      - verify the lower-friction downstream consumers in Docker, ensure every discovered runtime issue gets a local regression or tighter harness assertion, and reuse one phase-local package root and one phase-local image tag.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_06_downstream_cli_library
      PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh
      cd /home/yans/code/safelibs/ported/libexif
      export LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_06_downstream_cli_library
      export LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_06_downstream_cli_library
      expected_image_digest="$(./test-original.sh --print-image-inputs-sha256)"
      imagemagick_runtime_log=$(mktemp)
      trap 'rm -f "$imagemagick_runtime_log"' EXIT
      ./test-original.sh --mode runtime --only exif
      test "$(docker image inspect "$LIBEXIF_ORIGINAL_TEST_IMAGE" --format '{% raw %}{{ index .Config.Labels "io.safelibs.libexif.image-inputs-sha256" }}{% endraw %}')" = "$expected_image_digest"
      for app in exiftran gphoto2 ruby-exif CamlImages; do
        LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode runtime --only "$app"
      done
      if LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode runtime --only ImageMagick >"$imagemagick_runtime_log" 2>&1; then
        cat "$imagemagick_runtime_log" >&2
        exit 1
      fi
      grep -F "compile-only dependent: ImageMagick only supports --mode compile" "$imagemagick_runtime_log"
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_06_downstream_senior
    type: check
    bounce_target: impl_06_downstream_cli_library
    prompt: |-
      Reviewer role: Senior reviewer for `Downstream CLI and Library Runtime Matrix` in check phase `check_06_downstream_senior`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_06_downstream_senior`. Do not modify files.
      
      Specific Things to Verify
      - review each downstream runtime fix, confirm that Docker-only failures were converted into the smallest possible repo-local regression when feasible, and confirm that the harness still consumes `dependents.json` rather than rediscovering apps.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_06_downstream_cli_library
      PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh
      cd /home/yans/code/safelibs/ported/libexif
      export LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_06_downstream_cli_library
      export LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_06_downstream_cli_library
      imagemagick_runtime_log=$(mktemp)
      trap 'rm -f "$imagemagick_runtime_log"' EXIT
      ./test-original.sh --mode runtime --only exif
      for app in exiftran gphoto2 ruby-exif CamlImages; do
        LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode runtime --only "$app"
      done
      if LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode runtime --only ImageMagick >"$imagemagick_runtime_log" 2>&1; then
        cat "$imagemagick_runtime_log" >&2
        exit 1
      fi
      grep -F "compile-only dependent: ImageMagick only supports --mode compile" "$imagemagick_runtime_log"
      git show --stat --format=fuller HEAD
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_07_downstream_gui_services
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_07_downstream_gui_services` (Downstream GUI and Service Runtime Matrix).
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Preexisting Inputs
      - `dependents.json`
      - `test-original.sh`
      - `safe/tests/testdata/`
      - `safe/tests/run-package-build.sh`
      
      New Outputs
      - Phase-local package root `safe/.artifacts/impl_07_downstream_gui_services/` with fresh package metadata and validated overlay contents
      - Phase-local Docker image `libexif-original-test:impl_07_downstream_gui_services` with recorded image-input freshness metadata
      - A checker-visible downstream runtime summary at `safe/.artifacts/<implement-phase-id>/downstream/runtime-matrix.tsv` with one successful row per runtime-capable dependent in `dependents.json`
      - Updated or extended GUI or service probes in `test-original.sh`
      - Additional local regressions in `safe/tests/` where GUI or service failures can be reduced to a smaller reproducer
      - Library fixes required by GUI or service-visible runtime failures
      
      File Changes
      - `test-original.sh`
      - `safe/tests/*.rs`, `safe/tests/*.sh`, `safe/tests/original-c/**`, or `safe/tests/original-sh/**` for distilled regressions
      - `safe/tests/testdata/*` only if needed for a real service or plugin issue
      - `safe/src/**` as required by failures
      
      Implementation Details
      - Consume or update the listed artifacts in place. Do not refetch, recollect, rediscover, or regenerate them from scratch.
      - Preserve and extend the existing inline probes in `test-original.sh` rather than replacing them with a new harness. The script already knows how to build fixtures, install packages in the container, and drive GUI or service apps under Xvfb or service-specific configs.
      - Convert any discovered issue into a smaller repo-local regression whenever possible; if only the full app integration can reproduce it, strengthen the existing Docker assertion instead of creating a redundant second harness.
      - Keep the Docker image definition inline in `test-original.sh`. Workflow YAML must remain inline-only and consume the existing script.
      - This phase provides the first checker-visible full runtime-matrix proof. Before any checker uses `downstream/runtime-matrix.tsv` as evidence, delete that file from the phase-local root so the exact `./test-original.sh --mode runtime` invocation recreates the 14-row summary in `dependents.json` runtime order.
      - `test-original.sh --mode runtime --only <name>` must not overwrite or truncate an existing 14-row `downstream/runtime-matrix.tsv`. If per-dependent logs or artifacts are useful for focused reruns, place them under a separate non-summary path such as `downstream/only/runtime/<name>/`.
      - Reuse the phase-local package root and phase-local Docker image tag for the full `--mode runtime` invocation and every `--only` rerun in this phase. Run one initial full runtime-matrix invocation without `LIBEXIF_REQUIRE_REUSE`, then reuse-only reruns for the GUI and service entries most likely to regress under Xvfb or service orchestration. Do not rebuild safe packages or the image between entries once the phase-local artifacts exist, and leave the phase-local package root valid for `LIBEXIF_REQUIRE_REUSE=1 bash safe/tests/run-package-build.sh` after the runtime reruns complete.
      - Ensure the GUI or service probes stay deterministic, avoid hidden user-profile state, and keep their assertions tied to observable EXIF behavior rather than brittle UI timing.
      
      Success Criteria
      - `PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services bash safe/tests/run-package-build.sh`
      - `rm -f /home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services/downstream/runtime-matrix.tsv && LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_07_downstream_gui_services LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services ./test-original.sh --mode runtime`
      - `/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services/downstream/runtime-matrix.tsv` must be recreated by that invocation with header `name<TAB>source_package<TAB>assertion<TAB>artifact<TAB>status`, its name column must diff cleanly against `jq -r '.dependents[] | select(.runtime_functionality != null) | .name' /home/yans/code/safelibs/ported/libexif/dependents.json`, and it must contain exactly 14 rows with `status=ok`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_07_downstream_gui_services LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services ./test-original.sh --mode runtime --only eog-plugin-exif-display`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_07_downstream_gui_services LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services ./test-original.sh --mode runtime --only eog-plugin-map`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_07_downstream_gui_services LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services ./test-original.sh --mode runtime --only tracker-extract`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_07_downstream_gui_services LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services ./test-original.sh --mode runtime --only Shotwell`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_07_downstream_gui_services LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services ./test-original.sh --mode runtime --only FoxtrotGPS`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_07_downstream_gui_services LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services ./test-original.sh --mode runtime --only GTKam`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_07_downstream_gui_services LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services ./test-original.sh --mode runtime --only MiniDLNA`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_07_downstream_gui_services LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services ./test-original.sh --mode runtime --only Gerbera`
      - `LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_07_downstream_gui_services LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services ./test-original.sh --mode runtime --only libexif-gtk3`
      
      Git Commit Requirement
      - Commit the phase work to git before yielding. Do not yield with uncommitted changes.
  - id: check_07_downstream_tester
    type: check
    bounce_target: impl_07_downstream_gui_services
    prompt: |-
      Reviewer role: Tester for `Downstream GUI and Service Runtime Matrix` in check phase `check_07_downstream_tester`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_07_downstream_tester`. Do not modify files.
      
      Specific Things to Verify
      - verify the GUI, plugin, indexing, and media-service consumers in Docker, including the existing Xvfb- or dbus-driven probes, while reusing one phase-local package root and one phase-local image tag, and prove that the exact `--mode runtime` run recreates the full 14-row runtime summary.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services
      PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh
      cd /home/yans/code/safelibs/ported/libexif
      export LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_07_downstream_gui_services
      export LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services
      expected_image_digest="$(./test-original.sh --print-image-inputs-sha256)"
      runtime_summary="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/runtime-matrix.tsv"
      expected_runtime_names=$(mktemp)
      actual_runtime_names=$(mktemp)
      trap 'rm -f "$expected_runtime_names" "$actual_runtime_names"' EXIT
      rm -f "$runtime_summary"
      ./test-original.sh --mode runtime
      test -f "$runtime_summary"
      test "$(head -n1 "$runtime_summary")" = $'name\tsource_package\tassertion\tartifact\tstatus'
      jq -r '.dependents[] | select(.runtime_functionality != null) | .name' dependents.json >"$expected_runtime_names"
      awk -F'\t' 'NR > 1 { print $1 }' "$runtime_summary" >"$actual_runtime_names"
      diff -u "$expected_runtime_names" "$actual_runtime_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$runtime_summary")" = "14"
      test "$(docker image inspect "$LIBEXIF_ORIGINAL_TEST_IMAGE" --format '{% raw %}{{ index .Config.Labels "io.safelibs.libexif.image-inputs-sha256" }}{% endraw %}')" = "$expected_image_digest"
      for app in eog-plugin-exif-display eog-plugin-map tracker-extract Shotwell FoxtrotGPS GTKam MiniDLNA Gerbera libexif-gtk3; do
        LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode runtime --only "$app"
      done
      awk -F'\t' 'NR > 1 { print $1 }' "$runtime_summary" >"$actual_runtime_names"
      diff -u "$expected_runtime_names" "$actual_runtime_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$runtime_summary")" = "14"
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT" bash safe/tests/run-package-build.sh >/dev/null
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_07_downstream_senior
    type: check
    bounce_target: impl_07_downstream_gui_services
    prompt: |-
      Reviewer role: Senior reviewer for `Downstream GUI and Service Runtime Matrix` in check phase `check_07_downstream_senior`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_07_downstream_senior`. Do not modify files.
      
      Specific Things to Verify
      - review the heavier downstream probes, confirm that app fixes were not left as Docker-only tribal knowledge, ensure every added probe is stable and deterministic, and confirm that runtime-mode summary generation is not deferred to the final phase.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services
      PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh
      cd /home/yans/code/safelibs/ported/libexif
      export LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_07_downstream_gui_services
      export LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_07_downstream_gui_services
      runtime_summary="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/runtime-matrix.tsv"
      expected_runtime_names=$(mktemp)
      actual_runtime_names=$(mktemp)
      trap 'rm -f "$expected_runtime_names" "$actual_runtime_names"' EXIT
      rm -f "$runtime_summary"
      ./test-original.sh --mode runtime
      test -f "$runtime_summary"
      test "$(head -n1 "$runtime_summary")" = $'name\tsource_package\tassertion\tartifact\tstatus'
      jq -r '.dependents[] | select(.runtime_functionality != null) | .name' dependents.json >"$expected_runtime_names"
      awk -F'\t' 'NR > 1 { print $1 }' "$runtime_summary" >"$actual_runtime_names"
      diff -u "$expected_runtime_names" "$actual_runtime_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$runtime_summary")" = "14"
      for app in eog-plugin-exif-display eog-plugin-map tracker-extract Shotwell FoxtrotGPS GTKam MiniDLNA Gerbera libexif-gtk3; do
        LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode runtime --only "$app"
      done
      awk -F'\t' 'NR > 1 { print $1 }' "$runtime_summary" >"$actual_runtime_names"
      diff -u "$expected_runtime_names" "$actual_runtime_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$runtime_summary")" = "14"
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT" bash safe/tests/run-package-build.sh >/dev/null
      git show --stat --format=fuller HEAD
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_08_safety_perf_docs
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_08_safety_perf_docs` (Safety, Performance, and Documentation Hardening).
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Preexisting Inputs
      - `safe/SAFETY.md`
      - `safe/README`
      - `safe/NEWS`
      - `safe/doc/libexif-api.html/`
      - `safe/build.rs`
      - `safe/cshim/`
      - `safe/src/`
      - `safe/tests/run-package-build.sh`
      - `safe/tests/run-performance-compare.sh`
      - `safe/tests/perf/`
      
      New Outputs
      - Phase-local package root `safe/.artifacts/impl_08_safety_perf_docs/` with fresh package metadata and validated overlay contents
      - Updated `safe/SAFETY.md`
      - Updated public-facing documentation that no longer claims the implementation is plain C
      - Performance-driven code or harness adjustments if the Rust completion work regresses throughput
      - Further unsafe reductions if low-risk cleanup remains after earlier phases
      
      File Changes
      - `safe/SAFETY.md`
      - `safe/README`
      - `safe/NEWS`
      - `safe/build.rs`
      - `safe/cshim/exif-log-shim.c`
      - `safe/src/**`
      - `safe/tests/run-performance-compare.sh`
      - `safe/tests/perf/{bench-driver.c,fixture-manifest.txt,thresholds.env}`
      
      Implementation Details
      - Consume or update the listed artifacts in place. Do not refetch, recollect, rediscover, or regenerate them from scratch.
      - Remove any newly unnecessary `unsafe` introduced during compatibility fixes. The final unsafe surface should be limited to true ABI or FFI boundaries and other clearly unavoidable cases.
      - Update `safe/SAFETY.md` so it matches the actual post-phase code, including whether the vendor MakerNote helper C sources are fully gone.
      - Update shipped docs such as `safe/README` so they accurately describe the Rust implementation and its remaining narrow foreign boundaries. The current wording that the library is written in plain C is stale.
      - Consume the existing API docs tree in place. Do not regenerate `safe/doc/libexif-api.html/` while it already exists; keep validating and shipping the checked-in tree.
      - Use the existing performance harness and thresholds as the contract. Reuse the phase-local package root for performance runs instead of rebuilding the same packaged library again.
      - Run one explicit `run-package-build.sh` call followed by `LIBEXIF_REQUIRE_REUSE=1 bash tests/run-performance-compare.sh`, plus a deliberate missing-root rerun of the performance wrapper that must fail with `reuse-required package root is missing or stale`. Do not let the performance wrapper silently rebuild the package root.
      - If a threshold needs to change, document why in both the code review and `safe/NEWS`, and keep the change backed by measured data rather than convenience.
      - Preserve or improve the current thresholds encoded in `safe/tests/perf/thresholds.env`; do not relax them without measured justification captured in both the code review and `safe/NEWS`.
      
      Success Criteria
      - `PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_08_safety_perf_docs bash tests/run-package-build.sh`
      - `rg -n "\\bunsafe\\b" src tests build.rs || true`
      - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_08_safety_perf_docs bash tests/run-performance-compare.sh`
      - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_08_safety_perf_docs.missing bash tests/run-performance-compare.sh` must fail with `reuse-required package root is missing or stale`
      - `cargo test --release`
      - `! rg -n "written in plain C|does not require any additional library" README SAFETY.md NEWS`
      
      Git Commit Requirement
      - Commit the phase work to git before yielding. Do not yield with uncommitted changes.
  - id: check_08_hardening_tester
    type: check
    bounce_target: impl_08_safety_perf_docs
    prompt: |-
      Reviewer role: Tester for `Safety, Performance, and Documentation Hardening` in check phase `check_08_hardening_tester`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_08_hardening_tester`. Do not modify files.
      
      Specific Things to Verify
      - verify that the remaining `unsafe` is justified, performance remains within thresholds, the shipped documentation describes the Rust implementation accurately, and the performance wrapper honors reuse-only mode.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_08_safety_perf_docs
      missing_perf_log=$(mktemp)
      missing_root="${PACKAGE_BUILD_ROOT}.missing"
      trap 'rm -f "$missing_perf_log"' EXIT
      PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh
      rg -n "\\bunsafe\\b" src tests build.rs || true
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-performance-compare.sh
      rm -rf "$missing_root"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_root" bash tests/run-performance-compare.sh >"$missing_perf_log" 2>&1; then
        cat "$missing_perf_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_perf_log"
      cargo test --release
      ! rg -n "written in plain C|does not require any additional library" README SAFETY.md NEWS
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_08_hardening_senior
    type: check
    bounce_target: impl_08_safety_perf_docs
    prompt: |-
      Reviewer role: Senior reviewer for `Safety, Performance, and Documentation Hardening` in check phase `check_08_hardening_senior`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_08_hardening_senior`. Do not modify files.
      
      Specific Things to Verify
      - audit each remaining `unsafe` category against `safe/SAFETY.md`, confirm that the MakerNote port reduced foreign implementation code, and review any performance-threshold or documentation changes.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_08_safety_perf_docs
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-performance-compare.sh
      git show --stat --format=fuller HEAD
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_09_final_release
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_09_final_release` (Final Catch-All and Release Gate).
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Preexisting Inputs
      - `original/`
      - `original/libexif/Makefile.am`
      - `original/configure.ac`
      - `original/libexif/libexif.sym`
      - `original/libexif/exif-tag.c`
      - `original/debian/libexif12.symbols`
      - `original/test/`
      - `original/test/nls/`
      - `original/contrib/examples/`
      - `safe/Cargo.toml`
      - `safe/Cargo.lock`
      - `safe/COPYING`
      - `safe/README`
      - `safe/NEWS`
      - `safe/SECURITY.md`
      - `safe/SAFETY.md`
      - `safe/build.rs`
      - `safe/cshim/`
      - `safe/src/`
      - `safe/include/libexif/`
      - `safe/libexif.pc.in`
      - `safe/libexif-uninstalled.pc.in`
      - `safe/debian/`
      - `safe/po/`
      - `safe/doc/libexif-api.html/`
      - `safe/contrib/examples/`
      - `safe/tests/`
      - `relevant_cves.json`
      - `all_cves.json`
      - `dependents.json`
      - `test-original.sh`
      - `.plan/goal.md`
      - `.plan/plan.md`
      - `.plan/findings.md`
      - `.plan/phases/01-abi-packaging.md`
      - `.plan/phases/02-core-behavior.md`
      - `.plan/phases/03-makernote-rust-completion.md`
      - `.plan/phases/04-source-link-compat.md`
      - `.plan/phases/05-downstream-compile.md`
      - `.plan/phases/06-downstream-cli-library.md`
      - `.plan/phases/07-downstream-gui-services.md`
      - `.plan/phases/08-safety-perf-docs.md`
      - `.plan/phases/09-final-release.md`
      - `.plan/workflow-structure.yaml`
      - `safe/.artifacts/impl_01_abi_packaging/`
      - `safe/.artifacts/impl_01_abi_packaging/metadata/source-commit.txt`
      - `safe/.artifacts/impl_01_abi_packaging/metadata/package-inputs.sha256`
      - `safe/.artifacts/impl_01_abi_packaging/metadata/validated.ok`
      - `safe/.artifacts/impl_01_abi_packaging/compile-smoke/static/public-api-smoke-static`
      - `safe/.artifacts/impl_04_source_link_compat/`
      - `safe/.artifacts/impl_04_source_link_compat/metadata/source-commit.txt`
      - `safe/.artifacts/impl_04_source_link_compat/metadata/package-inputs.sha256`
      - `safe/.artifacts/impl_04_source_link_compat/metadata/validated.ok`
      - `safe/.artifacts/impl_04_source_link_compat/compile-smoke/static/public-api-smoke-static`
      - `safe/.artifacts/impl_05_downstream_compile/`
      - `safe/.artifacts/impl_05_downstream_compile/metadata/source-commit.txt`
      - `safe/.artifacts/impl_05_downstream_compile/metadata/package-inputs.sha256`
      - `safe/.artifacts/impl_05_downstream_compile/metadata/validated.ok`
      - `safe/.artifacts/impl_05_downstream_compile/downstream/compile-matrix.tsv`
      - `safe/.artifacts/impl_06_downstream_cli_library/`
      - `safe/.artifacts/impl_06_downstream_cli_library/metadata/source-commit.txt`
      - `safe/.artifacts/impl_06_downstream_cli_library/metadata/package-inputs.sha256`
      - `safe/.artifacts/impl_06_downstream_cli_library/metadata/validated.ok`
      - `safe/.artifacts/impl_07_downstream_gui_services/`
      - `safe/.artifacts/impl_07_downstream_gui_services/metadata/source-commit.txt`
      - `safe/.artifacts/impl_07_downstream_gui_services/metadata/package-inputs.sha256`
      - `safe/.artifacts/impl_07_downstream_gui_services/metadata/validated.ok`
      - `safe/.artifacts/impl_07_downstream_gui_services/downstream/runtime-matrix.tsv`
      - `safe/.artifacts/impl_08_safety_perf_docs/`
      - `safe/.artifacts/impl_08_safety_perf_docs/metadata/source-commit.txt`
      - `safe/.artifacts/impl_08_safety_perf_docs/metadata/package-inputs.sha256`
      - `safe/.artifacts/impl_08_safety_perf_docs/metadata/validated.ok`
      
      New Outputs
      - Phase-local package root `safe/.artifacts/impl_09_final_release/` with fresh package metadata and validated overlay contents
      - Phase-local Docker image `libexif-original-test:impl_09_final_release` with recorded image-input freshness metadata
      - Final checker-visible downstream compile summary at `safe/.artifacts/impl_09_final_release/downstream/compile-matrix.tsv`
      - Final checker-visible downstream runtime summary at `safe/.artifacts/impl_09_final_release/downstream/runtime-matrix.tsv`
      - The final catch-all code, harness, packaging, and documentation fixes needed for a release candidate
      - Final in-repo regressions for any late-found issue
      - Final package or changelog adjustments for the release-ready state
      
      File Changes
      - `safe/build.rs`
      - `safe/Cargo.toml` and `safe/Cargo.lock` if the release gate exposes a final build-configuration or dependency correction
      - `safe/cshim/exif-log-shim.c`
      - `safe/src/`
      - `safe/include/libexif/`
      - `safe/libexif.pc.in`
      - `safe/libexif-uninstalled.pc.in`
      - `safe/debian/`
      - `safe/README`
      - `safe/NEWS`
      - `safe/SAFETY.md`
      - `safe/tests/`
      - `safe/tests/link-compat/`
      - `safe/tests/perf/`
      - `safe/tests/testdata/`
      - `safe/contrib/examples/`
      - `dependents.json` only if a factual inventory error must be corrected in place
      - `test-original.sh`
      
      Implementation Details
      - Consume or update the listed artifacts in place. Do not refetch, recollect, rediscover, or regenerate them from scratch.
      - This is the only catch-all phase. Use it to absorb residual failures discovered by earlier dedicated phases, not to invent new test classes.
      - Keep fixes focused and traceable. Every late fix should still come with a local regression or a strengthened existing harness assertion.
      - The final release gate must rerun `bash tests/run-original-test-suite.sh` after `cargo test --release`, even if earlier phases already used it. The end-of-work proof has to explicitly revalidate the copied original C, shell, NLS, parser, and MakerNote-sensitive suite at the final commit rather than relying on stale earlier evidence.
      - Preserve the consume-existing-artifacts contract at the final gate. Do not regenerate `dependents.json` or `safe/doc/libexif-api.html/` while they already exist, and keep consuming the existing link-compat manifests, performance fixtures, CVE manifests, locales, test data, and planning artifacts in place.
      - Preserve parser tests that require explicit fixtures. Any late harness edits must keep the existing explicit fixture argv and `TEST_IMAGES` contracts rather than weakening them into rediscovery or implicit fixture enumeration.
      - Preserve the exact package-root freshness contract from phase 1 in the final gate: `metadata/package-inputs.sha256` must contain the exact sorted `sha256sum`-style manifest, with one `<sha256><two spaces><repo-relative-path>` line per input file, over `safe/{Cargo.toml,Cargo.lock,build.rs,COPYING,README,NEWS,SECURITY.md,libexif.pc.in,libexif-uninstalled.pc.in}`, `safe/src/**`, `safe/cshim/**`, `safe/include/**`, `safe/debian/**`, `safe/po/**`, `safe/contrib/examples/**`, `safe/doc/libexif-api.html/**`, `safe/tests/run-package-build.sh`, and any `original/**` file still consumed by the safe build such as `original/libexif/libexif.sym` and `original/libexif/exif-tag.c`.
      - `./test-original.sh --mode all` must run the complete compile matrix first and then the runtime matrix, reusing the same validated `LIBEXIF_DOWNSTREAM_PACKAGE_ROOT` and the same validated `LIBEXIF_ORIGINAL_TEST_IMAGE` within that invocation, and both downstream summary files must come from that single run.
      - Reuse the phase-local package root and phase-local Docker image tag for every package-backed command in this phase. The final release checker must prove that contract both positively and negatively for every wrapper named in the workflow contract: `run-package-build.sh`, `run-export-compare.sh`, `run-c-compile-smoke.sh`, `run-original-object-link-compat.sh`, and `run-performance-compare.sh` must all succeed against the existing root under `LIBEXIF_REQUIRE_REUSE=1`; each must fail with `reuse-required package root is missing or stale` when pointed at a deliberately missing root; and at least one local wrapper must also be exercised against a deliberately stale copied root that fails with the same substring. `test-original.sh` must likewise reject deliberately missing and deliberately stale image or downstream-package-root inputs with the exact reuse-only substrings. Do not treat the initial package-root creation as sufficient evidence for `run-package-build.sh`; rerun it in explicit reuse-only mode against the populated final root and against a deliberately missing root. Do not rebuild the same final current-commit package artifacts between local scripts and the downstream Docker harness.
      - Earlier phase-local package roots remain immutable here: phase 9 may create and reuse `safe/.artifacts/impl_09_final_release/`, but it may not overwrite or repurpose any earlier phase-local package root.
      - The final release checker must verify the static-link proof directly by executing `safe/.artifacts/impl_09_final_release/compile-smoke/static/public-api-smoke-static` and confirming that `readelf -d` contains no `NEEDED` entry for `libexif.so.12`.
      - The final release checker must diff `safe/.artifacts/impl_09_final_release/downstream/compile-matrix.tsv` against `jq -r '.dependents[].name' dependents.json` and require exactly 15 `status=ok` rows so a partial compile matrix cannot pass.
      - The final release checker must diff `safe/.artifacts/impl_09_final_release/downstream/runtime-matrix.tsv` against `jq -r '.dependents[] | select(.runtime_functionality != null) | .name' dependents.json` and require exactly 14 `status=ok` rows so `--mode all` cannot silently skip runtime-capable dependents after catch-all edits.
      - Before the final `./test-original.sh --mode all` invocation, the checker must delete any preexisting `downstream/compile-matrix.tsv` and `downstream/runtime-matrix.tsv` files so both summaries are forced to be recreated by that exact final run instead of being inherited from an earlier phase.
      - Focused reuse-only reruns after the final full-matrix pass must leave the full 15-row compile summary and full 14-row runtime summary intact, and must leave `safe/.artifacts/impl_09_final_release/` valid for `LIBEXIF_REQUIRE_REUSE=1 bash safe/tests/run-package-build.sh`.
      - The final release checker must assert the exact compile-only rejection string `compile-only dependent: ImageMagick only supports --mode compile` rather than treating any nonzero exit as sufficient.
      - Do not parallelize the final verification commands. The shared package-build scripts and Docker harnesses must remain serial.
      - The final commit should represent the release-candidate state that all checkers reason about.
      
      Critical Files
      - `safe/build.rs`: central build glue for version-script generation, SONAME injection, gettext env propagation, the log shim, and the remaining MakerNote helper C compilation that phase 3 should remove; the current high-value seams are `safe/build.rs:13-57` and `safe/build.rs:62-278`.
      - `safe/Cargo.toml` and `safe/Cargo.lock`: crate type, dependency, and build-script configuration if ABI or helper removal requires adjustment.
      - `safe/src/ffi/types.rs` and `safe/src/ffi/panic_boundary.rs`: ABI layout mirrors and panic containment for exported C entry points; `safe/src/ffi/types.rs:101-167` is the core public-struct contract checked by `safe/tests/abi_layout.rs`.
      - `safe/src/primitives/{byte_order.rs,format.rs,ifd.rs,utils.rs}`: scalar conversion helpers and public low-level ABI functions.
      - `safe/src/i18n.rs`: gettext or localedir initialization used by translated table strings and the copied NLS checks.
      - `safe/src/tables/{mod.rs,data_options.rs,gps_ifd.rs,tag_table.rs}`: tag metadata, option strings, GPS-table data, and lookup compatibility.
      - `safe/src/runtime/{mem.rs,log.rs,cstdio.rs}` plus `safe/cshim/exif-log-shim.c`: allocator semantics, logging callbacks or varargs behavior, and the remaining unavoidable C boundary if it still exists after hardening; the current varargs bridge lives in `safe/cshim/exif-log-shim.c:1-30`.
      - `safe/src/object/{content.rs,data.rs,entry.rs}`: core `ExifContent`, `ExifData`, and `ExifEntry` semantics, including refcounts, parent pointers, default-tag creation, and value formatting; `safe/src/object/data.rs:21-220` and `safe/src/object/content.rs:21-240` are the main ownership and graph-manipulation seams.
      - `safe/src/parser/{data_load.rs,data_save.rs,loader.rs,limits.rs,mod.rs}`: load or save behavior, incremental loader state machine, checked arithmetic, and parse-budget enforcement; `safe/src/parser/data_load.rs:92-260`, `safe/src/parser/data_save.rs:22-260`, and `safe/src/parser/loader.rs:34-260` are the key compatibility and CVE-sensitive paths.
      - `safe/src/mnote/{mod.rs,base.rs,apple.rs,canon.rs,fuji.rs,olympus.rs,pentax.rs}`: the remaining MakerNote implementation surface. `safe/src/mnote/mod.rs:44-132` is the MakerNote construction and save bridge. `apple.rs` and `fuji.rs` are currently internal helper trampolines, while `canon.rs`, `olympus.rs`, and `pentax.rs` are still part of the exported ABI and therefore must keep their public symbol and formatting behavior while becoming genuinely Rust-owned.
      - `safe/include/libexif/*.h`, `safe/libexif.pc.in`, and `safe/libexif-uninstalled.pc.in`: installed public development surface; keep compatible with the original package.
      - `safe/debian/{rules,control,libexif12.symbols,libexif12.install,libexif-dev.install,libexif-dev.docs,libexif-doc.docs,libexif-doc.examples,libexif-doc.doc-base,changelog}`: Debian package assembly, symbol manifest, docs or examples ownership, changelog, and exact installed file layout.
      - `safe/README`, `safe/NEWS`, and `safe/SAFETY.md`: shipped documentation that must accurately describe the Rust port, remaining foreign boundaries, and release notes.
      - `safe/tests/{abi_layout.rs,primitives_tables.rs,object_model.rs,cve_regressions.rs,ported_c.rs}`: local ABI and behavior regression coverage; `safe/tests/abi_layout.rs:31-220`, `safe/tests/object_model.rs:35-133`, `safe/tests/primitives_tables.rs:56-260`, and `safe/tests/cve_regressions.rs:210-314` are the current high-signal regression anchors.
      - `safe/tests/{run-c-test.sh,run-original-test-suite.sh,run-original-shell-test.sh,run-original-nls-test.sh,run-export-compare.sh,run-package-build.sh,run-c-compile-smoke.sh,run-original-object-link-compat.sh,run-performance-compare.sh,run-cve-regressions.sh,run-test-mnote-matrix.sh}`: serial orchestration of the compatibility matrix, now including phase-local package-root reuse, `--print-package-inputs-manifest`, `LIBEXIF_REQUIRE_REUSE=1` reuse-only checks, the dedicated MakerNote matrix that phase 3 and phase 9 must run explicitly, and the fixed `compile-smoke/static/public-api-smoke-static` artifact that proves static linking. The most important current scripts are `safe/tests/run-package-build.sh:9-106`, `safe/tests/run-export-compare.sh:9-62`, `safe/tests/run-c-compile-smoke.sh:9-33`, `safe/tests/run-original-object-link-compat.sh:88-248`, and `safe/tests/run-performance-compare.sh:145-224`.
      - `safe/tests/original-c/**`, `safe/tests/original-sh/**`, and `safe/tests/support/**`: copied upstream tests, nested NLS fixtures, shell helpers, and support-only headers that must be updated in place rather than recopied wholesale.
      - `safe/tests/link-compat/{object-manifest.txt,run-manifest.txt}`: object-relink coverage and exact output-comparison policy.
      - `safe/tests/perf/{bench-driver.c,fixture-manifest.txt,thresholds.env}`: performance workloads, fixture inventory, and acceptance thresholds.
      - `safe/tests/downstream/**`: checked-in helper manifests or probe sources only if the downstream compile matrix becomes too large to maintain inline in `test-original.sh`.
      - `safe/tests/testdata/*` and `safe/contrib/examples/*.c`: fixtures and example programs used by compile, relink, runtime, and downstream checks.
      - `dependents.json`: existing 15-app downstream inventory that already satisfies the "dozen applications" requirement and should only be corrected in place if factually wrong.
      - `test-original.sh`: existing Docker-based downstream-app harness that must gain explicit compile or runtime modes, `--print-image-inputs-sha256`, freshness-checked phase-local image reuse, freshness-checked consumption of prebuilt safe package roots, `LIBEXIF_REQUIRE_REUSE=1` reuse-only enforcement with exact reuse-failure substrings, checker-visible `downstream/compile-matrix.tsv` and `downstream/runtime-matrix.tsv` artifacts for the full compile and runtime inventories, and a fixed compile-only `ImageMagick` rule. The current inventory and probe seams are `test-original.sh:149-165`, `test-original.sh:259-321`, `test-original.sh:564-1793`, and the per-app runtime probes at `test-original.sh:1413-1757`.
      
      Final Verification
      Run final verification serially, in this exact order, with no other package-backed script running:
      
      1. `cd /home/yans/code/safelibs/ported/libexif/safe && cargo test --release`
      2. `cd /home/yans/code/safelibs/ported/libexif/safe && bash tests/run-original-test-suite.sh`
      3. `cd /home/yans/code/safelibs/ported/libexif/safe && bash tests/run-test-mnote-matrix.sh`
      4. `cd /home/yans/code/safelibs/ported/libexif/safe && export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release && PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh`
      5. `cd /home/yans/code/safelibs/ported/libexif/safe && export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release && expected_manifest=$(mktemp) && trap 'rm -f "$expected_manifest"' EXIT && multiarch=$(dpkg-architecture -qDEB_HOST_MULTIARCH) && test -f "$PACKAGE_BUILD_ROOT/libexif-dev/usr/lib/$multiarch/libexif.a" && test -f "$PACKAGE_BUILD_ROOT/root/usr/lib/$multiarch/libexif.a" && test "$(cat "$PACKAGE_BUILD_ROOT/metadata/source-commit.txt")" = "$(git -C /home/yans/code/safelibs/ported/libexif rev-parse HEAD)" && bash tests/run-package-build.sh --print-package-inputs-manifest >"$expected_manifest" && diff -u "$expected_manifest" "$PACKAGE_BUILD_ROOT/metadata/package-inputs.sha256" && test -f "$PACKAGE_BUILD_ROOT/metadata/validated.ok" && LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh >/dev/null`
      6. `cd /home/yans/code/safelibs/ported/libexif/safe && export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release && LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-export-compare.sh`
      7. `cd /home/yans/code/safelibs/ported/libexif/safe && export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release && static_smoke="$PACKAGE_BUILD_ROOT/compile-smoke/static/public-api-smoke-static" && LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-c-compile-smoke.sh && test -x "$static_smoke" && "$static_smoke" && ! readelf -d "$static_smoke" | grep -F 'libexif.so.12'`
      8. `cd /home/yans/code/safelibs/ported/libexif/safe && export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release && LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-original-object-link-compat.sh`
      9. `cd /home/yans/code/safelibs/ported/libexif/safe && export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release && LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-performance-compare.sh`
      10. `cd /home/yans/code/safelibs/ported/libexif/safe && export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release && missing_local_root="$PACKAGE_BUILD_ROOT.missing" && stale_local_root="$PACKAGE_BUILD_ROOT.stale" && missing_build_log=$(mktemp) && missing_export_log=$(mktemp) && missing_compile_log=$(mktemp) && missing_link_log=$(mktemp) && missing_perf_log=$(mktemp) && stale_local_root_log=$(mktemp) && trap 'rm -f "$missing_build_log" "$missing_export_log" "$missing_compile_log" "$missing_link_log" "$missing_perf_log" "$stale_local_root_log"; rm -rf "$stale_local_root"' EXIT && rm -rf "$missing_local_root" && if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_local_root" bash tests/run-package-build.sh >"$missing_build_log" 2>&1; then cat "$missing_build_log" >&2; exit 1; fi && grep -F "reuse-required package root is missing or stale" "$missing_build_log" && if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_local_root" bash tests/run-export-compare.sh >"$missing_export_log" 2>&1; then cat "$missing_export_log" >&2; exit 1; fi && grep -F "reuse-required package root is missing or stale" "$missing_export_log" && if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_local_root" bash tests/run-c-compile-smoke.sh >"$missing_compile_log" 2>&1; then cat "$missing_compile_log" >&2; exit 1; fi && grep -F "reuse-required package root is missing or stale" "$missing_compile_log" && if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_local_root" bash tests/run-original-object-link-compat.sh >"$missing_link_log" 2>&1; then cat "$missing_link_log" >&2; exit 1; fi && grep -F "reuse-required package root is missing or stale" "$missing_link_log" && if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_local_root" bash tests/run-performance-compare.sh >"$missing_perf_log" 2>&1; then cat "$missing_perf_log" >&2; exit 1; fi && grep -F "reuse-required package root is missing or stale" "$missing_perf_log" && rm -rf "$stale_local_root" && cp -a "$PACKAGE_BUILD_ROOT" "$stale_local_root" && printf 'stale\n' >"$stale_local_root/metadata/source-commit.txt" && if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$stale_local_root" bash tests/run-export-compare.sh >"$stale_local_root_log" 2>&1; then cat "$stale_local_root_log" >&2; exit 1; fi && grep -F "reuse-required package root is missing or stale" "$stale_local_root_log"`
      11. `cd /home/yans/code/safelibs/ported/libexif && export LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_09_final_release LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release && expected_image_digest="$(./test-original.sh --print-image-inputs-sha256)" && compile_summary="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/compile-matrix.tsv" && runtime_summary="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/runtime-matrix.tsv" && expected_compile_names=$(mktemp) && actual_compile_names=$(mktemp) && expected_runtime_names=$(mktemp) && actual_runtime_names=$(mktemp) && trap 'rm -f "$expected_compile_names" "$actual_compile_names" "$expected_runtime_names" "$actual_runtime_names"' EXIT && rm -f "$compile_summary" "$runtime_summary" && ./test-original.sh --mode all && test "$(docker image inspect "$LIBEXIF_ORIGINAL_TEST_IMAGE" --format '{% raw %}{{ index .Config.Labels "io.safelibs.libexif.image-inputs-sha256" }}{% endraw %}')" = "$expected_image_digest" && test -f "$compile_summary" && test "$(head -n1 "$compile_summary")" = $'name\tsource_package\tassertion\tartifact\tstatus' && jq -r '.dependents[].name' dependents.json >"$expected_compile_names" && awk -F'\t' 'NR > 1 { print $1 }' "$compile_summary" >"$actual_compile_names" && diff -u "$expected_compile_names" "$actual_compile_names" && test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$compile_summary")" = "15" && test -f "$runtime_summary" && test "$(head -n1 "$runtime_summary")" = $'name\tsource_package\tassertion\tartifact\tstatus' && jq -r '.dependents[] | select(.runtime_functionality != null) | .name' dependents.json >"$expected_runtime_names" && awk -F'\t' 'NR > 1 { print $1 }' "$runtime_summary" >"$actual_runtime_names" && diff -u "$expected_runtime_names" "$actual_runtime_names" && test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$runtime_summary")" = "14"`
      12. `cd /home/yans/code/safelibs/ported/libexif && export LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_09_final_release LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release && compile_summary="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/compile-matrix.tsv" && runtime_summary="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/runtime-matrix.tsv" && expected_compile_names=$(mktemp) && actual_compile_names=$(mktemp) && expected_runtime_names=$(mktemp) && actual_runtime_names=$(mktemp) && trap 'rm -f "$expected_compile_names" "$actual_compile_names" "$expected_runtime_names" "$actual_runtime_names"' EXIT && jq -r '.dependents[].name' dependents.json >"$expected_compile_names" && jq -r '.dependents[] | select(.runtime_functionality != null) | .name' dependents.json >"$expected_runtime_names" && LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode compile --only exif && LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode runtime --only exif && awk -F'\t' 'NR > 1 { print $1 }' "$compile_summary" >"$actual_compile_names" && diff -u "$expected_compile_names" "$actual_compile_names" && test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$compile_summary")" = "15" && awk -F'\t' 'NR > 1 { print $1 }' "$runtime_summary" >"$actual_runtime_names" && diff -u "$expected_runtime_names" "$actual_runtime_names" && test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$runtime_summary")" = "14" && LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT" bash safe/tests/run-package-build.sh >/dev/null`
      13. `cd /home/yans/code/safelibs/ported/libexif && export LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_09_final_release LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release && missing_image_log=$(mktemp) && stale_image_log=$(mktemp) && missing_root_log=$(mktemp) && stale_root_log=$(mktemp) && stale_image_tag="${LIBEXIF_ORIGINAL_TEST_IMAGE}-stale" && stale_root="${LIBEXIF_DOWNSTREAM_PACKAGE_ROOT}.stale" && trap 'rm -f "$missing_image_log" "$stale_image_log" "$missing_root_log" "$stale_root_log"; rm -rf "$stale_root"; docker image rm -f "$stale_image_tag" >/dev/null 2>&1 || true' EXIT && docker image rm -f "${LIBEXIF_ORIGINAL_TEST_IMAGE}-missing" >/dev/null 2>&1 || true && if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE="${LIBEXIF_ORIGINAL_TEST_IMAGE}-missing" ./test-original.sh --mode compile --only exif >"$missing_image_log" 2>&1; then cat "$missing_image_log" >&2; exit 1; fi && grep -F "reuse-required docker image is missing or stale" "$missing_image_log" && docker tag ubuntu:24.04 "$stale_image_tag" && if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE="$stale_image_tag" ./test-original.sh --mode compile --only exif >"$stale_image_log" 2>&1; then cat "$stale_image_log" >&2; exit 1; fi && grep -F "reuse-required docker image is missing or stale" "$stale_image_log" && rm -rf "${LIBEXIF_DOWNSTREAM_PACKAGE_ROOT}.missing" && if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_DOWNSTREAM_PACKAGE_ROOT="${LIBEXIF_DOWNSTREAM_PACKAGE_ROOT}.missing" ./test-original.sh --mode compile --only exif >"$missing_root_log" 2>&1; then cat "$missing_root_log" >&2; exit 1; fi && grep -F "reuse-required package root is missing or stale" "$missing_root_log" && rm -rf "$stale_root" && cp -a "$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT" "$stale_root" && printf 'stale\n' >"$stale_root/metadata/source-commit.txt" && if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_DOWNSTREAM_PACKAGE_ROOT="$stale_root" ./test-original.sh --mode compile --only exif >"$stale_root_log" 2>&1; then cat "$stale_root_log" >&2; exit 1; fi && grep -F "reuse-required package root is missing or stale" "$stale_root_log"`
      14. `cd /home/yans/code/safelibs/ported/libexif && export LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_09_final_release LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release && imagemagick_runtime_log=$(mktemp) && trap 'rm -f "$imagemagick_runtime_log"' EXIT && if LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode runtime --only ImageMagick >"$imagemagick_runtime_log" 2>&1; then cat "$imagemagick_runtime_log" >&2; exit 1; fi && grep -F "compile-only dependent: ImageMagick only supports --mode compile" "$imagemagick_runtime_log"`
      
      Success Criteria
      The final result is acceptable only if all of the following are true:
      
        - The safe build still exports the exact symbol set and symbol versions required by the original manifests.
        - The packaged library, headers, static archive, docs, examples, doc-base entry, and locales install under the expected Debian paths and produce `libexif12`, `libexif-dev`, and `libexif-doc`.
        - The final package root's `metadata/source-commit.txt`, `metadata/package-inputs.sha256`, and `metadata/validated.ok` match the current workspace, and `bash tests/run-package-build.sh --print-package-inputs-manifest` diffs cleanly against the stored manifest.
        - The final gate reruns `bash tests/run-original-test-suite.sh`, so the copied original C, shell, NLS, parser, and MakerNote-sensitive suite is revalidated at the release-candidate commit; all copied original C, shell, NLS, CVE, object-model, and primitive tests pass, and the dedicated MakerNote matrix passes.
        - Previously compiled original object files relink against `libexif-safe` and match original runtime behavior and output files according to the manifest comparison policy.
        - Source-compiled smoke clients still build against the packaged headers and pkg-config data, and the static-link smoke path proves that the packaged `libexif.a` is usable by executing `safe/.artifacts/impl_09_final_release/compile-smoke/static/public-api-smoke-static` and showing that `readelf -d` contains no `NEEDED` entry for `libexif.so.12`.
        - Every current downstream name in `dependents.json` successfully completes its explicit compile-mode check against the installed safe packages, including the entries that previously had only runtime coverage and the compile-only `ImageMagick` source-build-plus-`identify` assertion, and `safe/.artifacts/impl_09_final_release/downstream/compile-matrix.tsv` proves that coverage with exactly 15 `status=ok` rows whose name column matches `dependents.json`.
        - The full downstream runtime matrix passes for every dependent whose `runtime_functionality` is non-null in `dependents.json`, `safe/.artifacts/impl_09_final_release/downstream/runtime-matrix.tsv` proves that coverage with exactly 14 `status=ok` rows whose name column matches the runtime-capable subset of `dependents.json`, and compile-only dependents are rejected from runtime mode with the exact substring `compile-only dependent: ImageMagick only supports --mode compile` while still passing compile mode.
        - Focused reuse-only downstream reruns leave the full-matrix `downstream/compile-matrix.tsv` and `downstream/runtime-matrix.tsv` files intact and leave `safe/.artifacts/impl_09_final_release/` valid for `LIBEXIF_REQUIRE_REUSE=1 bash safe/tests/run-package-build.sh`.
        - Reuse-only reruns are checker-visible: every package-backed local wrapper passes with `LIBEXIF_REQUIRE_REUSE=1` against the existing root, including `safe/tests/run-package-build.sh`; deliberately missing local package roots fail with `reuse-required package root is missing or stale` for every wrapper, including `safe/tests/run-package-build.sh`; at least one deliberately stale copied local root fails with the same substring; the final Docker image label matches `./test-original.sh --print-image-inputs-sha256`; targeted downstream reruns pass with `LIBEXIF_REQUIRE_REUSE=1`; deliberately missing and deliberately stale Docker images fail with `reuse-required docker image is missing or stale`; and deliberately missing and deliberately stale downstream package roots fail with `reuse-required package root is missing or stale`.
        - Performance remains within `safe/tests/perf/thresholds.env`.
        - `safe/SAFETY.md` matches the actual remaining `unsafe` or FFI surface, and any residual non-Rust implementation code is explicitly justified as unavoidable.
      
      Git Commit Requirement
      - Commit the phase work to git before yielding. Do not yield with uncommitted changes.
  - id: check_09_release_tester
    type: check
    bounce_target: impl_09_final_release
    prompt: |-
      Reviewer role: Tester for `Final Catch-All and Release Gate` in check phase `check_09_release_tester`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_09_release_tester`. Do not modify files.
      
      Specific Things to Verify
      - run the full serial end-to-end verification matrix and prove that the safe package remains a drop-in replacement across the copied original C/shell/NLS/parser suite, ABI, source, link, runtime, safety, packaging, performance, MakerNote, static-archive, and downstream-app surfaces, with artifact reuse and final compile-plus-runtime summary proof after `--mode all`.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release
      expected_manifest=$(mktemp)
      expected_compile_names=$(mktemp)
      actual_compile_names=$(mktemp)
      expected_runtime_names=$(mktemp)
      actual_runtime_names=$(mktemp)
      missing_build_root_log=$(mktemp)
      missing_export_root_log=$(mktemp)
      missing_compile_root_log=$(mktemp)
      missing_link_root_log=$(mktemp)
      missing_perf_root_log=$(mktemp)
      stale_local_root_log=$(mktemp)
      missing_image_log=$(mktemp)
      stale_image_log=$(mktemp)
      missing_downstream_root_log=$(mktemp)
      stale_downstream_root_log=$(mktemp)
      imagemagick_runtime_log=$(mktemp)
      static_smoke="$PACKAGE_BUILD_ROOT/compile-smoke/static/public-api-smoke-static"
      missing_local_root="${PACKAGE_BUILD_ROOT}.missing"
      stale_local_root="${PACKAGE_BUILD_ROOT}.stale"
      missing_downstream_root="${PACKAGE_BUILD_ROOT}.downstream-missing"
      stale_downstream_root="${PACKAGE_BUILD_ROOT}.downstream-stale"
      missing_image_tag="libexif-original-test:impl_09_final_release-missing"
      stale_image_tag="libexif-original-test:impl_09_final_release-stale"
      trap 'rm -f "$expected_manifest" "$expected_compile_names" "$actual_compile_names" "$expected_runtime_names" "$actual_runtime_names" "$missing_build_root_log" "$missing_export_root_log" "$missing_compile_root_log" "$missing_link_root_log" "$missing_perf_root_log" "$stale_local_root_log" "$missing_image_log" "$stale_image_log" "$missing_downstream_root_log" "$stale_downstream_root_log" "$imagemagick_runtime_log"; rm -rf "$stale_local_root" "$stale_downstream_root"; docker image rm -f "$stale_image_tag" >/dev/null 2>&1 || true' EXIT
      cargo test --release
      bash tests/run-original-test-suite.sh
      bash tests/run-test-mnote-matrix.sh
      PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh
      multiarch=$(dpkg-architecture -qDEB_HOST_MULTIARCH)
      test -f "$PACKAGE_BUILD_ROOT/libexif-dev/usr/lib/$multiarch/libexif.a"
      test -f "$PACKAGE_BUILD_ROOT/root/usr/lib/$multiarch/libexif.a"
      test "$(cat "$PACKAGE_BUILD_ROOT/metadata/source-commit.txt")" = "$(git -C /home/yans/code/safelibs/ported/libexif rev-parse HEAD)"
      bash tests/run-package-build.sh --print-package-inputs-manifest >"$expected_manifest"
      diff -u "$expected_manifest" "$PACKAGE_BUILD_ROOT/metadata/package-inputs.sha256"
      test -f "$PACKAGE_BUILD_ROOT/metadata/validated.ok"
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh >/dev/null
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-export-compare.sh
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-c-compile-smoke.sh
      test -x "$static_smoke"
      "$static_smoke"
      ! readelf -d "$static_smoke" | grep -F 'libexif.so.12'
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-original-object-link-compat.sh
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-performance-compare.sh
      rm -rf "$missing_local_root"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_local_root" bash tests/run-package-build.sh >"$missing_build_root_log" 2>&1; then
        cat "$missing_build_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_build_root_log"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_local_root" bash tests/run-export-compare.sh >"$missing_export_root_log" 2>&1; then
        cat "$missing_export_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_export_root_log"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_local_root" bash tests/run-c-compile-smoke.sh >"$missing_compile_root_log" 2>&1; then
        cat "$missing_compile_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_compile_root_log"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_local_root" bash tests/run-original-object-link-compat.sh >"$missing_link_root_log" 2>&1; then
        cat "$missing_link_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_link_root_log"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_local_root" bash tests/run-performance-compare.sh >"$missing_perf_root_log" 2>&1; then
        cat "$missing_perf_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_perf_root_log"
      rm -rf "$stale_local_root"
      cp -a "$PACKAGE_BUILD_ROOT" "$stale_local_root"
      printf 'stale\n' >"$stale_local_root/metadata/source-commit.txt"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$stale_local_root" bash tests/run-export-compare.sh >"$stale_local_root_log" 2>&1; then
        cat "$stale_local_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$stale_local_root_log"
      cd /home/yans/code/safelibs/ported/libexif
      export LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_09_final_release
      export LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release
      expected_image_digest="$(./test-original.sh --print-image-inputs-sha256)"
      compile_summary="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/compile-matrix.tsv"
      runtime_summary="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/runtime-matrix.tsv"
      rm -f "$compile_summary" "$runtime_summary"
      ./test-original.sh --mode all
      test "$(docker image inspect "$LIBEXIF_ORIGINAL_TEST_IMAGE" --format '{% raw %}{{ index .Config.Labels "io.safelibs.libexif.image-inputs-sha256" }}{% endraw %}')" = "$expected_image_digest"
      test -f "$compile_summary"
      test "$(head -n1 "$compile_summary")" = $'name\tsource_package\tassertion\tartifact\tstatus'
      jq -r '.dependents[].name' dependents.json >"$expected_compile_names"
      awk -F'\t' 'NR > 1 { print $1 }' "$compile_summary" >"$actual_compile_names"
      diff -u "$expected_compile_names" "$actual_compile_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$compile_summary")" = "15"
      test -f "$runtime_summary"
      test "$(head -n1 "$runtime_summary")" = $'name\tsource_package\tassertion\tartifact\tstatus'
      jq -r '.dependents[] | select(.runtime_functionality != null) | .name' dependents.json >"$expected_runtime_names"
      awk -F'\t' 'NR > 1 { print $1 }' "$runtime_summary" >"$actual_runtime_names"
      diff -u "$expected_runtime_names" "$actual_runtime_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$runtime_summary")" = "14"
      LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode compile --only exif
      LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode runtime --only exif
      awk -F'\t' 'NR > 1 { print $1 }' "$compile_summary" >"$actual_compile_names"
      diff -u "$expected_compile_names" "$actual_compile_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$compile_summary")" = "15"
      awk -F'\t' 'NR > 1 { print $1 }' "$runtime_summary" >"$actual_runtime_names"
      diff -u "$expected_runtime_names" "$actual_runtime_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$runtime_summary")" = "14"
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT" bash safe/tests/run-package-build.sh >/dev/null
      docker image rm -f "$missing_image_tag" >/dev/null 2>&1 || true
      if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE="$missing_image_tag" ./test-original.sh --mode compile --only exif >"$missing_image_log" 2>&1; then
        cat "$missing_image_log" >&2
        exit 1
      fi
      grep -F "reuse-required docker image is missing or stale" "$missing_image_log"
      docker tag ubuntu:24.04 "$stale_image_tag"
      if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE="$stale_image_tag" ./test-original.sh --mode compile --only exif >"$stale_image_log" 2>&1; then
        cat "$stale_image_log" >&2
        exit 1
      fi
      grep -F "reuse-required docker image is missing or stale" "$stale_image_log"
      rm -rf "$missing_downstream_root"
      if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_DOWNSTREAM_PACKAGE_ROOT="$missing_downstream_root" ./test-original.sh --mode compile --only exif >"$missing_downstream_root_log" 2>&1; then
        cat "$missing_downstream_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_downstream_root_log"
      rm -rf "$stale_downstream_root"
      cp -a "$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT" "$stale_downstream_root"
      printf 'stale\n' >"$stale_downstream_root/metadata/source-commit.txt"
      if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_DOWNSTREAM_PACKAGE_ROOT="$stale_downstream_root" ./test-original.sh --mode compile --only exif >"$stale_downstream_root_log" 2>&1; then
        cat "$stale_downstream_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$stale_downstream_root_log"
      if LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode runtime --only ImageMagick >"$imagemagick_runtime_log" 2>&1; then
        cat "$imagemagick_runtime_log" >&2
        exit 1
      fi
      grep -F "compile-only dependent: ImageMagick only supports --mode compile" "$imagemagick_runtime_log"
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_09_release_senior
    type: check
    bounce_target: impl_09_final_release
    prompt: |-
      Reviewer role: Senior reviewer for `Final Catch-All and Release Gate` in check phase `check_09_release_senior`.
      
      Repository Root: `/home/yans/code/safelibs/ported/libexif`
      
      Review the current workspace for check phase `check_09_release_senior`. Do not modify files.
      
      Specific Things to Verify
      - perform the final senior review, confirm that every earlier checker issue has a corresponding fix or regression, and verify that the final git state is release-ready, including the copied original C/shell/NLS/parser suite rerun, the static archive, all-wrapper reuse-only, stale-artifact, and full compile-plus-runtime matrix contracts.
      - Run the exact commands below as written. Treat every embedded diff, grep, exact-string, row-count, and file-existence assertion as mandatory when present.
      
      Commands to Run
      ```bash
      cd /home/yans/code/safelibs/ported/libexif
      git show --stat --format=fuller HEAD
      cd /home/yans/code/safelibs/ported/libexif/safe
      export PACKAGE_BUILD_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release
      expected_manifest=$(mktemp)
      expected_compile_names=$(mktemp)
      actual_compile_names=$(mktemp)
      expected_runtime_names=$(mktemp)
      actual_runtime_names=$(mktemp)
      missing_local_root_log=$(mktemp)
      missing_build_root_log=$(mktemp)
      missing_image_log=$(mktemp)
      stale_image_log=$(mktemp)
      imagemagick_runtime_log=$(mktemp)
      static_smoke="$PACKAGE_BUILD_ROOT/compile-smoke/static/public-api-smoke-static"
      missing_local_root="${PACKAGE_BUILD_ROOT}.missing"
      stale_image_tag="libexif-original-test:impl_09_final_release-stale"
      trap 'rm -f "$expected_manifest" "$expected_compile_names" "$actual_compile_names" "$expected_runtime_names" "$actual_runtime_names" "$missing_local_root_log" "$missing_build_root_log" "$missing_image_log" "$stale_image_log" "$imagemagick_runtime_log"; docker image rm -f "$stale_image_tag" >/dev/null 2>&1 || true' EXIT
      cargo test --release
      bash tests/run-original-test-suite.sh
      bash tests/run-test-mnote-matrix.sh
      multiarch=$(dpkg-architecture -qDEB_HOST_MULTIARCH)
      test -f "$PACKAGE_BUILD_ROOT/libexif-dev/usr/lib/$multiarch/libexif.a"
      bash tests/run-package-build.sh --print-package-inputs-manifest >"$expected_manifest"
      diff -u "$expected_manifest" "$PACKAGE_BUILD_ROOT/metadata/package-inputs.sha256"
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-package-build.sh >/dev/null
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-export-compare.sh
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-c-compile-smoke.sh
      test -x "$static_smoke"
      "$static_smoke"
      ! readelf -d "$static_smoke" | grep -F 'libexif.so.12'
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-original-object-link-compat.sh
      LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PACKAGE_BUILD_ROOT" bash tests/run-performance-compare.sh
      rm -rf "$missing_local_root"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_local_root" bash tests/run-package-build.sh >"$missing_build_root_log" 2>&1; then
        cat "$missing_build_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_build_root_log"
      if LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$missing_local_root" bash tests/run-c-compile-smoke.sh >"$missing_local_root_log" 2>&1; then
        cat "$missing_local_root_log" >&2
        exit 1
      fi
      grep -F "reuse-required package root is missing or stale" "$missing_local_root_log"
      cd /home/yans/code/safelibs/ported/libexif
      export LIBEXIF_ORIGINAL_TEST_IMAGE=libexif-original-test:impl_09_final_release
      export LIBEXIF_DOWNSTREAM_PACKAGE_ROOT=/home/yans/code/safelibs/ported/libexif/safe/.artifacts/impl_09_final_release
      expected_image_digest="$(./test-original.sh --print-image-inputs-sha256)"
      compile_summary="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/compile-matrix.tsv"
      runtime_summary="$LIBEXIF_DOWNSTREAM_PACKAGE_ROOT/downstream/runtime-matrix.tsv"
      rm -f "$compile_summary" "$runtime_summary"
      ./test-original.sh --mode all
      test "$(docker image inspect "$LIBEXIF_ORIGINAL_TEST_IMAGE" --format '{% raw %}{{ index .Config.Labels "io.safelibs.libexif.image-inputs-sha256" }}{% endraw %}')" = "$expected_image_digest"
      test -f "$compile_summary"
      test "$(head -n1 "$compile_summary")" = $'name\tsource_package\tassertion\tartifact\tstatus'
      jq -r '.dependents[].name' dependents.json >"$expected_compile_names"
      awk -F'\t' 'NR > 1 { print $1 }' "$compile_summary" >"$actual_compile_names"
      diff -u "$expected_compile_names" "$actual_compile_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$compile_summary")" = "15"
      test -f "$runtime_summary"
      test "$(head -n1 "$runtime_summary")" = $'name\tsource_package\tassertion\tartifact\tstatus'
      jq -r '.dependents[] | select(.runtime_functionality != null) | .name' dependents.json >"$expected_runtime_names"
      awk -F'\t' 'NR > 1 { print $1 }' "$runtime_summary" >"$actual_runtime_names"
      diff -u "$expected_runtime_names" "$actual_runtime_names"
      test "$(awk -F'\t' 'NR > 1 && $5 == "ok" { count++ } END { print count + 0 }' "$runtime_summary")" = "14"
      LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode runtime --only Shotwell
      docker image rm -f "${LIBEXIF_ORIGINAL_TEST_IMAGE}-missing" >/dev/null 2>&1 || true
      if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE="${LIBEXIF_ORIGINAL_TEST_IMAGE}-missing" ./test-original.sh --mode compile --only exif >"$missing_image_log" 2>&1; then
        cat "$missing_image_log" >&2
        exit 1
      fi
      grep -F "reuse-required docker image is missing or stale" "$missing_image_log"
      docker tag ubuntu:24.04 "$stale_image_tag"
      if LIBEXIF_REQUIRE_REUSE=1 LIBEXIF_ORIGINAL_TEST_IMAGE="$stale_image_tag" ./test-original.sh --mode compile --only exif >"$stale_image_log" 2>&1; then
        cat "$stale_image_log" >&2
        exit 1
      fi
      grep -F "reuse-required docker image is missing or stale" "$stale_image_log"
      if LIBEXIF_REQUIRE_REUSE=1 ./test-original.sh --mode runtime --only ImageMagick >"$imagemagick_runtime_log" 2>&1; then
        cat "$imagemagick_runtime_log" >&2
        exit 1
      fi
      grep -F "compile-only dependent: ImageMagick only supports --mode compile" "$imagemagick_runtime_log"
      ```
      
      End with exactly one verdict line:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
