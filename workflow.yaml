linear: true
yaml_source_mode: inline-only
verifier_encoding: explicit-phases
phases:
  - id: impl_01_validator_setup_baseline
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_01_validator_setup_baseline` (`Validator Setup, Package Override, and Baseline Run`).
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      
      This prompt is self-contained. The agent sees only this prompt. Consume or update the listed Preexisting Inputs in place. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist. Preserve unrelated user work.
      
      Workflow topology is fixed by `.plan/workflow-structure.yaml`: execute this implement phase as `impl_01_validator_setup_baseline` and leave its verifier phases immediately after it in this order: `check_01_setup_software_tester`, `check_01_setup_senior_tester`. Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists. Use only the fixed verifier `bounce_target` values from the structure file.
      
      Commit work to git before yielding. Stage only files that are in scope for this phase and do not stage validator run artifacts, build artifacts, or unrelated work.
      
      # Phase Name
      
      Validator Setup, Package Override, and Baseline Run
      
      # Implement Phase ID
      
      `impl_01_validator_setup_baseline`
      
      # Preexisting Inputs
      
      - `/home/yans/safelibs/pipeline/ports/port-libexif/safe`
      - `/home/yans/safelibs/pipeline/ports/port-libexif/original`
      - `dependents.json`, `relevant_cves.json`, `all_cves.json`, and `test-original.sh`; consume these existing artifacts in place and do not recollect, rediscover, regenerate, or replace them from scratch.
      - `safe/PORT.md`; use only as existing background documentation and do not regenerate it for this goal.
      - `safe/Cargo.toml` defines package `libexif-safe` version `0.6.24`, edition 2021, with library target `exif` emitted as `rlib`, `cdylib`, and `staticlib`.
      - `safe/build.rs` reads `original/libexif/libexif.sym`, preprocesses `original/libexif/exif-tag.c`, writes generated Rust/linker artifacts into Cargo `OUT_DIR`, and compiles `safe/cshim/exif-log-shim.c`.
      - Public C ABI surface inputs: `safe/include/libexif/*`, `safe/debian/*`, `safe/libexif.pc.in`, `safe/libexif-uninstalled.pc.in`, `safe/src/ffi/types.rs`, and exported functions in `safe/src/**/*`.
      - Existing local tests and harnesses: `safe/tests/*.rs`, `safe/tests/run-cve-regressions.sh`, `safe/tests/run-original-test-suite.sh`, `safe/tests/run-export-compare.sh`, `safe/tests/run-c-compile-smoke.sh`, `safe/tests/run-package-build.sh`, copied original C/shell tests, smoke C tests, and MakerNote fixtures under `safe/tests/testdata/*`.
      - Existing reusable package artifact root `safe/.artifacts/impl_09_final_release`; `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt` records stale commit `2ed681a8f27bf33ab58718465ee44306ce142afe`, so this phase must refresh the package root before copying `.deb` files into the validator override root.
      - Remote validator repository `https://github.com/safelibs/validator`.
      - Validator reference `main` commit `5d908be26e33f071e119ffe1a52e3149f1e5ec4e`.
      - Validator README/code facts at that revision: Docker-based Ubuntu 24.04 matrix, `original` and `port` modes, package-oriented `port` mode, required `<override-deb-root>/<library>/*.deb`, required `--port-deb-lock`, and no direct library-path mode.
      - Local repository facts: reference parent repo HEAD `7e641db0a231ea0acf64cdaed5497909e645b5d7`; unrelated tracked `workflow.yaml` modification; untracked `safe/.artifacts/` and `safe/target/`; no nested validator checkout at `/home/yans/safelibs/pipeline/ports/port-libexif/validator` at planning time; existing `.plan/phases/*` and `.plan/workflow-structure.yaml` were an older documentation workflow and are not inputs to this validator plan.
      - Validator manifest facts at commit `5d908be26e33f071e119ffe1a52e3149f1e5ec4e`: 5 source cases and 130 usage cases, 135 total; source case ids are `compile-link-smoke`, `invalid-data-handling`, `jpeg-exif-c-api-parse`, `maker-note-handling`, and `tag-lookup-value-formatting`; usage cases run the Ubuntu `exif` CLI against overridden `libexif12` and `libexif-dev`.
      - Docker, `make`, `python3`, `jq`, `git`, `dpkg`, `dpkg-deb`, `dpkg-architecture`, and a working C toolchain.
      
      # New Outputs
      
      - `validator/` nested checkout, cloned or fast-forwarded only in this phase.
      - Recorded cloned or pulled validator commit actually used.
      - `validator/artifacts/debs/local/libexif/libexif12_*.deb`
      - `validator/artifacts/debs/local/libexif/libexif-dev_*.deb`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator/artifacts/libexif-original/**` original Ubuntu-package baseline artifacts when Docker is available.
      - `validator/artifacts/libexif-safe/**` local override validator artifacts and proof.
      - `validator-report.md` initial report with validator commit, commands executed, package artifact provenance, testcase counts, original Ubuntu baseline, local override results, failure table, fix plan by class, validator bug waivers, package-build blocker fixes, and final status.
      - Top-level `.gitignore` entries for `/validator/`, `/safe/.artifacts/`, and `/safe/target/` if missing.
      - Minimal `safe/` package-build prerequisite fix only if the existing package build blocks all validator execution.
      - One parent-repo commit for setup/report work.
      
      # File Changes
      
      - Create or update `.gitignore` with `/validator/`, `/safe/.artifacts/`, and `/safe/target/`.
      - Create `validator-report.md`.
      - If `safe/tests/run-package-build.sh` or package inputs fail before any validator testcase can run, make the smallest prerequisite fix under `safe/`, add a local smoke/regression check when practical, and record it under `Package-build blocker fixes:` in `validator-report.md`.
      - Do not edit `safe/` for validator testcase failures in this phase.
      - Do not modify the validator suite to make failures pass. The only exception is a clearly identified validator bug, which is not expected in this setup phase.
      - Treat `validator/` and `validator/artifacts/` as run artifacts, not parent-repo deliverables. Do not stage or commit them in the parent repo.
      - Do not stage unrelated existing work such as `workflow.yaml`, `safe/.artifacts/`, or `safe/target/`.
      
      # Implementation Details
      
      1. Execute linearly. Do not use `parallel_groups` or any other parallel topology.
      2. Use self-contained inline-only YAML when this phase is encoded into workflow YAML. Do not use top-level `include`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or any other YAML-source indirection. Define every verifier as an explicit top-level `check` phase with exactly one fixed `bounce_target` inside this implement block. Do not use `bounce_targets` lists.
      3. Consume existing artifacts in place. Treat `safe/`, `original/`, `dependents.json`, `relevant_cves.json`, `all_cves.json`, `test-original.sh`, `safe/PORT.md`, `safe/tests/**/*`, and `safe/.artifacts/impl_09_final_release` as inputs. Do not recollect, rediscover, regenerate, or replace them from scratch.
      4. Refresh the package artifact root only with `safe/tests/run-package-build.sh` and `PACKAGE_BUILD_ROOT=/home/yans/safelibs/pipeline/ports/port-libexif/safe/.artifacts/impl_09_final_release`.
      5. Check phases must not refresh package artifacts; any check phase invoking `safe/tests/run-package-build.sh` or a package-backed harness must use `LIBEXIF_REQUIRE_REUSE=1`.
      6. Create or update the validator checkout exactly once in this phase at `validator/`. Later phases must use that checkout and must not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      7. Use validator `port` mode with local override packages. The local override directory is `validator/artifacts/debs/local/libexif/` and must contain only canonical local `libexif12_*.deb` and `libexif-dev_*.deb` unless a validator log proves another canonical package is required.
      8. Write the local lock to `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`.
      9. The local lock must use schema `1`, mode `port`, and one `libraries` entry for `libexif`.
      10. The `libexif` lock entry must set `repository` to `local:/home/yans/safelibs/pipeline/ports/port-libexif/safe`, `commit` to the current parent repo `git rev-parse HEAD`, `release_tag` to `local-<short-parent-commit>`, `tag_ref` to `refs/tags/local-<short-parent-commit>`, `unported_original_packages` to `[]`, and `debs` entries for `libexif12` and `libexif-dev` in canonical apt package order.
      11. Each lock `debs` entry must record the package name, exact filename, `Architecture` field from `dpkg-deb --field`, lowercase SHA256, and byte size for the copied file under `validator/artifacts/debs/local/libexif/`.
      12. Include top-level lock fields `generated_at: "1970-01-01T00:00:00Z"`, `source_config: "repositories.yml"`, and `source_inventory: "local:/home/yans/safelibs/pipeline/ports/port-libexif/safe"` for clarity even though current validator `run_matrix.py` only requires `schema_version`, `mode`, and `libraries`.
      13. Validator `port` mode can return success even when testcase failures are recorded, because failures after successful override installation are validation findings. Every checker that reruns the validator must inspect `*/port/results/libexif/summary.json` and fail explicitly for forbidden failures.
      14. Every checker must assert provenance freshness: `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt` equals current parent `HEAD`; the local lock commit equals current parent `HEAD`; copied override `.deb` filenames and SHA256 values match canonical files in `safe/.artifacts/impl_09_final_release/artifacts/`; every result JSON in the checked artifact root has `port_commit` equal to current parent `HEAD`.
      15. Any checker that validates a proof must also assert the proof's `libraries[] | select(.library=="libexif") | .port_commit` equals current parent `HEAD`.
      16. Record parent repo state with `git status --short --branch` and `git rev-parse HEAD`. Preserve unrelated tracked and untracked work.
      17. Check Docker availability with `docker info`. If Docker is unavailable, write `validator-report.md` with the blocker, commit the report, perform the post-commit package/lock refresh if possible, and do not mark the goal complete.
      18. Clone or update the validator only here:
          - `git -C validator pull --ff-only` if `validator/.git` exists.
          - `git clone https://github.com/safelibs/validator validator` otherwise.
          - Record `git -C validator rev-parse HEAD` and the validator README mode/override details in `validator-report.md`.
      19. Run validator setup checks:
          - `cd validator && make unit`
          - `cd validator && python3 tools/testcases.py --config repositories.yml --tests-root tests --library libexif --list-summary`
          - `cd validator && python3 tools/testcases.py --config repositories.yml --tests-root tests --library libexif --check --min-source-cases 5 --min-usage-cases 130 --min-cases 135`
      20. Refresh local safe packages:
          - `PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
          If this fails before any validator testcase can run, fix the package-build prerequisite in this phase, rerun it, and report the fix.
      21. Verify `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt` equals `git rev-parse HEAD`.
      22. Rebuild `validator/artifacts/debs/local/libexif/` by removing the old leaf, recreating it, and copying only `libexif12_*.deb` and `libexif-dev_*.deb` from `safe/.artifacts/impl_09_final_release/artifacts/`.
      23. Generate `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json` using the exact schema and fields above.
      24. Run original baseline:
          - `cd validator && RECORD_CASTS=1 ARTIFACT_ROOT=artifacts/libexif-original LIBRARY=libexif make matrix-original`
          - `cd validator && python3 tools/verify_proof_artifacts.py --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-original --proof-output artifacts/libexif-original/proof/original-validation-proof.json --mode original --library libexif --require-casts --min-source-cases 5 --min-usage-cases 130 --min-cases 135`
      25. Run local safe port matrix and proof:
          - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
          - `cd validator && jq . artifacts/libexif-safe/port/results/libexif/summary.json`
          - `cd validator && python3 tools/verify_proof_artifacts.py --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe --proof-output artifacts/libexif-safe/proof/libexif-safe-validation-proof.json --mode port --library libexif --require-casts --min-source-cases 5 --min-usage-cases 130 --min-cases 135`
      26. Populate `validator-report.md` with `Validator checkout`, `libexif-safe package under test`, `Commands executed`, `Original Ubuntu baseline`, `Local override validator run`, `Failure table`, `Fix plan by class`, `Validator bug waivers`, `Package-build blocker fixes`, and `Final status`. Make `validator/artifacts/libexif-safe/**` the authoritative post-commit artifact path and rely on checker assertions for final commit-specific provenance.
      27. Build the failure table from `validator/artifacts/libexif-safe/port/results/libexif/*.json`. Classify source API/ABI/package failures to phase 2, ordinary CLI/metadata usage failures to phase 3, malformed/crash/timeout/safety failures to phase 4, and unclassified failures or validator-bug candidates to phase 5.
      28. Stage only `.gitignore`, `validator-report.md`, and any phase-owned prerequisite safe fix. Do not stage `validator/`, `safe/.artifacts/`, `safe/target/`, or unrelated files.
      29. Immediately after the parent-repo commit, rerun package build for the new `HEAD`, recopy the two canonical `.deb`s, and regenerate the local lock for the new `HEAD`.
      30. Remove stale safe baseline run outputs while preserving the local lock:
          - `cd validator && rm -rf artifacts/libexif-safe/port artifacts/libexif-safe/proof/libexif-safe-validation-proof.json`
      31. Rerun the local safe port matrix and proof into `validator/artifacts/libexif-safe` using the post-commit lock:
          - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
          - `cd validator && python3 tools/verify_proof_artifacts.py --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe --proof-output artifacts/libexif-safe/proof/libexif-safe-validation-proof.json --mode port --library libexif --require-casts --min-source-cases 5 --min-usage-cases 130 --min-cases 135`
      32. Do not edit or recommit `validator-report.md` after the post-commit setup rerun. The report must point to the artifact path; checker assertions establish exact post-commit provenance.
      
      # Verification Phases
      
      ## `check_01_setup_software_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_01_validator_setup_baseline`
      - Purpose: Verify validator checkout, package override layout, reuse-only package freshness, lock/result/proof provenance, baseline artifacts, and report structure.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `test -d validator/.git`
        - `git -C validator rev-parse HEAD`
        - `test -f validator-report.md`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `cmp -s safe/.artifacts/impl_09_final_release/metadata/package-inputs.sha256 <(bash safe/tests/run-package-build.sh --print-package-inputs-manifest)`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test -d validator/artifacts/debs/local/libexif`
        - `ls validator/artifacts/debs/local/libexif/libexif12_*.deb validator/artifacts/debs/local/libexif/libexif-dev_*.deb`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `cd validator && python3 tools/testcases.py --config repositories.yml --tests-root tests --library libexif --check --min-source-cases 5 --min-usage-cases 130 --min-cases 135`
        - `cd validator && jq -e '.cases >= 135 and .source_cases >= 5 and .usage_cases >= 130 and (.passed + .failed == .cases)' artifacts/libexif-safe/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .override_debs_installed != true) | .testcase_id' artifacts/libexif-safe/port/results/libexif/*.json)"`
        - `jq -e --arg head "$(git rev-parse HEAD)" '.libraries[] | select(.library=="libexif") | .port_commit == $head' validator/artifacts/libexif-safe/proof/libexif-safe-validation-proof.json`
      
      ## `check_01_setup_senior_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_01_validator_setup_baseline`
      - Purpose: Review setup scope, artifact contract, no staged validator clone, baseline classification, and any phase-1 prerequisite package-build fix.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `git status --short --ignored`
        - `git show --stat --format=fuller HEAD`
        - `git diff --name-only HEAD~1..HEAD`
        - `test -z "$(git diff --name-only HEAD~1..HEAD | rg -v '^(\.gitignore|validator-report\.md|safe/)' || true)"`
        - `if git diff --name-only HEAD~1..HEAD | rg '^safe/' >/dev/null; then grep -q '^Package-build blocker fixes: ' validator-report.md; fi`
        - `git -C validator status --short`
        - `grep -E 'Validator commit|libexif-safe package|Commands executed|Failure table|Fix plan by class|Final status' validator-report.md`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `cd validator && jq -e '.cases >= 135 and (.passed + .failed == .cases)' artifacts/libexif-safe/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe/port/results/libexif/*.json)"`
        - `jq -e --arg head "$(git rev-parse HEAD)" '.libraries[] | select(.library=="libexif") | .port_commit == $head' validator/artifacts/libexif-safe/proof/libexif-safe-validation-proof.json`
      
      # Success Criteria
      
      - Validator checkout exists at `validator/` and the actual checked-out commit is recorded in `validator-report.md`.
      - The validator commit used is compatible with reference `5d908be26e33f071e119ffe1a52e3149f1e5ec4e`, including Ubuntu 24.04 Docker matrix behavior, package-oriented `port` mode, and no direct library-path mode.
      - `safe/.artifacts/impl_09_final_release`, copied override `.deb`s, local lock, result JSON, and proof all point to the current parent `HEAD`.
      - `validator/artifacts/debs/local/libexif/` contains only canonical local `libexif12_*.deb` and `libexif-dev_*.deb` unless a validator log proves another canonical package is required.
      - The local lock contains exact required fields: schema `1`, mode `port`, `repository`, `commit`, `release_tag`, `tag_ref`, `unported_original_packages`, `debs`, `generated_at`, `source_config`, and `source_inventory`.
      - `make unit`, testcase summary/check commands, original Ubuntu baseline, local port matrix, and proof verification have run or blockers are explicitly documented.
      - Baseline result set contains all current libexif cases unless the report documents a current validator count change.
      - Every failed result has a failure class and owning phase.
      - Parent commit excludes the nested validator checkout/artifacts, `safe/.artifacts/`, `safe/target/`, and unrelated work.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. Stage only `.gitignore`, `validator-report.md`, and any phase-owned prerequisite `safe/` fix; after the commit, refresh the package root, recopy canonical override `.deb`s, regenerate `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json` for the new `HEAD`, and rerun the post-commit local port matrix and proof without making a second report commit.
  - id: check_01_setup_software_tester
    type: check
    bounce_target: impl_01_validator_setup_baseline
    prompt: |-
      Reviewer role: Software tester reviewer for `check_01_setup_software_tester`.
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      Fixed bounce target: `impl_01_validator_setup_baseline`
      
      You are reviewing the work from `impl_01_validator_setup_baseline`. Run the commands listed below exactly where applicable, inspect their outputs and artifacts, and verify the phase-specific requirements. Do not refresh package artifacts except through commands that explicitly set `LIBEXIF_REQUIRE_REUSE=1`; do not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      
      Preexisting Inputs
      - `validator/.git`
      - `validator-report.md`
      - `.gitignore`
      - `safe/.artifacts/impl_09_final_release`
      - `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt`
      - `safe/.artifacts/impl_09_final_release/metadata/package-inputs.sha256`
      - `safe/.artifacts/impl_09_final_release/artifacts/`
      - `validator/artifacts/debs/local/libexif/`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator/artifacts/libexif-safe/port/results/libexif/summary.json`
      - `validator/artifacts/libexif-safe/port/results/libexif/`
      - `validator/artifacts/libexif-safe/proof/libexif-safe-validation-proof.json`
      - `validator/artifacts/libexif-original/`
      
      Specific Things To Verify
      - validator checkout and recorded commit exist and are not parent-repo deliverables
      - package override directory contains the canonical local libexif Debian packages
      - package root, local lock, result JSON, and proof provenance all match current parent HEAD
      - validator report contains the required setup, failure classification, and status sections
      
      ## `check_01_setup_software_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_01_validator_setup_baseline`
      - Purpose: Verify validator checkout, package override layout, reuse-only package freshness, lock/result/proof provenance, baseline artifacts, and report structure.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `test -d validator/.git`
        - `git -C validator rev-parse HEAD`
        - `test -f validator-report.md`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `cmp -s safe/.artifacts/impl_09_final_release/metadata/package-inputs.sha256 <(bash safe/tests/run-package-build.sh --print-package-inputs-manifest)`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test -d validator/artifacts/debs/local/libexif`
        - `ls validator/artifacts/debs/local/libexif/libexif12_*.deb validator/artifacts/debs/local/libexif/libexif-dev_*.deb`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `cd validator && python3 tools/testcases.py --config repositories.yml --tests-root tests --library libexif --check --min-source-cases 5 --min-usage-cases 130 --min-cases 135`
        - `cd validator && jq -e '.cases >= 135 and .source_cases >= 5 and .usage_cases >= 130 and (.passed + .failed == .cases)' artifacts/libexif-safe/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .override_debs_installed != true) | .testcase_id' artifacts/libexif-safe/port/results/libexif/*.json)"`
        - `jq -e --arg head "$(git rev-parse HEAD)" '.libraries[] | select(.library=="libexif") | .port_commit == $head' validator/artifacts/libexif-safe/proof/libexif-safe-validation-proof.json`
      
      End your response with exactly one of these verdict lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_01_setup_senior_tester
    type: check
    bounce_target: impl_01_validator_setup_baseline
    prompt: |-
      Reviewer role: Senior tester reviewer for `check_01_setup_senior_tester`.
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      Fixed bounce target: `impl_01_validator_setup_baseline`
      
      You are reviewing the work from `impl_01_validator_setup_baseline`. Run the commands listed below exactly where applicable, inspect their outputs and artifacts, and verify the phase-specific requirements. Do not refresh package artifacts except through commands that explicitly set `LIBEXIF_REQUIRE_REUSE=1`; do not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      
      Preexisting Inputs
      - `validator/.git`
      - `validator-report.md`
      - `.gitignore`
      - `safe/.artifacts/impl_09_final_release`
      - `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt`
      - `safe/.artifacts/impl_09_final_release/artifacts/`
      - `validator/artifacts/debs/local/libexif/`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator/artifacts/libexif-safe/port/results/libexif/summary.json`
      - `validator/artifacts/libexif-safe/port/results/libexif/`
      - `validator/artifacts/libexif-safe/proof/libexif-safe-validation-proof.json`
      
      Specific Things To Verify
      - validator checkout and recorded commit exist and are not parent-repo deliverables
      - package override directory contains the canonical local libexif Debian packages
      - package root, local lock, result JSON, and proof provenance all match current parent HEAD
      - validator report contains the required setup, failure classification, and status sections
      
      ## `check_01_setup_senior_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_01_validator_setup_baseline`
      - Purpose: Review setup scope, artifact contract, no staged validator clone, baseline classification, and any phase-1 prerequisite package-build fix.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `git status --short --ignored`
        - `git show --stat --format=fuller HEAD`
        - `git diff --name-only HEAD~1..HEAD`
        - `test -z "$(git diff --name-only HEAD~1..HEAD | rg -v '^(\.gitignore|validator-report\.md|safe/)' || true)"`
        - `if git diff --name-only HEAD~1..HEAD | rg '^safe/' >/dev/null; then grep -q '^Package-build blocker fixes: ' validator-report.md; fi`
        - `git -C validator status --short`
        - `grep -E 'Validator commit|libexif-safe package|Commands executed|Failure table|Fix plan by class|Final status' validator-report.md`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `cd validator && jq -e '.cases >= 135 and (.passed + .failed == .cases)' artifacts/libexif-safe/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe/port/results/libexif/*.json)"`
        - `jq -e --arg head "$(git rev-parse HEAD)" '.libraries[] | select(.library=="libexif") | .port_commit == $head' validator/artifacts/libexif-safe/proof/libexif-safe-validation-proof.json`
      
      End your response with exactly one of these verdict lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_02_fix_source_api_validator_failures
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_02_fix_source_api_validator_failures` (`Fix Source API, ABI, Packaging, and C-Level Validator Failures`).
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      
      This prompt is self-contained. The agent sees only this prompt. Consume or update the listed Preexisting Inputs in place. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist. Preserve unrelated user work.
      
      Workflow topology is fixed by `.plan/workflow-structure.yaml`: execute this implement phase as `impl_02_fix_source_api_validator_failures` and leave its verifier phases immediately after it in this order: `check_02_source_software_tester`, `check_02_source_senior_tester`. Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists. Use only the fixed verifier `bounce_target` values from the structure file.
      
      Commit work to git before yielding. Stage only files that are in scope for this phase and do not stage validator run artifacts, build artifacts, or unrelated work.
      
      # Phase Name
      
      Fix Source API, ABI, Packaging, and C-Level Validator Failures
      
      # Implement Phase ID
      
      `impl_02_fix_source_api_validator_failures`
      
      # Preexisting Inputs
      
      - `validator-report.md` with the phase-1 failure table and source-class assignments.
      - Existing `validator/` checkout created or updated only by phase 1. This phase must use that checkout and must not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      - Phase-1 validator artifacts, including `validator/artifacts/libexif-safe/**`, `validator/artifacts/libexif-original/**` when Docker was available, result JSON logs/casts, and `validator/artifacts/libexif-safe/proof/libexif-safe-validation-proof.json`.
      - `validator/artifacts/debs/local/libexif/*.deb` containing the canonical local `libexif12_*.deb` and `libexif-dev_*.deb`.
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`.
      - `safe/`, `original/`, `dependents.json`, `relevant_cves.json`, `all_cves.json`, `test-original.sh`, `safe/PORT.md`, `safe/tests/**/*`, and `safe/.artifacts/impl_09_final_release`; consume these existing artifacts in place and do not recollect, rediscover, regenerate, or replace them from scratch.
      - `safe/src/**/*`, `safe/include/libexif/*`, `safe/debian/*`, `safe/libexif.pc.in`, `safe/libexif-uninstalled.pc.in`, `safe/build.rs`, `safe/Cargo.toml`, and `safe/Cargo.lock`.
      - Existing local harnesses: `safe/tests/*.rs`, `safe/tests/run-cve-regressions.sh`, `safe/tests/run-original-test-suite.sh`, `safe/tests/run-export-compare.sh`, `safe/tests/run-c-compile-smoke.sh`, `safe/tests/run-package-build.sh`, copied original C/shell tests, smoke C tests, and MakerNote fixtures under `safe/tests/testdata/*`.
      - Upstream ABI evidence in `original/libexif/libexif.sym`, `original/debian/libexif12.symbols`, `original/test/*`, and generated artifacts produced by `safe/build.rs`.
      - Validator reference facts from phase 1: reference validator `main` commit `5d908be26e33f071e119ffe1a52e3149f1e5ec4e`; Docker-based Ubuntu 24.04 matrix; `original` and package-oriented `port` modes; `port` mode requires `<override-deb-root>/<library>/*.deb` and `--port-deb-lock`; this validator revision has no direct library-path mode.
      - Validator libexif manifest facts: 5 source cases and 130 usage cases, 135 total; source case ids `compile-link-smoke`, `invalid-data-handling`, `jpeg-exif-c-api-parse`, `maker-note-handling`, and `tag-lookup-value-formatting`; usage cases run the Ubuntu `exif` CLI against overridden `libexif12` and `libexif-dev`.
      
      # New Outputs
      
      - Minimal local regression tests for each fixed source-class failure.
      - Fixed `libexif-safe` source, header, pkg-config, symbol, or packaging behavior as needed.
      - Refreshed `safe/.artifacts/impl_09_final_release` package artifacts for the phase commit.
      - Re-copied canonical override `.deb`s under `validator/artifacts/debs/local/libexif/`.
      - Regenerated `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json` for the phase commit.
      - Updated `validator-report.md` with source-class dispositions and retest results.
      - One parent-repo commit.
      
      # File Changes
      
      - Prefer source-class regressions in `safe/tests/validator_regressions.rs`.
      - For compile/link or header/pkg-config failures, add `safe/tests/smoke/validator-compile-link.c` when needed and wire it into `safe/tests/run-c-compile-smoke.sh`.
      - Conditional implementation files: `safe/include/libexif/*.h`, `safe/libexif.pc.in`, `safe/libexif-uninstalled.pc.in`, `safe/debian/*`, `safe/build.rs`, `safe/src/tables/tag_table.rs`, `safe/src/object/*`, `safe/src/parser/*`, `safe/src/primitives/utils.rs`, and `safe/src/mnote/*.rs`.
      - Always update `validator-report.md`.
      - Do not modify the validator suite to make legitimate libexif-safe failures pass.
      - Treat `validator/`, `validator/artifacts/`, `safe/.artifacts/`, and `safe/target/` as non-committed run/build artifacts.
      
      # Implementation Details
      
      - Execute linearly. Do not introduce `parallel_groups`, workflow indirection, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or `bounce_targets` lists. This phase's verifiers are explicit top-level `check` phases with fixed `bounce_target: impl_02_fix_source_api_validator_failures`.
      - Start only from failures assigned to this phase in `validator-report.md`. Consume existing phase-1 artifacts instead of rediscovering or regenerating setup artifacts.
      - Preserve the package artifact contract: refresh the package root only with `PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`; check phases must use `LIBEXIF_REQUIRE_REUSE=1` when invoking `safe/tests/run-package-build.sh` or package-backed harnesses.
      - Use validator `port` mode with local override packages. Do not run `make fetch-port-debs`; do not use direct library-path mode; do not add noncanonical `.deb`s unless a validator log proves another canonical package is required.
      - Maintain the local lock at `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json` with schema `1`, mode `port`, one `libexif` library entry, `repository: local:/home/yans/safelibs/pipeline/ports/port-libexif/safe`, `commit: <current parent HEAD>`, `release_tag: local-<short-parent-commit>`, `tag_ref: refs/tags/local-<short-parent-commit>`, `unported_original_packages: []`, canonical apt-order `debs` entries for `libexif12` and `libexif-dev`, exact filenames, `Architecture` from `dpkg-deb --field`, lowercase SHA256, byte size, `generated_at: "1970-01-01T00:00:00Z"`, `source_config: "repositories.yml"`, and `source_inventory: "local:/home/yans/safelibs/pipeline/ports/port-libexif/safe"`.
      - Reproduce each source failure locally before fixing when practical.
      - Add traceable regression coverage for `compile-link-smoke`, `invalid-data-handling`, `jpeg-exif-c-api-parse`, `maker-note-handling`, and `tag-lookup-value-formatting` as applicable.
      - Every new regression test name or comment must reference the validator testcase id or failure class it covers.
      - Fix the underlying ABI, parser, object, table, MakerNote, header, or package boundary. Do not edit validator scripts for legitimate libexif-safe failures.
      - If no source-class failures exist, update `validator-report.md` with that no-op disposition and commit it. Use `git commit --allow-empty` only if the report already contains the exact disposition and no file changed.
      - After edits, run relevant local tests, refresh packages, recopy override `.deb`s, rerun the full libexif validator matrix, update `validator-report.md`, and commit before yielding.
      - Validator `port` mode can return success even when testcase failures are recorded. Inspect the relevant `*/port/results/libexif/summary.json` and result JSON files explicitly instead of trusting process status.
      - Immediately after the parent-repo commit, refresh `safe/.artifacts/impl_09_final_release`, recopy canonical override `.deb`s, and regenerate the local lock for the new `HEAD`. This applies to report-only commits because the package script keys freshness to `HEAD`.
      - Every post-commit checker must be able to assert: `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt` equals current parent `HEAD`; local lock commit equals current parent `HEAD`; copied override `.deb` filenames and SHA256 values match canonical files in `safe/.artifacts/impl_09_final_release/artifacts/`; every checked result JSON has `port_commit` equal to current parent `HEAD`.
      
      # Verification Phases
      
      ## `check_02_source_software_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_02_fix_source_api_validator_failures`
      - Purpose: Confirm source-class validator failures have local regressions, pass locally, and no source cases still fail.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `test ! -f safe/tests/validator_regressions.rs || cargo test --manifest-path safe/Cargo.toml --test validator_regressions -- --test-threads=1`
        - `cargo test --manifest-path safe/Cargo.toml --release`
        - `bash safe/tests/run-cve-regressions.sh`
        - `bash safe/tests/run-original-test-suite.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-c-compile-smoke.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-check02 --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
        - `cd validator && jq -e '.source_cases >= 5 and .usage_cases >= 130 and (.passed + .failed == .cases)' artifacts/libexif-safe-check02/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe-check02/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .override_debs_installed != true) | .testcase_id' artifacts/libexif-safe-check02/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .status=="failed" and .kind=="source") | .testcase_id' artifacts/libexif-safe-check02/port/results/libexif/*.json)"`
      
      ## `check_02_source_senior_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_02_fix_source_api_validator_failures`
      - Purpose: Review source fixes for ABI compatibility, package surface correctness, regression traceability, and non-overfitting.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `git show --stat --format=fuller HEAD`
        - `git diff HEAD~1..HEAD -- safe/src safe/tests safe/include safe/debian safe/Cargo.toml safe/build.rs validator-report.md`
        - `rg -n 'validator|compile-link-smoke|invalid-data-handling|jpeg-exif-c-api-parse|maker-note-handling|tag-lookup-value-formatting' safe/tests validator-report.md`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-export-compare.sh`
        - `objdump -T safe/.artifacts/impl_09_final_release/root/usr/lib/$(dpkg-architecture -qDEB_HOST_MULTIARCH)/libexif.so.12.3.4 | rg 'exif_data_new|exif_loader_write|exif_entry_get_value|exif_tag_get_name_in_ifd|exif_mnote_data_canon_new'`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `test -z "$(jq -r --arg head "$(git rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' validator/artifacts/libexif-safe-check02/port/results/libexif/*.json)"`
        - `test -z "$(jq -r 'select(.testcase_id and .status=="failed" and .kind=="source") | .testcase_id' validator/artifacts/libexif-safe-check02/port/results/libexif/*.json)"`
      
      # Success Criteria
      
      - New local regressions pass and reference the relevant validator testcase id or source failure class.
      - All source-class failures assigned in `validator-report.md` are fixed or explicitly reported as no-op because none existed.
      - No result in `validator/artifacts/libexif-safe-check02/port/results/libexif/*.json` has `status=="failed"` and `kind=="source"`.
      - Header, pkg-config, symbol, ABI, parser, object, table, and MakerNote fixes preserve upstream libexif behavior and do not overfit validator scripts.
      - Package/result/lock provenance is tied to current parent `HEAD`.
      - Parent repo commit contains only `validator-report.md` and necessary `safe/` files; it excludes nested validator files and run artifacts.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. If no source-class failures exist, commit the `validator-report.md` no-op disposition, using `git commit --allow-empty` only when the exact disposition is already present and no file changed; immediately after the commit, refresh the package root, recopy canonical override `.deb`s, and regenerate the local lock for the new `HEAD`.
  - id: check_02_source_software_tester
    type: check
    bounce_target: impl_02_fix_source_api_validator_failures
    prompt: |-
      Reviewer role: Software tester reviewer for `check_02_source_software_tester`.
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      Fixed bounce target: `impl_02_fix_source_api_validator_failures`
      
      You are reviewing the work from `impl_02_fix_source_api_validator_failures`. Run the commands listed below exactly where applicable, inspect their outputs and artifacts, and verify the phase-specific requirements. Do not refresh package artifacts except through commands that explicitly set `LIBEXIF_REQUIRE_REUSE=1`; do not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      
      Preexisting Inputs
      - `safe/Cargo.toml`
      - `safe/tests/`
      - `safe/.artifacts/impl_09_final_release`
      - `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt`
      - `safe/.artifacts/impl_09_final_release/artifacts/`
      - `validator/`
      - `validator/artifacts/debs/local/libexif/`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator-report.md`
      
      Specific Things To Verify
      - source/API/ABI/package validator failures assigned to phase 2 are covered by local regressions or a documented no-op disposition
      - local safe tests and package-backed smoke checks pass using reuse-only package artifacts
      - the check02 validator artifact root has no failed source cases
      - package, lock, copied debs, and result JSON provenance match current parent HEAD
      
      ## `check_02_source_software_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_02_fix_source_api_validator_failures`
      - Purpose: Confirm source-class validator failures have local regressions, pass locally, and no source cases still fail.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `test ! -f safe/tests/validator_regressions.rs || cargo test --manifest-path safe/Cargo.toml --test validator_regressions -- --test-threads=1`
        - `cargo test --manifest-path safe/Cargo.toml --release`
        - `bash safe/tests/run-cve-regressions.sh`
        - `bash safe/tests/run-original-test-suite.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-c-compile-smoke.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-check02 --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
        - `cd validator && jq -e '.source_cases >= 5 and .usage_cases >= 130 and (.passed + .failed == .cases)' artifacts/libexif-safe-check02/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe-check02/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .override_debs_installed != true) | .testcase_id' artifacts/libexif-safe-check02/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .status=="failed" and .kind=="source") | .testcase_id' artifacts/libexif-safe-check02/port/results/libexif/*.json)"`
      
      End your response with exactly one of these verdict lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_02_source_senior_tester
    type: check
    bounce_target: impl_02_fix_source_api_validator_failures
    prompt: |-
      Reviewer role: Senior tester reviewer for `check_02_source_senior_tester`.
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      Fixed bounce target: `impl_02_fix_source_api_validator_failures`
      
      You are reviewing the work from `impl_02_fix_source_api_validator_failures`. Run the commands listed below exactly where applicable, inspect their outputs and artifacts, and verify the phase-specific requirements. Do not refresh package artifacts except through commands that explicitly set `LIBEXIF_REQUIRE_REUSE=1`; do not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      
      Preexisting Inputs
      - `safe/src/`
      - `safe/tests/`
      - `safe/include/`
      - `safe/debian/`
      - `safe/Cargo.toml`
      - `safe/build.rs`
      - `safe/.artifacts/impl_09_final_release`
      - `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt`
      - `safe/.artifacts/impl_09_final_release/artifacts/`
      - `validator/artifacts/debs/local/libexif/`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator/artifacts/libexif-safe-check02/port/results/libexif/`
      - `validator-report.md`
      
      Specific Things To Verify
      - source/API/ABI/package validator failures assigned to phase 2 are covered by local regressions or a documented no-op disposition
      - local safe tests and package-backed smoke checks pass using reuse-only package artifacts
      - the check02 validator artifact root has no failed source cases
      - package, lock, copied debs, and result JSON provenance match current parent HEAD
      
      ## `check_02_source_senior_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_02_fix_source_api_validator_failures`
      - Purpose: Review source fixes for ABI compatibility, package surface correctness, regression traceability, and non-overfitting.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `git show --stat --format=fuller HEAD`
        - `git diff HEAD~1..HEAD -- safe/src safe/tests safe/include safe/debian safe/Cargo.toml safe/build.rs validator-report.md`
        - `rg -n 'validator|compile-link-smoke|invalid-data-handling|jpeg-exif-c-api-parse|maker-note-handling|tag-lookup-value-formatting' safe/tests validator-report.md`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-export-compare.sh`
        - `objdump -T safe/.artifacts/impl_09_final_release/root/usr/lib/$(dpkg-architecture -qDEB_HOST_MULTIARCH)/libexif.so.12.3.4 | rg 'exif_data_new|exif_loader_write|exif_entry_get_value|exif_tag_get_name_in_ifd|exif_mnote_data_canon_new'`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `test -z "$(jq -r --arg head "$(git rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' validator/artifacts/libexif-safe-check02/port/results/libexif/*.json)"`
        - `test -z "$(jq -r 'select(.testcase_id and .status=="failed" and .kind=="source") | .testcase_id' validator/artifacts/libexif-safe-check02/port/results/libexif/*.json)"`
      
      End your response with exactly one of these verdict lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_03_fix_cli_usage_metadata_failures
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_03_fix_cli_usage_metadata_failures` (`Fix CLI Usage, Metadata Formatting, XML, Thumbnail, and Dependent-Client Failures`).
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      
      This prompt is self-contained. The agent sees only this prompt. Consume or update the listed Preexisting Inputs in place. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist. Preserve unrelated user work.
      
      Workflow topology is fixed by `.plan/workflow-structure.yaml`: execute this implement phase as `impl_03_fix_cli_usage_metadata_failures` and leave its verifier phases immediately after it in this order: `check_03_usage_software_tester`, `check_03_usage_senior_tester`. Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists. Use only the fixed verifier `bounce_target` values from the structure file.
      
      Commit work to git before yielding. Stage only files that are in scope for this phase and do not stage validator run artifacts, build artifacts, or unrelated work.
      
      # Phase Name
      
      Fix CLI Usage, Metadata Formatting, XML, Thumbnail, and Dependent-Client Failures
      
      # Implement Phase ID
      
      `impl_03_fix_cli_usage_metadata_failures`
      
      # Preexisting Inputs
      
      - Phase-1 and phase-2 validator artifacts and report entries, including `validator-report.md`, `validator/artifacts/libexif-safe/**`, and `validator/artifacts/libexif-safe-check02/**`.
      - Existing `validator/` checkout created or updated only by phase 1. This phase must use that checkout and must not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      - `validator/tests/libexif/tests/cases/usage/*.sh` from the existing checkout.
      - Validator sample fixtures under `validator/tests/libexif/tests/fixtures/samples/`.
      - Validator logs/casts and result JSON showing the `exif` CLI usage behavior.
      - `safe/`, `original/`, `dependents.json`, `relevant_cves.json`, `all_cves.json`, `test-original.sh`, `safe/PORT.md`, `safe/tests/**/*`, and `safe/.artifacts/impl_09_final_release`; consume these existing artifacts in place and do not recollect, rediscover, regenerate, or replace them from scratch.
      - Local fixtures under `safe/tests/testdata/*`, copied original C/shell tests, smoke C tests, and MakerNote fixtures.
      - Existing local harnesses: `safe/tests/*.rs`, `safe/tests/run-cve-regressions.sh`, `safe/tests/run-original-test-suite.sh`, `safe/tests/run-export-compare.sh`, `safe/tests/run-c-compile-smoke.sh`, and `safe/tests/run-package-build.sh`.
      - Public ABI and behavior inputs: `safe/src/**/*`, `safe/include/libexif/*`, `safe/debian/*`, `safe/libexif.pc.in`, `safe/libexif-uninstalled.pc.in`, `safe/src/ffi/types.rs`, and exported functions in `safe/src/**/*`.
      - Validator reference facts from phase 1: reference validator `main` commit `5d908be26e33f071e119ffe1a52e3149f1e5ec4e`; Docker-based Ubuntu 24.04 matrix; `original` and package-oriented `port` modes; `port` mode requires `<override-deb-root>/<library>/*.deb` and `--port-deb-lock`; this validator revision has no direct library-path mode.
      - Validator libexif manifest facts: 5 source cases and 130 usage cases, 135 total; usage cases run the Ubuntu `exif` CLI against overridden `libexif12` and `libexif-dev`; usage failures are libexif ABI/behavior regressions unless evidence proves a validator bug.
      
      # New Outputs
      
      - Minimal local regressions for each usage-class failure.
      - Fixed value formatting, save/load, thumbnail, MakerNote, tag, XML, machine-readable, or dependent-client behavior in `safe/src`.
      - Refreshed `safe/.artifacts/impl_09_final_release` package artifacts for the phase commit.
      - Re-copied canonical override `.deb`s under `validator/artifacts/debs/local/libexif/`.
      - Regenerated `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json` for the phase commit.
      - Updated `validator-report.md` with usage-class dispositions and retest results.
      - One parent-repo commit.
      
      # File Changes
      
      - Prefer `safe/tests/validator_regressions.rs` for direct ABI regressions that model `exif` CLI expectations.
      - Use `safe/tests/cve_regressions.rs` only for malformed input or CVE-like safety regressions that also appear in usage logs.
      - Likely implementation files: `safe/src/object/entry.rs`, `safe/src/tables/tag_table.rs`, `safe/src/parser/data_load.rs`, `safe/src/parser/data_save.rs`, `safe/src/mnote/*.rs`, and `safe/src/primitives/utils.rs`.
      - Always update `validator-report.md`.
      - Do not modify validator scripts for legitimate usage failures.
      - Treat `validator/`, `validator/artifacts/`, `safe/.artifacts/`, and `safe/target/` as non-committed run/build artifacts.
      
      # Implementation Details
      
      - Execute linearly. Do not introduce `parallel_groups`, workflow indirection, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or `bounce_targets` lists. This phase's verifiers are explicit top-level `check` phases with fixed `bounce_target: impl_03_fix_cli_usage_metadata_failures`.
      - Start only from failures assigned to this phase in `validator-report.md`. Consume existing prior artifacts instead of regenerating setup artifacts.
      - Preserve the package artifact contract: refresh the package root only with `PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`; check phases must use `LIBEXIF_REQUIRE_REUSE=1` when invoking `safe/tests/run-package-build.sh` or package-backed harnesses.
      - Use validator `port` mode with local override packages. Do not run `make fetch-port-debs`; do not use direct library-path mode; do not add noncanonical `.deb`s unless a validator log proves another canonical package is required.
      - Maintain the local lock at `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json` with schema `1`, mode `port`, one `libexif` library entry, `repository: local:/home/yans/safelibs/pipeline/ports/port-libexif/safe`, `commit: <current parent HEAD>`, `release_tag: local-<short-parent-commit>`, `tag_ref: refs/tags/local-<short-parent-commit>`, `unported_original_packages: []`, canonical apt-order `debs` entries for `libexif12` and `libexif-dev`, exact filenames, `Architecture` from `dpkg-deb --field`, lowercase SHA256, byte size, `generated_at: "1970-01-01T00:00:00Z"`, `source_config: "repositories.yml"`, and `source_inventory: "local:/home/yans/safelibs/pipeline/ports/port-libexif/safe"`.
      - Inspect validator logs for each usage failure and map the failing `exif` CLI behavior to underlying libexif ABI calls.
      - Add direct Rust/C ABI regressions using the same fixture and tag/value expectation whenever practical. Do not add tests that shell out to the validator from `safe/`.
      - Every new regression test name or comment must reference the validator testcase id or usage failure class it covers.
      - Preserve upstream behavior by checking `original/` code and copied original tests before changing formatting strings or tag tables.
      - For XML and machine-readable failures, test the underlying tag names, IFD names, values, and escaping-sensitive strings produced by libexif APIs.
      - For thumbnail and set/remove/readback failures, add regressions around `exif_data_save_data`, `exif_data_new_from_data`, `exif_content_remove_entry`, and thumbnail fields as appropriate.
      - If no usage-class failures exist, update `validator-report.md` with that no-op disposition and commit it. Use `git commit --allow-empty` only if the report already contains the exact disposition and no file changed.
      - Commit before yielding, then refresh package root, recopy override `.deb`s, and regenerate the local lock for the new `HEAD`.
      - Validator `port` mode can return success even when testcase failures are recorded. Inspect the relevant `*/port/results/libexif/summary.json` and result JSON files explicitly instead of trusting process status.
      - Every post-commit checker must be able to assert: `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt` equals current parent `HEAD`; local lock commit equals current parent `HEAD`; copied override `.deb` filenames and SHA256 values match canonical files in `safe/.artifacts/impl_09_final_release/artifacts/`; every checked result JSON has `port_commit` equal to current parent `HEAD`.
      
      # Verification Phases
      
      ## `check_03_usage_software_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_03_fix_cli_usage_metadata_failures`
      - Purpose: Confirm usage-class failures are locally covered and no non-safety usage cases still fail.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `cargo test --manifest-path safe/Cargo.toml --release`
        - `bash safe/tests/run-original-test-suite.sh`
        - `bash safe/tests/run-cve-regressions.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-check03 --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
        - `cd validator && jq -e '.source_cases >= 5 and .usage_cases >= 130 and (.passed + .failed == .cases)' artifacts/libexif-safe-check03/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe-check03/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .override_debs_installed != true) | .testcase_id' artifacts/libexif-safe-check03/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .status=="failed" and .kind=="usage" and (((.testcase_id // "") | test("invalid|truncated|corrupt|malformed|timeout|crash|no-exif-error"; "i")) | not)) | .testcase_id' artifacts/libexif-safe-check03/port/results/libexif/*.json)"`
      
      ## `check_03_usage_senior_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_03_fix_cli_usage_metadata_failures`
      - Purpose: Review whether usage fixes preserve upstream libexif semantics instead of hard-coding validator strings.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `git show --stat --format=fuller HEAD`
        - `git diff HEAD~1..HEAD -- safe/src safe/tests validator-report.md`
        - `rg -n 'validator|usage-exif-cli|EXIF_TAG_|mnote_|thumbnail|xml|machine' safe/tests validator-report.md`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-export-compare.sh`
        - `jq -e '.source_cases >= 5 and .usage_cases >= 130 and (.passed + .failed == .cases)' validator/artifacts/libexif-safe-check03/port/results/libexif/summary.json`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `test -z "$(jq -r --arg head "$(git rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' validator/artifacts/libexif-safe-check03/port/results/libexif/*.json)"`
        - `test -z "$(jq -r 'select(.testcase_id and .status=="failed" and .kind=="usage" and (((.testcase_id // "") | test("invalid|truncated|corrupt|malformed|timeout|crash|no-exif-error"; "i")) | not)) | .testcase_id' validator/artifacts/libexif-safe-check03/port/results/libexif/*.json)"`
      
      # Success Criteria
      
      - Relevant local ABI/behavior regressions pass and reference the validator testcase id or usage failure class they cover.
      - No usage failures assigned to this phase remain in `validator/artifacts/libexif-safe-check03/port/results/libexif/*.json`.
      - Any remaining usage failures are only malformed/crash/timeout/safety-class cases intentionally left for phase 4 or documented validator-bug candidates left for phase 5.
      - Fixes preserve upstream libexif semantics for tag names, values, IFD names, XML/machine-readable output, thumbnail behavior, and save/load behavior.
      - Package/result/lock provenance is tied to current parent `HEAD`.
      - Parent repo commit contains only `validator-report.md` and necessary `safe/` files; it excludes nested validator files and run artifacts.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. If no usage-class failures exist, commit the `validator-report.md` no-op disposition, using `git commit --allow-empty` only when the exact disposition is already present and no file changed; immediately after the commit, refresh the package root, recopy canonical override `.deb`s, and regenerate the local lock for the new `HEAD`.
  - id: check_03_usage_software_tester
    type: check
    bounce_target: impl_03_fix_cli_usage_metadata_failures
    prompt: |-
      Reviewer role: Software tester reviewer for `check_03_usage_software_tester`.
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      Fixed bounce target: `impl_03_fix_cli_usage_metadata_failures`
      
      You are reviewing the work from `impl_03_fix_cli_usage_metadata_failures`. Run the commands listed below exactly where applicable, inspect their outputs and artifacts, and verify the phase-specific requirements. Do not refresh package artifacts except through commands that explicitly set `LIBEXIF_REQUIRE_REUSE=1`; do not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      
      Preexisting Inputs
      - `safe/Cargo.toml`
      - `safe/tests/`
      - `safe/.artifacts/impl_09_final_release`
      - `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt`
      - `safe/.artifacts/impl_09_final_release/artifacts/`
      - `validator/`
      - `validator/artifacts/debs/local/libexif/`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator-report.md`
      
      Specific Things To Verify
      - usage/metadata/XML/thumbnail/dependent-client failures assigned to phase 3 are covered by local regressions or a documented no-op disposition
      - local safe tests and package-backed checks pass using reuse-only package artifacts
      - the check03 validator artifact root has no non-safety usage failures
      - package, lock, copied debs, and result JSON provenance match current parent HEAD
      
      ## `check_03_usage_software_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_03_fix_cli_usage_metadata_failures`
      - Purpose: Confirm usage-class failures are locally covered and no non-safety usage cases still fail.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `cargo test --manifest-path safe/Cargo.toml --release`
        - `bash safe/tests/run-original-test-suite.sh`
        - `bash safe/tests/run-cve-regressions.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-check03 --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
        - `cd validator && jq -e '.source_cases >= 5 and .usage_cases >= 130 and (.passed + .failed == .cases)' artifacts/libexif-safe-check03/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe-check03/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .override_debs_installed != true) | .testcase_id' artifacts/libexif-safe-check03/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .status=="failed" and .kind=="usage" and (((.testcase_id // "") | test("invalid|truncated|corrupt|malformed|timeout|crash|no-exif-error"; "i")) | not)) | .testcase_id' artifacts/libexif-safe-check03/port/results/libexif/*.json)"`
      
      End your response with exactly one of these verdict lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_03_usage_senior_tester
    type: check
    bounce_target: impl_03_fix_cli_usage_metadata_failures
    prompt: |-
      Reviewer role: Senior tester reviewer for `check_03_usage_senior_tester`.
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      Fixed bounce target: `impl_03_fix_cli_usage_metadata_failures`
      
      You are reviewing the work from `impl_03_fix_cli_usage_metadata_failures`. Run the commands listed below exactly where applicable, inspect their outputs and artifacts, and verify the phase-specific requirements. Do not refresh package artifacts except through commands that explicitly set `LIBEXIF_REQUIRE_REUSE=1`; do not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      
      Preexisting Inputs
      - `safe/src/`
      - `safe/tests/`
      - `safe/.artifacts/impl_09_final_release`
      - `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt`
      - `safe/.artifacts/impl_09_final_release/artifacts/`
      - `validator/artifacts/debs/local/libexif/`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator/artifacts/libexif-safe-check03/port/results/libexif/`
      - `validator/artifacts/libexif-safe-check03/port/results/libexif/summary.json`
      - `validator-report.md`
      
      Specific Things To Verify
      - usage/metadata/XML/thumbnail/dependent-client failures assigned to phase 3 are covered by local regressions or a documented no-op disposition
      - local safe tests and package-backed checks pass using reuse-only package artifacts
      - the check03 validator artifact root has no non-safety usage failures
      - package, lock, copied debs, and result JSON provenance match current parent HEAD
      
      ## `check_03_usage_senior_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_03_fix_cli_usage_metadata_failures`
      - Purpose: Review whether usage fixes preserve upstream libexif semantics instead of hard-coding validator strings.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `git show --stat --format=fuller HEAD`
        - `git diff HEAD~1..HEAD -- safe/src safe/tests validator-report.md`
        - `rg -n 'validator|usage-exif-cli|EXIF_TAG_|mnote_|thumbnail|xml|machine' safe/tests validator-report.md`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-export-compare.sh`
        - `jq -e '.source_cases >= 5 and .usage_cases >= 130 and (.passed + .failed == .cases)' validator/artifacts/libexif-safe-check03/port/results/libexif/summary.json`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `test -z "$(jq -r --arg head "$(git rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' validator/artifacts/libexif-safe-check03/port/results/libexif/*.json)"`
        - `test -z "$(jq -r 'select(.testcase_id and .status=="failed" and .kind=="usage" and (((.testcase_id // "") | test("invalid|truncated|corrupt|malformed|timeout|crash|no-exif-error"; "i")) | not)) | .testcase_id' validator/artifacts/libexif-safe-check03/port/results/libexif/*.json)"`
      
      End your response with exactly one of these verdict lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_04_fix_malformed_input_safety_failures
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_04_fix_malformed_input_safety_failures` (`Fix Malformed Input, Crash, Timeout, and Memory-Safety Validator Failures`).
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      
      This prompt is self-contained. The agent sees only this prompt. Consume or update the listed Preexisting Inputs in place. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist. Preserve unrelated user work.
      
      Workflow topology is fixed by `.plan/workflow-structure.yaml`: execute this implement phase as `impl_04_fix_malformed_input_safety_failures` and leave its verifier phases immediately after it in this order: `check_04_safety_software_tester`, `check_04_safety_senior_tester`. Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists. Use only the fixed verifier `bounce_target` values from the structure file.
      
      Commit work to git before yielding. Stage only files that are in scope for this phase and do not stage validator run artifacts, build artifacts, or unrelated work.
      
      # Phase Name
      
      Fix Malformed Input, Crash, Timeout, and Memory-Safety Validator Failures
      
      # Implement Phase ID
      
      `impl_04_fix_malformed_input_safety_failures`
      
      # Preexisting Inputs
      
      - Failure table entries in `validator-report.md` classified as malformed input, crash, timeout, resource exhaustion, panic, no-EXIF error, MakerNote safety, or memory-safety regressions.
      - Phase-1 through phase-3 validator artifacts and report entries, including `validator/artifacts/libexif-safe/**`, `validator/artifacts/libexif-safe-check02/**`, and `validator/artifacts/libexif-safe-check03/**`.
      - Existing `validator/` checkout created or updated only by phase 1. This phase must use that checkout and must not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      - Validator logs/casts and result JSON for failing malformed-input cases.
      - `safe/`, `original/`, `dependents.json`, `relevant_cves.json`, `all_cves.json`, `test-original.sh`, `safe/PORT.md`, `safe/tests/**/*`, and `safe/.artifacts/impl_09_final_release`; consume these existing artifacts in place and do not recollect, rediscover, regenerate, or replace them from scratch.
      - `safe/tests/cve_regressions.rs` and `safe/tests/cve-regressions-manifest.txt`.
      - Parser, object, runtime, and MakerNote implementation files: `safe/src/parser/*`, `safe/src/object/*`, `safe/src/runtime/*`, and `safe/src/mnote/*.rs`.
      - Existing local harnesses: `safe/tests/*.rs`, `safe/tests/run-cve-regressions.sh`, `safe/tests/run-original-test-suite.sh`, `safe/tests/run-export-compare.sh`, `safe/tests/run-c-compile-smoke.sh`, `safe/tests/run-package-build.sh`, copied original C/shell tests, smoke C tests, and MakerNote fixtures under `safe/tests/testdata/*`.
      - Validator reference facts from phase 1: reference validator `main` commit `5d908be26e33f071e119ffe1a52e3149f1e5ec4e`; Docker-based Ubuntu 24.04 matrix; `original` and package-oriented `port` modes; `port` mode requires `<override-deb-root>/<library>/*.deb` and `--port-deb-lock`; this validator revision has no direct library-path mode.
      - Validator libexif manifest facts: 5 source cases and 130 usage cases, 135 total; source case ids `compile-link-smoke`, `invalid-data-handling`, `jpeg-exif-c-api-parse`, `maker-note-handling`, and `tag-lookup-value-formatting`; usage cases run the Ubuntu `exif` CLI against overridden `libexif12` and `libexif-dev`.
      
      # New Outputs
      
      - Minimal malformed-input regression tests.
      - Parser/object/MakerNote/runtime fixes that reject or safely handle the inputs.
      - Refreshed `safe/.artifacts/impl_09_final_release` package artifacts for the phase commit.
      - Re-copied canonical override `.deb`s under `validator/artifacts/debs/local/libexif/`.
      - Regenerated `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json` for the phase commit.
      - Updated `validator-report.md` with safety-class dispositions and retest results.
      - One parent-repo commit.
      
      # File Changes
      
      - Prefer `safe/tests/cve_regressions.rs` for CVE-like malformed input cases.
      - Use `safe/tests/validator_regressions.rs` for non-CVE validator safety cases.
      - Likely implementation files: `safe/src/parser/*`, `safe/src/object/*`, `safe/src/mnote/*.rs`, and `safe/src/runtime/mem.rs`.
      - Always update `validator-report.md`.
      - Do not modify validator scripts for legitimate malformed/crash/timeout/safety failures.
      - Treat `validator/`, `validator/artifacts/`, `safe/.artifacts/`, and `safe/target/` as non-committed run/build artifacts.
      
      # Implementation Details
      
      - Execute linearly. Do not introduce `parallel_groups`, workflow indirection, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or `bounce_targets` lists. This phase's verifiers are explicit top-level `check` phases with fixed `bounce_target: impl_04_fix_malformed_input_safety_failures`.
      - Start only from failures assigned to this phase in `validator-report.md`. Consume existing prior artifacts instead of regenerating setup artifacts.
      - Preserve the package artifact contract: refresh the package root only with `PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`; check phases must use `LIBEXIF_REQUIRE_REUSE=1` when invoking `safe/tests/run-package-build.sh` or package-backed harnesses.
      - Use validator `port` mode with local override packages. Do not run `make fetch-port-debs`; do not use direct library-path mode; do not add noncanonical `.deb`s unless a validator log proves another canonical package is required.
      - Maintain the local lock at `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json` with schema `1`, mode `port`, one `libexif` library entry, `repository: local:/home/yans/safelibs/pipeline/ports/port-libexif/safe`, `commit: <current parent HEAD>`, `release_tag: local-<short-parent-commit>`, `tag_ref: refs/tags/local-<short-parent-commit>`, `unported_original_packages: []`, canonical apt-order `debs` entries for `libexif12` and `libexif-dev`, exact filenames, `Architecture` from `dpkg-deb --field`, lowercase SHA256, byte size, `generated_at: "1970-01-01T00:00:00Z"`, `source_config: "repositories.yml"`, and `source_inventory: "local:/home/yans/safelibs/pipeline/ports/port-libexif/safe"`.
      - Reproduce each failure with the smallest local byte fixture or constructed buffer. Do not rely only on Docker logs.
      - Every new regression test name or comment must reference the validator testcase id or malformed/crash/timeout/safety failure class it covers.
      - For parser offset/length defects, use checked arithmetic, bounded slice helpers, `ParseBudget`, and explicit IFD bounds rather than wrapping arithmetic.
      - For loader failures, verify both incremental `exif_loader_write` and `exif_loader_get_data` behavior.
      - For MakerNote malformed data, bound count, component, offset, and payload calculations before allocating or copying.
      - If a failure exposes a panic crossing an FFI boundary, fix the underlying unwrap/panic source and ensure exported functions still route through `panic_boundary`.
      - If no safety-class failures exist, update `validator-report.md` with that no-op disposition and commit it. Use `git commit --allow-empty` only if the report already contains the exact disposition and no file changed.
      - Commit before yielding, then refresh package root, recopy override `.deb`s, and regenerate the local lock for the new `HEAD`.
      - Validator `port` mode can return success even when testcase failures are recorded. Inspect the relevant `*/port/results/libexif/summary.json` and result JSON files explicitly instead of trusting process status.
      - Every post-commit checker must be able to assert: `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt` equals current parent `HEAD`; local lock commit equals current parent `HEAD`; copied override `.deb` filenames and SHA256 values match canonical files in `safe/.artifacts/impl_09_final_release/artifacts/`; every checked result JSON has `port_commit` equal to current parent `HEAD`.
      
      # Verification Phases
      
      ## `check_04_safety_software_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_04_fix_malformed_input_safety_failures`
      - Purpose: Verify malformed-input fixes with focused safety regressions and a full validator rerun.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `cargo test --manifest-path safe/Cargo.toml --test cve_regressions -- --test-threads=1`
        - `test ! -f safe/tests/validator_regressions.rs || cargo test --manifest-path safe/Cargo.toml --test validator_regressions -- --test-threads=1`
        - `cargo test --manifest-path safe/Cargo.toml --release`
        - `bash safe/tests/run-cve-regressions.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-check04 --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
        - `cd validator && jq -e '.source_cases >= 5 and .usage_cases >= 130 and (.passed + .failed == .cases)' artifacts/libexif-safe-check04/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe-check04/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .override_debs_installed != true) | .testcase_id' artifacts/libexif-safe-check04/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .status=="failed" and (((.testcase_id // "") | test("invalid|truncated|corrupt|malformed|timeout|crash|maker-note|no-exif-error"; "i")) or (((.error // "") | test("invalid|truncated|corrupt|malformed|timed out|crash|panic|segmentation"; "i"))))) | .testcase_id' artifacts/libexif-safe-check04/port/results/libexif/*.json)"`
      
      ## `check_04_safety_senior_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_04_fix_malformed_input_safety_failures`
      - Purpose: Review bounds checks, allocator behavior, panic boundaries, and absence of validator-only bypasses.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `git diff HEAD~1..HEAD -- safe/src/parser safe/src/object safe/src/mnote safe/src/runtime safe/tests validator-report.md`
        - `rg -n 'validator|invalid|truncated|corrupt|malformed|timeout|crash|maker-note|no-exif-error|CVE' safe/tests validator-report.md`
        - `rg -n 'checked_add|checked_mul|ParseBudget|MAX_|slice_range|copy_nonoverlapping|from_raw_parts|unwrap\(|panic!' safe/src/parser safe/src/object safe/src/mnote safe/tests`
        - `bash safe/tests/run-original-test-suite.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `test -z "$(jq -r --arg head "$(git rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' validator/artifacts/libexif-safe-check04/port/results/libexif/*.json)"`
        - `test -z "$(jq -r 'select(.testcase_id and .status=="failed" and (((.testcase_id // "") | test("invalid|truncated|corrupt|malformed|timeout|crash|maker-note|no-exif-error"; "i")) or (((.error // "") | test("invalid|truncated|corrupt|malformed|timed out|crash|panic|segmentation"; "i"))))) | .testcase_id' validator/artifacts/libexif-safe-check04/port/results/libexif/*.json)"`
      
      # Success Criteria
      
      - New safety regressions pass and reference the relevant validator testcase id or malformed/crash/timeout/safety failure class.
      - No malformed-input, crash, timeout, panic, no-EXIF error, MakerNote safety, or memory-safety failures assigned to this phase remain in `validator/artifacts/libexif-safe-check04/port/results/libexif/*.json`.
      - Bounds, allocation, slice, loader, MakerNote, and panic-boundary changes address root causes rather than bypassing validator cases.
      - Package/result/lock provenance is tied to current parent `HEAD`.
      - Parent repo commit contains only `validator-report.md` and necessary `safe/` files; it excludes nested validator files and run artifacts.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. If no safety-class failures exist, commit the `validator-report.md` no-op disposition, using `git commit --allow-empty` only when the exact disposition is already present and no file changed; immediately after the commit, refresh the package root, recopy canonical override `.deb`s, and regenerate the local lock for the new `HEAD`.
  - id: check_04_safety_software_tester
    type: check
    bounce_target: impl_04_fix_malformed_input_safety_failures
    prompt: |-
      Reviewer role: Software tester reviewer for `check_04_safety_software_tester`.
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      Fixed bounce target: `impl_04_fix_malformed_input_safety_failures`
      
      You are reviewing the work from `impl_04_fix_malformed_input_safety_failures`. Run the commands listed below exactly where applicable, inspect their outputs and artifacts, and verify the phase-specific requirements. Do not refresh package artifacts except through commands that explicitly set `LIBEXIF_REQUIRE_REUSE=1`; do not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      
      Preexisting Inputs
      - `safe/Cargo.toml`
      - `safe/tests/cve_regressions.rs`
      - `safe/tests/`
      - `safe/.artifacts/impl_09_final_release`
      - `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt`
      - `safe/.artifacts/impl_09_final_release/artifacts/`
      - `validator/`
      - `validator/artifacts/debs/local/libexif/`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator-report.md`
      
      Specific Things To Verify
      - malformed-input, crash, timeout, panic, no-EXIF, MakerNote, and memory-safety failures assigned to phase 4 are covered by focused regressions or a documented no-op disposition
      - bounds, allocation, loader, MakerNote, and panic-boundary behavior remains robust
      - the check04 validator artifact root has no safety-class failures
      - package, lock, copied debs, and result JSON provenance match current parent HEAD
      
      ## `check_04_safety_software_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_04_fix_malformed_input_safety_failures`
      - Purpose: Verify malformed-input fixes with focused safety regressions and a full validator rerun.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `cargo test --manifest-path safe/Cargo.toml --test cve_regressions -- --test-threads=1`
        - `test ! -f safe/tests/validator_regressions.rs || cargo test --manifest-path safe/Cargo.toml --test validator_regressions -- --test-threads=1`
        - `cargo test --manifest-path safe/Cargo.toml --release`
        - `bash safe/tests/run-cve-regressions.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-check04 --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
        - `cd validator && jq -e '.source_cases >= 5 and .usage_cases >= 130 and (.passed + .failed == .cases)' artifacts/libexif-safe-check04/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe-check04/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .override_debs_installed != true) | .testcase_id' artifacts/libexif-safe-check04/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .status=="failed" and (((.testcase_id // "") | test("invalid|truncated|corrupt|malformed|timeout|crash|maker-note|no-exif-error"; "i")) or (((.error // "") | test("invalid|truncated|corrupt|malformed|timed out|crash|panic|segmentation"; "i"))))) | .testcase_id' artifacts/libexif-safe-check04/port/results/libexif/*.json)"`
      
      End your response with exactly one of these verdict lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_04_safety_senior_tester
    type: check
    bounce_target: impl_04_fix_malformed_input_safety_failures
    prompt: |-
      Reviewer role: Senior tester reviewer for `check_04_safety_senior_tester`.
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      Fixed bounce target: `impl_04_fix_malformed_input_safety_failures`
      
      You are reviewing the work from `impl_04_fix_malformed_input_safety_failures`. Run the commands listed below exactly where applicable, inspect their outputs and artifacts, and verify the phase-specific requirements. Do not refresh package artifacts except through commands that explicitly set `LIBEXIF_REQUIRE_REUSE=1`; do not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      
      Preexisting Inputs
      - `safe/src/parser/`
      - `safe/src/object/`
      - `safe/src/mnote/`
      - `safe/src/runtime/`
      - `safe/tests/`
      - `safe/.artifacts/impl_09_final_release`
      - `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt`
      - `safe/.artifacts/impl_09_final_release/artifacts/`
      - `validator/artifacts/debs/local/libexif/`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator/artifacts/libexif-safe-check04/port/results/libexif/`
      - `validator/artifacts/libexif-safe-check04/port/results/libexif/summary.json`
      - `validator-report.md`
      
      Specific Things To Verify
      - malformed-input, crash, timeout, panic, no-EXIF, MakerNote, and memory-safety failures assigned to phase 4 are covered by focused regressions or a documented no-op disposition
      - bounds, allocation, loader, MakerNote, and panic-boundary behavior remains robust
      - the check04 validator artifact root has no safety-class failures
      - package, lock, copied debs, and result JSON provenance match current parent HEAD
      
      ## `check_04_safety_senior_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_04_fix_malformed_input_safety_failures`
      - Purpose: Review bounds checks, allocator behavior, panic boundaries, and absence of validator-only bypasses.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `git diff HEAD~1..HEAD -- safe/src/parser safe/src/object safe/src/mnote safe/src/runtime safe/tests validator-report.md`
        - `rg -n 'validator|invalid|truncated|corrupt|malformed|timeout|crash|maker-note|no-exif-error|CVE' safe/tests validator-report.md`
        - `rg -n 'checked_add|checked_mul|ParseBudget|MAX_|slice_range|copy_nonoverlapping|from_raw_parts|unwrap\(|panic!' safe/src/parser safe/src/object safe/src/mnote safe/tests`
        - `bash safe/tests/run-original-test-suite.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `test -z "$(jq -r --arg head "$(git rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' validator/artifacts/libexif-safe-check04/port/results/libexif/*.json)"`
        - `test -z "$(jq -r 'select(.testcase_id and .status=="failed" and (((.testcase_id // "") | test("invalid|truncated|corrupt|malformed|timeout|crash|maker-note|no-exif-error"; "i")) or (((.error // "") | test("invalid|truncated|corrupt|malformed|timed out|crash|panic|segmentation"; "i"))))) | .testcase_id' validator/artifacts/libexif-safe-check04/port/results/libexif/*.json)"`
      
      End your response with exactly one of these verdict lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_05_fix_remaining_or_validator_bugs
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_05_fix_remaining_or_validator_bugs` (`Fix Remaining Failures or Document Validator Bugs`).
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      
      This prompt is self-contained. The agent sees only this prompt. Consume or update the listed Preexisting Inputs in place. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist. Preserve unrelated user work.
      
      Workflow topology is fixed by `.plan/workflow-structure.yaml`: execute this implement phase as `impl_05_fix_remaining_or_validator_bugs` and leave its verifier phases immediately after it in this order: `check_05_remaining_software_tester`, `check_05_remaining_senior_tester`. Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists. Use only the fixed verifier `bounce_target` values from the structure file.
      
      Commit work to git before yielding. Stage only files that are in scope for this phase and do not stage validator run artifacts, build artifacts, or unrelated work.
      
      # Phase Name
      
      Fix Remaining Failures or Document Validator Bugs
      
      # Implement Phase ID
      
      `impl_05_fix_remaining_or_validator_bugs`
      
      # Preexisting Inputs
      
      - All prior reports and validator artifacts, including `validator-report.md`, `validator/artifacts/libexif-safe/**`, `validator/artifacts/libexif-safe-check02/**`, `validator/artifacts/libexif-safe-check03/**`, and `validator/artifacts/libexif-safe-check04/**`.
      - Any validator failures not resolved by phases 2 through 4.
      - Existing `validator/` checkout created or updated only by phase 1. This phase must use that checkout and must not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      - Validator testcase scripts and logs for remaining failures, especially `validator/tests/libexif/tests/cases/**`.
      - `safe/`, `original/`, `dependents.json`, `relevant_cves.json`, `all_cves.json`, `test-original.sh`, `safe/PORT.md`, `safe/tests/**/*`, and `safe/.artifacts/impl_09_final_release`; consume these existing artifacts in place and do not recollect, rediscover, regenerate, or replace them from scratch.
      - Existing local harnesses: `safe/tests/*.rs`, `safe/tests/run-cve-regressions.sh`, `safe/tests/run-original-test-suite.sh`, `safe/tests/run-export-compare.sh`, `safe/tests/run-c-compile-smoke.sh`, `safe/tests/run-package-build.sh`, copied original C/shell tests, smoke C tests, and MakerNote fixtures under `safe/tests/testdata/*`.
      - Root-cause code and package surfaces under `safe/src/**/*`, `safe/include/libexif/*`, `safe/debian/*`, `safe/libexif.pc.in`, `safe/libexif-uninstalled.pc.in`, and `safe/build.rs`.
      - Validator reference facts from phase 1: reference validator `main` commit `5d908be26e33f071e119ffe1a52e3149f1e5ec4e`; Docker-based Ubuntu 24.04 matrix; `original` and package-oriented `port` modes; `port` mode requires `<override-deb-root>/<library>/*.deb` and `--port-deb-lock`; this validator revision has no direct library-path mode.
      - Validator libexif manifest facts: 5 source cases and 130 usage cases, 135 total; source case ids `compile-link-smoke`, `invalid-data-handling`, `jpeg-exif-c-api-parse`, `maker-note-handling`, and `tag-lookup-value-formatting`; usage cases run the Ubuntu `exif` CLI against overridden `libexif12` and `libexif-dev`; usage failures are libexif ABI/behavior regressions unless evidence proves a validator bug.
      
      # New Outputs
      
      - Additional minimal local regressions and safe fixes for remaining legitimate libexif-safe failures.
      - Or a narrowly documented validator bug waiver with evidence and, only if unavoidable, a local scoped skip for a single validator testcase.
      - Updated `validator-report.md`.
      - Refreshed `safe/.artifacts/impl_09_final_release` package artifacts for the phase commit.
      - Re-copied canonical override `.deb`s under `validator/artifacts/debs/local/libexif/`.
      - Regenerated `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json` for the phase commit.
      - One parent-repo commit.
      
      # File Changes
      
      - Conditional `safe/tests/validator_regressions.rs`, `safe/tests/cve_regressions.rs`, `safe/tests/smoke/*`, and root-cause `safe/src/**/*` files.
      - `validator-report.md`.
      - Local nested-validator changes are allowed only for a documented validator bug and must be limited to one affected testcase under `validator/tests/libexif/tests/cases/**`; do not stage them in the parent repo.
      - Treat `validator/`, `validator/artifacts/`, `safe/.artifacts/`, and `safe/target/` as non-committed run/build artifacts.
      
      # Implementation Details
      
      - Execute linearly. Do not introduce `parallel_groups`, workflow indirection, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or `bounce_targets` lists. This phase's verifiers are explicit top-level `check` phases with fixed `bounce_target: impl_05_fix_remaining_or_validator_bugs`.
      - Rerun or inspect the latest validator artifact root and list every remaining failure.
      - Preserve the package artifact contract: refresh the package root only with `PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`; check phases must use `LIBEXIF_REQUIRE_REUSE=1` when invoking `safe/tests/run-package-build.sh` or package-backed harnesses.
      - Use validator `port` mode with local override packages. Do not run `make fetch-port-debs`; do not use direct library-path mode; do not add noncanonical `.deb`s unless a validator log proves another canonical package is required.
      - Maintain the local lock at `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json` with schema `1`, mode `port`, one `libexif` library entry, `repository: local:/home/yans/safelibs/pipeline/ports/port-libexif/safe`, `commit: <current parent HEAD>`, `release_tag: local-<short-parent-commit>`, `tag_ref: refs/tags/local-<short-parent-commit>`, `unported_original_packages: []`, canonical apt-order `debs` entries for `libexif12` and `libexif-dev`, exact filenames, `Architecture` from `dpkg-deb --field`, lowercase SHA256, byte size, `generated_at: "1970-01-01T00:00:00Z"`, `source_config: "repositories.yml"`, and `source_inventory: "local:/home/yans/safelibs/pipeline/ports/port-libexif/safe"`.
      - For each failure, decide whether it is a legitimate libexif-safe compatibility/safety issue or a validator bug.
      - For legitimate libexif-safe issues, add a minimal regression under `safe/`, fix `safe/`, refresh packages, rerun tests, and report the fix.
      - Every new regression test name or comment must reference the validator testcase id or failure class it covers.
      - For validator bugs, document original libexif behavior, Ubuntu baseline evidence, upstream source evidence, or testcase-script evidence. Record testcase id, validator commit, script path, observed command, expected behavior, actual behavior, evidence, justification, and why this is a validator bug in `validator-report.md`.
      - Do not use broad filters, global skips, or result rewriting. If a skip is required, skip only the single proven-bug testcase, preserve the testcase id and overall case count, and implement the local skip inside that one validator script with waiver output rather than deleting or renaming the case.
      - Never stage the nested validator patch in the parent repo.
      - This is the final phase allowed to make `libexif-safe` behavior changes for validator failures. It must reach a clean validator summary after any documented single-testcase validator-bug waiver.
      - If no remaining failures exist, update `validator-report.md` with that no-op disposition and commit it. Use `git commit --allow-empty` only if the report already contains the exact disposition and no file changed.
      - Commit before yielding, then refresh package root, recopy override `.deb`s, and regenerate the local lock for the new `HEAD`.
      - Validator `port` mode can return success even when testcase failures are recorded. Inspect the relevant `*/port/results/libexif/summary.json` and result JSON files explicitly instead of trusting process status.
      - Every post-commit checker must be able to assert: `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt` equals current parent `HEAD`; local lock commit equals current parent `HEAD`; copied override `.deb` filenames and SHA256 values match canonical files in `safe/.artifacts/impl_09_final_release/artifacts/`; every checked result JSON has `port_commit` equal to current parent `HEAD`.
      
      # Verification Phases
      
      ## `check_05_remaining_software_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_05_fix_remaining_or_validator_bugs`
      - Purpose: Verify every remaining validator failure is fixed or explicitly waived as a validator bug.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `cargo test --manifest-path safe/Cargo.toml --release`
        - `bash safe/tests/run-cve-regressions.sh`
        - `bash safe/tests/run-original-test-suite.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-c-compile-smoke.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-export-compare.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-check05 --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
        - `cd validator && jq -e '.failed == 0 and .passed == .cases and .source_cases >= 5 and .usage_cases >= 130' artifacts/libexif-safe-check05/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe-check05/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .override_debs_installed != true) | .testcase_id' artifacts/libexif-safe-check05/port/results/libexif/*.json)"`
      
      ## `check_05_remaining_senior_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_05_fix_remaining_or_validator_bugs`
      - Purpose: Review catch-all fixes and any validator bug waiver for rigor, scope, and traceability.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `git show --stat --format=fuller HEAD`
        - `git diff HEAD~1..HEAD`
        - `rg -n 'validator|usage-exif-cli|compile-link-smoke|invalid-data-handling|jpeg-exif-c-api-parse|maker-note-handling|tag-lookup-value-formatting|truncated|malformed|waiver' safe/tests validator-report.md`
        - `grep -n 'Validator bug waivers' -A80 validator-report.md`
        - `git -C validator diff -- tests/libexif || true`
        - `jq -e '.failed == 0 and .passed == .cases and .source_cases >= 5 and .usage_cases >= 130' validator/artifacts/libexif-safe-check05/port/results/libexif/summary.json`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `test -z "$(jq -r --arg head "$(git rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' validator/artifacts/libexif-safe-check05/port/results/libexif/*.json)"`
      
      # Success Criteria
      
      - Full local validator matrix passes after any explicitly documented single-testcase validator-bug waiver.
      - Every remaining failure has a cited validator log path and disposition in `validator-report.md`.
      - Any validator local patch is scoped to one testcase, preserves testcase identity/case count, and is documented with validator commit `5d908be26e33f071e119ffe1a52e3149f1e5ec4e` or the actual recorded checkout commit if different.
      - Parent repo commit does not include nested validator files.
      - Package/result/lock provenance is tied to current parent `HEAD`.
      - This phase leaves no known validator failures for phase 6 to fix in `safe/`.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. If no remaining failures exist, commit the `validator-report.md` no-op disposition, using `git commit --allow-empty` only when the exact disposition is already present and no file changed; immediately after the commit, refresh the package root, recopy canonical override `.deb`s, and regenerate the local lock for the new `HEAD`.
  - id: check_05_remaining_software_tester
    type: check
    bounce_target: impl_05_fix_remaining_or_validator_bugs
    prompt: |-
      Reviewer role: Software tester reviewer for `check_05_remaining_software_tester`.
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      Fixed bounce target: `impl_05_fix_remaining_or_validator_bugs`
      
      You are reviewing the work from `impl_05_fix_remaining_or_validator_bugs`. Run the commands listed below exactly where applicable, inspect their outputs and artifacts, and verify the phase-specific requirements. Do not refresh package artifacts except through commands that explicitly set `LIBEXIF_REQUIRE_REUSE=1`; do not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      
      Preexisting Inputs
      - `safe/Cargo.toml`
      - `safe/tests/`
      - `safe/.artifacts/impl_09_final_release`
      - `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt`
      - `safe/.artifacts/impl_09_final_release/artifacts/`
      - `validator/`
      - `validator/artifacts/debs/local/libexif/`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator-report.md`
      
      Specific Things To Verify
      - all remaining validator failures are fixed or narrowly documented as validator bugs
      - any local nested-validator waiver is scoped to one proven testcase and is not staged in the parent repo
      - the check05 validator artifact root is clean with zero failures
      - package, lock, copied debs, and result JSON provenance match current parent HEAD
      
      ## `check_05_remaining_software_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_05_fix_remaining_or_validator_bugs`
      - Purpose: Verify every remaining validator failure is fixed or explicitly waived as a validator bug.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `cargo test --manifest-path safe/Cargo.toml --release`
        - `bash safe/tests/run-cve-regressions.sh`
        - `bash safe/tests/run-original-test-suite.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-c-compile-smoke.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-export-compare.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-check05 --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
        - `cd validator && jq -e '.failed == 0 and .passed == .cases and .source_cases >= 5 and .usage_cases >= 130' artifacts/libexif-safe-check05/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe-check05/port/results/libexif/*.json)"`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .override_debs_installed != true) | .testcase_id' artifacts/libexif-safe-check05/port/results/libexif/*.json)"`
      
      End your response with exactly one of these verdict lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_05_remaining_senior_tester
    type: check
    bounce_target: impl_05_fix_remaining_or_validator_bugs
    prompt: |-
      Reviewer role: Senior tester reviewer for `check_05_remaining_senior_tester`.
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      Fixed bounce target: `impl_05_fix_remaining_or_validator_bugs`
      
      You are reviewing the work from `impl_05_fix_remaining_or_validator_bugs`. Run the commands listed below exactly where applicable, inspect their outputs and artifacts, and verify the phase-specific requirements. Do not refresh package artifacts except through commands that explicitly set `LIBEXIF_REQUIRE_REUSE=1`; do not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      
      Preexisting Inputs
      - `safe/tests/`
      - `safe/.artifacts/impl_09_final_release`
      - `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt`
      - `safe/.artifacts/impl_09_final_release/artifacts/`
      - `validator/`
      - `validator/artifacts/debs/local/libexif/`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator/artifacts/libexif-safe-check05/port/results/libexif/`
      - `validator/artifacts/libexif-safe-check05/port/results/libexif/summary.json`
      - `validator-report.md`
      
      Specific Things To Verify
      - all remaining validator failures are fixed or narrowly documented as validator bugs
      - any local nested-validator waiver is scoped to one proven testcase and is not staged in the parent repo
      - the check05 validator artifact root is clean with zero failures
      - package, lock, copied debs, and result JSON provenance match current parent HEAD
      
      ## `check_05_remaining_senior_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_05_fix_remaining_or_validator_bugs`
      - Purpose: Review catch-all fixes and any validator bug waiver for rigor, scope, and traceability.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `git show --stat --format=fuller HEAD`
        - `git diff HEAD~1..HEAD`
        - `rg -n 'validator|usage-exif-cli|compile-link-smoke|invalid-data-handling|jpeg-exif-c-api-parse|maker-note-handling|tag-lookup-value-formatting|truncated|malformed|waiver' safe/tests validator-report.md`
        - `grep -n 'Validator bug waivers' -A80 validator-report.md`
        - `git -C validator diff -- tests/libexif || true`
        - `jq -e '.failed == 0 and .passed == .cases and .source_cases >= 5 and .usage_cases >= 130' validator/artifacts/libexif-safe-check05/port/results/libexif/summary.json`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `test -z "$(jq -r --arg head "$(git rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' validator/artifacts/libexif-safe-check05/port/results/libexif/*.json)"`
      
      End your response with exactly one of these verdict lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_06_final_validator_report
    type: implement
    bounce_target: null
    prompt: |-
      Role: Software engineer implementing phase `impl_06_final_validator_report` (`Final Validator Run, Proof, Site Check, and Report`).
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      
      This prompt is self-contained. The agent sees only this prompt. Consume or update the listed Preexisting Inputs in place. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist. Preserve unrelated user work.
      
      Workflow topology is fixed by `.plan/workflow-structure.yaml`: execute this implement phase as `impl_06_final_validator_report` and leave its verifier phases immediately after it in this order: `check_06_final_software_tester`, `check_06_final_senior_tester`. Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists. Use only the fixed verifier `bounce_target` values from the structure file.
      
      Commit work to git before yielding. Stage only files that are in scope for this phase and do not stage validator run artifacts, build artifacts, or unrelated work.
      
      # Phase Name
      
      Final Validator Run, Proof, Site Check, and Report
      
      # Implement Phase ID
      
      `impl_06_final_validator_report`
      
      # Preexisting Inputs
      
      - All safe fixes and local regressions from prior phases.
      - Refreshed `safe/.artifacts/impl_09_final_release`.
      - Existing `validator/` checkout created or updated only by phase 1. This phase must use that checkout and must not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      - Existing local override root `validator/artifacts/debs/local/libexif/`.
      - Existing local lock `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`.
      - Phase-5 clean validator artifact root `validator/artifacts/libexif-safe-check05/`.
      - `validator-report.md`.
      - `safe/`, `original/`, `dependents.json`, `relevant_cves.json`, `all_cves.json`, `test-original.sh`, `safe/PORT.md`, `safe/tests/**/*`, and `safe/.artifacts/impl_09_final_release`; consume these existing artifacts in place and do not recollect, rediscover, regenerate, or replace them from scratch.
      - Existing local harnesses: `safe/tests/*.rs`, `safe/tests/run-cve-regressions.sh`, `safe/tests/run-original-test-suite.sh`, `safe/tests/run-export-compare.sh`, `safe/tests/run-c-compile-smoke.sh`, and `safe/tests/run-package-build.sh`.
      - Validator reference facts from phase 1: reference validator `main` commit `5d908be26e33f071e119ffe1a52e3149f1e5ec4e`; Docker-based Ubuntu 24.04 matrix; `original` and package-oriented `port` modes; `port` mode requires `<override-deb-root>/<library>/*.deb` and `--port-deb-lock`; this validator revision has no direct library-path mode.
      - Validator libexif manifest facts: 5 source cases and 130 usage cases, 135 total; source case ids `compile-link-smoke`, `invalid-data-handling`, `jpeg-exif-c-api-parse`, `maker-note-handling`, and `tag-lookup-value-formatting`; usage cases run the Ubuntu `exif` CLI against overridden `libexif12` and `libexif-dev`.
      
      # New Outputs
      
      - Final `validator-report.md` summary committed before the post-commit final run.
      - `validator/artifacts/libexif-safe-final/**` post-commit final run artifacts.
      - `validator/artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json`.
      - `validator/artifacts/libexif-safe-final-site/**` rendered site if proof succeeds.
      - One parent-repo commit.
      
      # File Changes
      
      - `validator-report.md`.
      - No `safe/` source changes in this phase. `impl_05_fix_remaining_or_validator_bugs` was the last phase allowed to make libexif-safe behavior changes for validator failures.
      - If the final run finds a libexif-safe behavior failure, document the failure in `validator-report.md`, commit the report, and let the fixed-bounce final checker fail. Do not bounce to an earlier implement phase.
      - Treat `validator/`, `validator/artifacts/`, `safe/.artifacts/`, and `safe/target/` as non-committed run/build artifacts.
      
      # Implementation Details
      
      1. Execute linearly. Do not introduce `parallel_groups`, workflow indirection, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or `bounce_targets` lists. This phase's verifiers are explicit top-level `check` phases with fixed `bounce_target: impl_06_final_validator_report`.
      2. Preserve the package artifact contract: refresh the package root only with `PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`; check phases must use `LIBEXIF_REQUIRE_REUSE=1` when invoking `safe/tests/run-package-build.sh` or package-backed harnesses.
      3. Use validator `port` mode with local override packages. Do not run `make fetch-port-debs`; do not use direct library-path mode; do not add noncanonical `.deb`s unless a validator log proves another canonical package is required.
      4. Maintain the local lock at `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json` with schema `1`, mode `port`, one `libexif` library entry, `repository: local:/home/yans/safelibs/pipeline/ports/port-libexif/safe`, `commit: <current parent HEAD>`, `release_tag: local-<short-parent-commit>`, `tag_ref: refs/tags/local-<short-parent-commit>`, `unported_original_packages: []`, canonical apt-order `debs` entries for `libexif12` and `libexif-dev`, exact filenames, `Architecture` from `dpkg-deb --field`, lowercase SHA256, byte size, `generated_at: "1970-01-01T00:00:00Z"`, `source_config: "repositories.yml"`, and `source_inventory: "local:/home/yans/safelibs/pipeline/ports/port-libexif/safe"`.
      5. Confirm phase 5 produced a clean validator result and current package/lock provenance.
      6. Run a candidate final matrix and proof into `validator/artifacts/libexif-safe-final-candidate` using the current lock. If it fails, update `validator-report.md` with the failure evidence and do not claim final success.
      7. Update `validator-report.md` with stable final content: validator commit, safe fix commits, package artifact filenames and SHA256 values, commands executed, original baseline summary, candidate/final artifact paths, failures found, fixes applied, validator bug waivers or `None`, and a final clean-run statement that names `validator/artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json` as the post-commit proof path.
      8. Final success requires a clean full libexif validator run against the local override packages, a proof artifact for that same run, proof/result `port_commit` equal to current parent `HEAD`, and a `validator-report.md` summary containing validator commit, commands executed, testcase counts, failures found, fixes applied, skipped validator bugs if any, and final status.
      9. Commit `validator-report.md` before yielding any final result.
      10. Immediately after that commit, refresh `safe/.artifacts/impl_09_final_release`, recopy canonical `.deb`s, and regenerate `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json` for the final parent `HEAD`.
      11. Remove stale final artifacts:
          - `cd validator && rm -rf artifacts/libexif-safe-final artifacts/libexif-safe-final-site`
      12. Rerun the final local override matrix after the report commit:
          - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-final --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
          - `cd validator && jq -e '.failed == 0 and .passed == .cases and .source_cases >= 5 and .usage_cases >= 130' artifacts/libexif-safe-final/port/results/libexif/summary.json`
      13. Generate the post-commit proof:
          - `cd validator && python3 tools/verify_proof_artifacts.py --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-final --proof-output artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json --mode port --library libexif --require-casts --min-source-cases 5 --min-usage-cases 130 --min-cases 135`
      14. Render and verify the review site when proof succeeds:
          - `cd validator && python3 tools/render_site.py --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-final --proof-path artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json --output-root artifacts/libexif-safe-final-site`
          - `cd validator && bash scripts/verify-site.sh --config repositories.yml --tests-root tests --artifacts-root artifacts/libexif-safe-final --proof-path artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json --site-root artifacts/libexif-safe-final-site --library libexif`
      15. Do not edit or recommit `validator-report.md` after the post-commit final run. Step 7 must word the report so the post-commit artifact path and checker assertions complete provenance without another commit cycle.
      16. Validator `port` mode can return success even when testcase failures are recorded. Inspect `validator/artifacts/libexif-safe-final/port/results/libexif/summary.json` and result JSON files explicitly.
      17. Every post-commit checker must be able to assert: `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt` equals current parent `HEAD`; local lock commit equals current parent `HEAD`; copied override `.deb` filenames and SHA256 values match canonical files in `safe/.artifacts/impl_09_final_release/artifacts/`; every checked result JSON has `port_commit` equal to current parent `HEAD`; proof has `libraries[] | select(.library=="libexif") | .port_commit` equal to current parent `HEAD`.
      
      # Verification Phases
      
      ## `check_06_final_software_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_06_final_validator_report`
      - Purpose: Independently reproduce the final validator run and proof against current `HEAD`.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `cargo test --manifest-path safe/Cargo.toml --release`
        - `bash safe/tests/run-cve-regressions.sh`
        - `bash safe/tests/run-original-test-suite.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-c-compile-smoke.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-export-compare.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-final-check --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
        - `cd validator && python3 tools/verify_proof_artifacts.py --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-final-check --proof-output artifacts/libexif-safe-final-check/proof/libexif-safe-validation-proof.json --mode port --library libexif --require-casts --min-source-cases 5 --min-usage-cases 130 --min-cases 135`
        - `cd validator && jq -e '.failed == 0 and .passed == .cases and .source_cases >= 5 and .usage_cases >= 130' artifacts/libexif-safe-final-check/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe-final-check/port/results/libexif/*.json)"`
        - `jq -e --arg head "$(git rev-parse HEAD)" '.libraries[] | select(.library=="libexif") | .port_commit == $head' validator/artifacts/libexif-safe-final-check/proof/libexif-safe-validation-proof.json`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .override_debs_installed != true) | .testcase_id' artifacts/libexif-safe-final-check/port/results/libexif/*.json)"`
      
      ## `check_06_final_senior_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_06_final_validator_report`
      - Purpose: Final review of report completeness, committed scope, post-commit final artifacts, and clean run claims.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `git log --oneline --decorate -n 12`
        - `git status --short --ignored`
        - `git diff --name-only HEAD~6..HEAD || true`
        - `grep -E 'Validator commit|Checks executed|Failures found|Fixes applied|Final clean run|Validator bug waivers' validator-report.md`
        - `git -C validator rev-parse HEAD`
        - `git -C validator status --short`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `jq -e '.failed == 0 and .passed == .cases and .source_cases >= 5 and .usage_cases >= 130' validator/artifacts/libexif-safe-final/port/results/libexif/summary.json`
        - `test -z "$(jq -r --arg head "$(git rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' validator/artifacts/libexif-safe-final/port/results/libexif/*.json)"`
        - `test -f validator/artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json`
        - `jq -e --arg head "$(git rev-parse HEAD)" '.libraries[] | select(.library=="libexif") | .port_commit == $head' validator/artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json`
      
      # Success Criteria
      
      - Final post-commit result summary reports zero failures and all expected cases passed.
      - Final proof exists and has `port_commit` equal to current parent `HEAD`.
      - Final rendered site is produced by `python3 tools/render_site.py` and validated by `bash scripts/verify-site.sh`.
      - Final report includes validator commit, checks executed, failures found, fixes applied, validator bug waivers, and final status.
      - No nested validator artifacts are committed in the parent repo.
      
      # Critical Files
      
      - `.gitignore`: must ignore `/validator/`, `/safe/.artifacts/`, and `/safe/target/`.
      - `validator-report.md`: primary persistent report for setup, validator commit, commands, testcase counts, original baseline, local override results, failure classes, regression tests, fixes, validator bug waivers, package artifacts, proof path, and final status.
      - `safe/tests/validator_regressions.rs`: preferred integration test file for validator-derived ABI and behavior regressions that do not belong to existing CVE tests.
      - `safe/tests/cve_regressions.rs`: extend only for malformed-input or CVE-like validator regressions.
      - `safe/tests/smoke/public-api-smoke.c`, `safe/tests/smoke/validator-compile-link.c` if added, and `safe/tests/run-c-compile-smoke.sh`: extend only for compile/link/header/pkg-config regressions.
      - `safe/src/parser/limits.rs`, `safe/src/parser/data_load.rs`, `safe/src/parser/loader.rs`, and `safe/src/parser/data_save.rs`: parser resource limits, JPEG/EXIF payload discovery, loader behavior, serialization, and malformed input handling.
      - `safe/src/object/data.rs`, `safe/src/object/content.rs`, and `safe/src/object/entry.rs`: object graph ownership, entry lookup/removal, formatting, fix-up, thumbnail state, and public ABI behavior.
      - `safe/src/tables/tag_table.rs` and `safe/src/primitives/utils.rs`: tag metadata, IFD-specific lookup behavior, and byte-order scalar helpers.
      - `safe/src/mnote/*.rs`: MakerNote load/save/count/name/title/description/value behavior.
      - `safe/src/runtime/mem.rs` and `safe/src/runtime/log.rs`: allocator and logging behavior if validator exposes ownership, callback, or variadic logging regressions.
      - `safe/include/libexif/*.h`, `safe/libexif.pc.in`, `safe/libexif-uninstalled.pc.in`, `safe/debian/*`, and `safe/build.rs`: package, header, generated symbol, and install surface for compile/link and validator Docker override failures.
      - `safe/.artifacts/impl_09_final_release`: reusable package artifact root, refreshed by script but never committed.
      - `validator/`: nested external validator checkout. It is used for runs and may contain local artifacts or one narrowly scoped local skip for a proven validator bug, but it is never committed in the parent repo.
      
      # Final Verification
      
      Run from `/home/yans/safelibs/pipeline/ports/port-libexif` after all implementation phases.
      
      1. Local safe validation:
         - `cargo test --manifest-path safe/Cargo.toml --release`
         - `bash safe/tests/run-cve-regressions.sh`
         - `bash safe/tests/run-original-test-suite.sh`
         - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-c-compile-smoke.sh`
         - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-export-compare.sh`
         - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
      2. Provenance assertions:
         - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
         - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
         - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
      3. Validator final run:
         - `cd validator && python3 tools/testcases.py --config repositories.yml --tests-root tests --library libexif --check --min-source-cases 5 --min-usage-cases 130 --min-cases 135`
         - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-final --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
         - `cd validator && python3 tools/verify_proof_artifacts.py --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-final --proof-output artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json --mode port --library libexif --require-casts --min-source-cases 5 --min-usage-cases 130 --min-cases 135`
         - `cd validator && python3 tools/render_site.py --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-final --proof-path artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json --output-root artifacts/libexif-safe-final-site`
         - `cd validator && bash scripts/verify-site.sh --config repositories.yml --tests-root tests --artifacts-root artifacts/libexif-safe-final --proof-path artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json --site-root artifacts/libexif-safe-final-site --library libexif`
      4. Result assertions:
         - `jq -e '.failed == 0 and .passed == .cases and .source_cases >= 5 and .usage_cases >= 130' validator/artifacts/libexif-safe-final/port/results/libexif/summary.json`
         - `test -z "$(jq -r --arg head "$(git rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' validator/artifacts/libexif-safe-final/port/results/libexif/*.json)"`
         - `jq -e --arg head "$(git rev-parse HEAD)" '.libraries[] | select(.library=="libexif") | .port_commit == $head' validator/artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json`
         - `grep -E 'Validator commit|Checks executed|Failures found|Fixes applied|Final clean run|Validator bug waivers' validator-report.md`
      5. Git hygiene:
         - `git status --short --ignored`
         - Confirm parent commits include only `.gitignore`, `validator-report.md`, and the safe files needed for tested fixes.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. Commit `validator-report.md` before the post-commit final run, then refresh the package root, recopy canonical override `.deb`s, regenerate the local lock for the new `HEAD`, run the final matrix/proof/site commands, and do not edit or recommit `validator-report.md` afterward.
  - id: check_06_final_software_tester
    type: check
    bounce_target: impl_06_final_validator_report
    prompt: |-
      Reviewer role: Software tester reviewer for `check_06_final_software_tester`.
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      Fixed bounce target: `impl_06_final_validator_report`
      
      You are reviewing the work from `impl_06_final_validator_report`. Run the commands listed below exactly where applicable, inspect their outputs and artifacts, and verify the phase-specific requirements. Do not refresh package artifacts except through commands that explicitly set `LIBEXIF_REQUIRE_REUSE=1`; do not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      
      Preexisting Inputs
      - `safe/Cargo.toml`
      - `safe/tests/`
      - `safe/.artifacts/impl_09_final_release`
      - `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt`
      - `safe/.artifacts/impl_09_final_release/artifacts/`
      - `validator/`
      - `validator/artifacts/debs/local/libexif/`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator-report.md`
      
      Specific Things To Verify
      - final report is complete and committed before the post-commit final run
      - final validator run, proof, and rendered site artifacts exist where required
      - final or independently reproduced validator summary is clean with zero failures
      - package, lock, copied debs, result JSON, and proof provenance match current parent HEAD
      
      ## `check_06_final_software_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_06_final_validator_report`
      - Purpose: Independently reproduce the final validator run and proof against current `HEAD`.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `cargo test --manifest-path safe/Cargo.toml --release`
        - `bash safe/tests/run-cve-regressions.sh`
        - `bash safe/tests/run-original-test-suite.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-c-compile-smoke.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-export-compare.sh`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `cd validator && bash test.sh --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-final-check --mode port --override-deb-root artifacts/debs/local --port-deb-lock artifacts/libexif-safe/proof/local-port-debs-lock.json --library libexif --record-casts`
        - `cd validator && python3 tools/verify_proof_artifacts.py --config repositories.yml --tests-root tests --artifact-root artifacts/libexif-safe-final-check --proof-output artifacts/libexif-safe-final-check/proof/libexif-safe-validation-proof.json --mode port --library libexif --require-casts --min-source-cases 5 --min-usage-cases 130 --min-cases 135`
        - `cd validator && jq -e '.failed == 0 and .passed == .cases and .source_cases >= 5 and .usage_cases >= 130' artifacts/libexif-safe-final-check/port/results/libexif/summary.json`
        - `cd validator && test -z "$(jq -r --arg head "$(git -C .. rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' artifacts/libexif-safe-final-check/port/results/libexif/*.json)"`
        - `jq -e --arg head "$(git rev-parse HEAD)" '.libraries[] | select(.library=="libexif") | .port_commit == $head' validator/artifacts/libexif-safe-final-check/proof/libexif-safe-validation-proof.json`
        - `cd validator && test -z "$(jq -r 'select(.testcase_id and .override_debs_installed != true) | .testcase_id' artifacts/libexif-safe-final-check/port/results/libexif/*.json)"`
      
      End your response with exactly one of these verdict lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_06_final_senior_tester
    type: check
    bounce_target: impl_06_final_validator_report
    prompt: |-
      Reviewer role: Senior tester reviewer for `check_06_final_senior_tester`.
      
      Repository Root: `/home/yans/safelibs/pipeline/ports/port-libexif`
      Fixed bounce target: `impl_06_final_validator_report`
      
      You are reviewing the work from `impl_06_final_validator_report`. Run the commands listed below exactly where applicable, inspect their outputs and artifacts, and verify the phase-specific requirements. Do not refresh package artifacts except through commands that explicitly set `LIBEXIF_REQUIRE_REUSE=1`; do not run `git pull`, `git fetch`, reclone the validator, or run `make fetch-port-debs`.
      
      Preexisting Inputs
      - `safe/.artifacts/impl_09_final_release`
      - `safe/.artifacts/impl_09_final_release/metadata/source-commit.txt`
      - `safe/.artifacts/impl_09_final_release/artifacts/`
      - `validator/`
      - `validator/artifacts/debs/local/libexif/`
      - `validator/artifacts/libexif-safe/proof/local-port-debs-lock.json`
      - `validator/artifacts/libexif-safe-final/port/results/libexif/`
      - `validator/artifacts/libexif-safe-final/port/results/libexif/summary.json`
      - `validator/artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json`
      - `validator-report.md`
      
      Specific Things To Verify
      - final report is complete and committed before the post-commit final run
      - final validator run, proof, and rendered site artifacts exist where required
      - final or independently reproduced validator summary is clean with zero failures
      - package, lock, copied debs, result JSON, and proof provenance match current parent HEAD
      
      ## `check_06_final_senior_tester`
      
      - Type: `check`
      - Fixed `bounce_target`: `impl_06_final_validator_report`
      - Purpose: Final review of report completeness, committed scope, post-commit final artifacts, and clean run claims.
      - Commands:
        - `cd /home/yans/safelibs/pipeline/ports/port-libexif`
        - `git log --oneline --decorate -n 12`
        - `git status --short --ignored`
        - `git diff --name-only HEAD~6..HEAD || true`
        - `grep -E 'Validator commit|Checks executed|Failures found|Fixes applied|Final clean run|Validator bug waivers' validator-report.md`
        - `git -C validator rev-parse HEAD`
        - `git -C validator status --short`
        - `LIBEXIF_REQUIRE_REUSE=1 PACKAGE_BUILD_ROOT="$PWD/safe/.artifacts/impl_09_final_release" bash safe/tests/run-package-build.sh`
        - `test "$(cat safe/.artifacts/impl_09_final_release/metadata/source-commit.txt)" = "$(git rev-parse HEAD)"`
        - `for package in libexif12 libexif-dev; do srcs=$(find safe/.artifacts/impl_09_final_release/artifacts -maxdepth 1 -type f -name "${package}_*.deb" | sort); test "$(printf '%s\n' "$srcs" | sed '/^$/d' | wc -l)" -eq 1; src=$(printf '%s\n' "$srcs"); dst="validator/artifacts/debs/local/libexif/$(basename "$src")"; test -f "$dst"; test "$(sha256sum "$src" | cut -d' ' -f1)" = "$(sha256sum "$dst" | cut -d' ' -f1)"; done`
        - `test "$(jq -r '.libraries[] | select(.library=="libexif") | .commit' validator/artifacts/libexif-safe/proof/local-port-debs-lock.json)" = "$(git rev-parse HEAD)"`
        - `jq -e '.failed == 0 and .passed == .cases and .source_cases >= 5 and .usage_cases >= 130' validator/artifacts/libexif-safe-final/port/results/libexif/summary.json`
        - `test -z "$(jq -r --arg head "$(git rev-parse HEAD)" 'select(.testcase_id and .port_commit != $head) | .testcase_id' validator/artifacts/libexif-safe-final/port/results/libexif/*.json)"`
        - `test -f validator/artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json`
        - `jq -e --arg head "$(git rev-parse HEAD)" '.libraries[] | select(.library=="libexif") | .port_commit == $head' validator/artifacts/libexif-safe-final/proof/libexif-safe-validation-proof.json`
      
      End your response with exactly one of these verdict lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
